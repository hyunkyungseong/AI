# 상단 GNB(전역 내비게이션) 배치

> 상태: 🔲 미착수 — 사용자가 2026-08-12에 "다음 세션에서 하고 싶다"고만 남기고 이번 세션은 종료.
> 아직 요구사항이 전혀 논의되지 않았음 — 다음 세션 시작 시 Read-only 분석 전에 반드시 사용자에게
> 먼저 확인부터 할 것(선계획후코딩 원칙).

## 다음 세션 시작 시 확인할 것 (제안 — 실제 착수 시 사용자와 다시 확정)

1. "상단 GNB 배치"가 정확히 뭘 의미하는지 확인
   - 지금 화면 구조(현재 `frontend/components/Dashboard.tsx` 최상위 탭 5개 + 로그인 등)를 먼저
     Read로 파악하고, 사용자가 원하는 게 다음 중 어느 쪽인지 확인:
     - 지금 있는 최상위 탭 메뉴의 위치·스타일만 조정(예: 지금은 어디 있는지부터 확인)
     - 로고·사용자명·로그아웃 버튼 등을 포함하는 새로운 상단 바(GNB) 자체를 신설
     - 반응형(모바일) 대응이 목적인지
2. 참고할 기존 화면/스크린샷이 있는지, 아니면 텍스트 설명으로 진행할지 확인
3. 설계 확정 후 Plan 작성 → 사용자 승인(ExitPlanMode) → 구현 → `tsc`·`eslint`·`build` 검증 →
   화면 테스트 체크리스트 제공 순서로 진행(이 프로젝트 표준 절차)