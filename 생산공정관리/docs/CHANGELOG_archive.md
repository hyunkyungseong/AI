# 작업현황 대시보드 생성 — 작업 이력 아카이브 (2026-05 ~ 2026-07)

> `docs/CHANGELOG.md`가 커져서 2026-08-01 이전 이력을 이 파일로 분리했습니다(2026-08-31 분리).
> 세션 시작 시 자동으로 읽지는 않으며, 과거 이력 조회가 필요할 때만 참고하세요.

---

## ✅ 완료된 작업

### 2026-05-26
- CLAUDE.md, SKILL.md 초기 파일 생성
- 프로젝트 폴더 구조 생성 (`data/`, `scripts/`, `output/`, `work/`, `docs/`, `docs/results/`)
- `운영통계자료.xlsx` → `data/` 폴더로 이동
- `작업현황-대시보드-생성-KNOWLEDGE.md` → `docs/` 폴더로 이동
- CHANGELOG.md 초기 생성

### 2026-05-29
- `data/운영통계자료.xlsx` 데이터 분석 완료 (17,674행, 13개 컬럼, 거래처 51개, 담당자 11명)
- 핵심 비즈니스 로직 전체 확정 (컬럼 정의, 청구페이지 계산 3가지 경우, 공급가액 공식)
- 거래명세서 업무 흐름 확정 (생성 → 이메일 발송 → 발송여부 저장)
- 거래처 마스터 테이블 구조 확정 (5종 단가 + 수신이메일)
- 기술 스택 확정 및 패키지 설치 (Python 3.12.5, Streamlit 1.58.0, Plotly 6.7.0)
- `scripts/preprocess.py` 완성 및 검증
  - 추가 컬럼: 연월, 날짜, 시간대, 거래처명, 사업부, 확정청구페이지
  - DM사업부(강서윤·노재민·김성수·김희원·임병민) / N사업부(나머지) 구분
  - 결과: `work/processed.pkl` (17,673행)
- `scripts/init_db.py` 완성 → `work/dashboard.db` 생성
  - 거래처마스터 테이블: 미래에셋증권 초기 데이터 적재
  - 거래명세서이력 테이블 생성
- `scripts/app.py` 대시보드 기초 개발 완료 (4탭 구조 + 사업부 필터)
  - 탭 1: 작업현황 요약 — 핵심 지표·비교 차트·월별 추이 구조 완성, 수치 1차 확인
  - 탭 2: 거래처별 현황 — 기본 구조 완성
  - 탭 3: 담당자별 현황 + 시간대별 히트맵 — 기본 구조 완성
  - 탭 4: 거래명세서 관리 — 미발송 목록·마스터 관리·발송이력 구조 완성

### 2026-06-01
- 탭 1 작업현황 요약 수정 완료
  - 월별 추이 그래프 삭제
  - 이번달 현황 카드에 "출력자재사용량(장)" 추가 (출력페이지와 봉입건수 사이)
  - 전월대비·전년동월대비 막대차트 순서 변경 (이전기간 왼쪽, 이번달 오른쪽)
- 탭 4 거래명세서 관리 수정 완료
  - 사이드바 거래처·담당자 필터가 미발송 목록에도 적용되도록 수정
  - 미발송 목록 상하 배치 적용

### 2026-06-02
- 탭 2(거래처별 현황) 차트 수치 레이블 단위 변경
  - 출력페이지 순위 · 봉입건수 순위 막대그래프 → 소수점 1자리 M 단위 표기 (예: 1.2M)
- 탭 3(담당자별 현황) 차트 수치 레이블 단위 변경
  - 담당자별 출력페이지 · 봉입건수 막대그래프 → 소수점 1자리 M 단위 표기
- 탭 1(작업현황 요약) 지표 카드 타이틀 "확정청구페이지" → "청구페이지"로 변경 (값 유지)
- 탭 4(거래명세서 관리) 미발송 목록 개선
  - "장수" 필드 추가 (업무의뢰서 단위 합계)
  - 전체선택/취소 버튼 2개 → 체크박스 1개("선택(전체)")로 변경
  - 목록 위에 "미발송 N건" 실제 건수 표시 추가
  - 필드명 "확정청구페이지" → "청구페이지"로 변경 (데이터는 확정청구페이지 값 유지)
  - 선택 항목 세부내역에서 "확정청구페이지" 컬럼 제거
- 기술 검토: Streamlit NumberColumn은 천단위 콤마 미지원 → 우측정렬 유지를 위해 현상 수용
- 탭 4(거래명세서 관리) 발행요청목록 탭 구성
  - 상단 목록 + 선택된 업무의뢰서 세부내역 구조로 변경
  - 공급가액·세액·합계 천단위 콤마 TextColumn 표시
- 탭 4(거래명세서 관리) 거래명세서 발행 2단계 워크플로우 구현
  - 명칭 정비: "미발송 목록" → "미발행 목록", "거래명세서 생성" → "거래명세서 요청", "발송이력" → "발행요청목록"
  - "거래명세서 요청" 버튼: 거래명세서이력 DB에 발행대기(발행여부=0) 저장 → 미발행 목록에서 즉시 제거 → 발행요청목록 탭 자동 이동
  - 발행요청목록: 사이드바 필터(거래처·담당자) 적용, 발행여부 "발행대기"/"발행완료" 표시
  - "거래명세서 발행" 버튼: 선택된 항목 발행여부 0→1 업데이트 (발행완료 처리)
- 버그 수정: 미발행 목록 필터링 오류 (업무의뢰서번호 타입 불일치 — float vs string)
  - DB 저장 시 문자열 `"12345"` ↔ processed.pkl `float 12345.0` → `int` 통일로 해결
- 탭 4(거래명세서 관리) 발행요청목록 전면 개편
  - 기존: 거래명세서 단위(1건/invoice) 표시
  - 변경: 업무의뢰서 단위로 펼쳐서 미발행 목록과 동일한 필드 구조로 표시
    - 담당자·의뢰서번호·사업부·거래처명·업무명·업무명상세·작업일자·청구페이지·장수·봉입건수·발행여부
  - "거래명세서 발행" 버튼: 업무의뢰서번호 → 이력 id 역추적 후 발행완료 처리
- 탭 4 발행요청목록 탭 자동 이동 안정화
  - `t4_탭이동` 카운터(2회) 도입 → 거래명세서 요청 후 체크박스 첫 클릭 시 탭 복귀 버그 해결
- 발행요청목록 "거래명세서 발행" 버튼 위치 변경 → 세부내역 위로 이동
- 개발/테스트 데이터 초기화 (거래명세서이력 테이블 전체 삭제)

### 2026-06-04
- `운영통계자료.xlsx` B열 "등록자" 컬럼 추가 반영 (`preprocess.py` 수정 불필요 — 자동 통과)
- 탭 3(담당자별 현황) 작업자 현황 통합
  - 마케팅담당자(DM·N사업부 색상 유지)와 작업자(초록)를 동일 차트에 합쳐 표시
  - 명칭 변경: "등록자" → "작업자" (차트 범례·히트맵 타이틀)
  - 막대 순서 고정: 작업자 → DM사업부 → N사업부 (각 그룹 내 값 내림차순)
  - 두 역할 모두 해당하는 담당자 → "(작업자)" 접미사로 구분 표시 (예: 박현수·박현수(작업자))
  - 시간대 히트맵 나란히 배치: 마케팅담당자(YlOrRd) / 작업자(Blues)
  - 작업자 데이터 없을 시 안내 메시지 표시
- 배치 파일 오류 수정 (`ts` 명령어 오류)
  - 원인: `streamlit run app.py --server.runOnSave true` 에서 `true`를 별도 명령으로 오인
  - 해결: 해당 옵션 제거 + `.streamlit/config.toml` 생성 (`runOnSave = true` 설정)
- 막대그래프 M 단위 표기 개선
  - 반올림 → 절삭(내림) 방식으로 변경 (예: 8,990,000 → 8.99M, 기존 9.0M → 8.99M)
  - 소수 2자리 표기 (기존 1자리 → 2자리)
  - 대상: 탭 2(거래처별 현황) · 탭 3(담당자별 현황) 4개 차트 일괄 적용
- 탭 4(거래명세서 관리) 발행요청목록 검색 기능 추가 (승인자용)
  - 파이프(`|`) 구분 의뢰서번호 붙여넣기 → `검색` 버튼으로 필터링
  - `전체 보기` 버튼으로 검색 해제
  - 검색 결과 건수 / 전체 건수 표시
- 탭 4(거래명세서 관리) 발행요청목록 선택 시 합계 표시
  - 선택된 업무의뢰서 상단에 장수·봉입건수·출력페이지·청구페이지 합계 카드 표시
  - HTML 커스텀 렌더링 (항목 간격 최소화, 타이틀 > 수치 크기 구성)

### 2026-06-07
- CLAUDE.md 비개발자 관리 가이드 추가 및 관련 충돌 3곳 수정
  - `.claude/plans/` 폴더 생성 및 단계별 플랜 파일 3개 생성
  - ExitPlanMode 승인 절차 명문화
- `운영통계자료.xlsx` 구조 변경 반영
  - 업무의뢰서번호·작업내역서번호 위치 변경 → 컬럼명 기준 읽기로 코드 영향 없음
  - 반제품 컬럼(Y/N) 신규 추가 → `apply_반제품_logic()` 구현 (Y이면 건수=0)
- `자재사용현황.xlsx` 신규 연동
  - `load_자재_summary()` 구현 — 자재사용현황.xlsx를 직접 읽어 업무의뢰서번호별 집계 후 summary에 합산
  - 수량 과다 집계 버그 수정 (join 방식 → 소스 파일 직접 집계 방식으로 변경)
- KNOWLEDGE.md 컬럼 구조 15개로 업데이트 및 자재사용현황 구조 문서화
- 탭2(거래처별 현황) 범례 겹침 수정 — 두 차트 범례를 차트 상단으로 이동
- 탭4(미발행 목록) UI 개선
  - 탭 내 거래처 필터 삭제 (사이드바 필터로 통합)
  - 업무의뢰서번호 검색 필터 추가 (파이프 구분자 붙여넣기 방식, 미발행 목록·선택 영역 공통 적용)
  - 용지수량·봉투수량·삽지수량 컬럼 추가 (미발행 목록·선택된 업무의뢰서 합계)
- 탭4(발행요청목록) UI 개선
  - 용지수량·봉투수량·삽지수량 컬럼 추가
  - 선택된 업무의뢰서 합계 카드에 용지·봉투·삽지 합계 추가
- 탭4 전반 UI 고도화
  - 선택된 업무의뢰서 세부내역: 거래처명·작업일자 컬럼 추가
  - 용지/봉투/삽지 수량은 의뢰서 단위 합계라 행별 표기 불가 → 합계 텍스트에서만 제공 (KeyError 버그 수정)
  - NumberColumn format `",.0f"` 오류 수정 → `"%,d"` (천단위 콤마 + 우측정렬 정상 동작)
  - 미발행 목록·발행요청목록·세부내역 전체 순번 No 컬럼(1부터 시작) 추가, hide_index=True 적용
  - 미발행 목록 선택된 업무의뢰서 합계 텍스트: "확정청구페이지" → "청구페이지", 장수 항목 추가
  - 미발행 목록·발행요청목록: 선택~업무명 컬럼 좌우 스크롤 시 고정(pinned=True) — 7개 컬럼
  - 세부내역 테이블: No·업무의뢰서번호·거래처명·작업일자·업무명 컬럼 고정(5개)
- plan_2단계_탭4기능마무리.md 단가 구조 재설계 내용 추가
  - 현재 거래처명 단위 단가 → 고객사·계약·업무·작업 계층 구조로 전환 필요 확인
  - 진행 전 확인 필요 3가지 항목 플랜에 기재
- "반제품" 필드명 → "반제품여부"로 변경 반영
  - `scripts/preprocess.py` `apply_반제품_logic()` 내 컬럼명 수정
  - KNOWLEDGE.md 컬럼 구조표 및 주요 결정사항 필드명 수정
- 0단계 검증 완료 — 반제품여부 실데이터 포함 새 운영통계자료.xlsx 정상 업로드 확인

### 2026-06-17
- 배포용 배치파일 3개 신규 생성 (다른 PC 전달용)
  - `처음실행_자동설치.bat`: Python 3.12 자동 설치 → 패키지 설치 → 대시보드 실행 (1파일 자동화)
  - `패키지_설치.bat`: Python 3.14 환경용 패키지 설치 전용 (`py -3.14 -m pip`)
  - `대시보드_실행_3.14.bat`: Python 3.14용 대시보드 실행 배치
- 탭4 미발행/발행요청목록 합계 영역에 **추가용지** 항목 추가
  - 계산식: 장수 - 봉입건수 (장수 또는 봉입건수가 0이면 추가용지=0)
  - 표시 위치: 용지와 삽지 사이
- 예상공급가 추가봉입비 계산 공식 변경
  - 변경 전: `삽지_사용량_합 × 추가봉입단가`
  - 변경 후: `(삽지_사용량_합 + 추가용지) × 추가봉입단가`
- 거래처 마스터 삭제 기능 추가 (탭4 거래처 마스터 탭)
  - 표 맨 앞 열에 "선택" 체크박스 추가 → 체크 후 삭제 버튼 클릭
  - `@st.dialog` 확인 팝업 (거래처명 표시 + 삭제/취소 버튼)
  - 복수 선택 일괄 삭제 지원 / 미선택 시 삭제 버튼 비활성화
  - 저장·삭제 버튼 한 줄 나란히 배치 (원래 크기 유지)
- 버그 수정: 단가 계산 NaN 오류 (거래처마스터 단가 필드 null → NaN 미처리)
  - 원인: `봉투제작단가` 등 DB null 값이 pandas NaN으로 읽혀 `NaN or 0 = NaN` (NaN은 truthy)
  - 결과: `총액 = NaN` → `예상공급가액 = None` → "단가 미등록" 오표시
  - 수정: 단가맵 생성 시 `pd.to_numeric(...).fillna(0)` 적용

### 2026-06-21
- 거래명세서 Excel 자동 생성 기능 완성 (탭4 "거래명세서 발행" 버튼)
  - 최초 생성 방식: Excel COM(win32com) — 이후 zipfile 방식으로 교체됨(2026-06-22 참고)
  - 파일명 형식: `yyyyMMdd_HHmm_거래처명_업무명.xlsx`
  - 품목 순서: 출력비 → 봉입비 → 출력자재비 → 봉입자재비 → 추가봉입비 → 삽지비
  - D14 금액한글: `num2words(lang='ko')` 라이브러리 사용
  - 다운로드 버튼 캐시 방지: `t4c_dl_version` 카운터로 key 변경
- 예상공급가액 행별 계산 개선 완료
  - 동일 의뢰서 내 복수 작업명(개인일반/기업일반/기업예외 등) 각각 단가 적용 후 합산
  - 자재사용현황.xlsx → (업무의뢰서번호, 작업이름) 기준 집계로 개선 (기존: 의뢰서 전체 합산)
- 봉투 자재명 각대/대봉투 별도 단가 관리 추가
  - 자재명에 '각대' 또는 '대봉투' 포함 시 각대대봉투로 분류 (일반봉투와 분리)
  - 단가마스터에 `각대대봉투단가`(봉투제작비), `각대대봉투봉입단가`(봉입비) 필드 추가
  - DB 자동 마이그레이션 (init_db.py) 반영
  - 탭4 단가 관리 UI: 두 필드 입력란 추가 (4열×2행 레이아웃)
  - 계산식: 일반봉입료=건수×봉입단가 / 각대대봉투봉입료=각대대봉투수량×각대대봉투봉입단가
- 단가 구조 전면 재설계 완료 (거래처 기본단가 + 업무명·작업명별 예외단가)
  - 거래처마스터 단가 5개 필드 제거, 단가마스터 테이블 신규 생성
  - DB 자동 마이그레이션 (`init_db.py`): 기존 단가 → 단가마스터 기본단가(업무명·작업명=NULL)로 이관
  - 단가 조회 fallback 3단계: 거래처+업무명+작업명 → 거래처+업무명 → 거래처 기본단가 → 빈 칸
  - 단가마스터에 삽지제작단가 필드 추가 (출력/봉입/추가봉입/용지제작/봉투제작/삽지제작 6종)
  - 예상공급가 계산에 삽지제작비 반영: `삽지_사용량_합 × 삽지제작단가`
  - 단가 조회 키: `작업명` 컬럼 사용 (업무명상세 아님 — 혼동 주의)
- 탭4 거래처마스터 탭 UI 전면 개편
  - 기본정보 / 단가 관리 라디오 탭 전환 구조 (Streamlit st.tabs → radio 방식으로 변경)
  - 기본정보: 거래처 체크 → "→ 단가 관리" 버튼으로 해당 거래처 단가 탭 자동 이동
  - 단가 관리: 거래처명→업무명→작업명 3단계 cascade 드롭다운 새 단가 등록
  - 단가 입력 필드 초기값 공백 처리 (value=None, placeholder="0.00")
  - 모든 단가 필드 소수 2자리 표기 (format="%.2f")

### 2026-06-22
- 거래명세서 Excel 생성 방식 COM → zipfile+regex 로 교체
  - Excel 설치 불필요, 생성 속도 ~10초 → 1~2초
  - `data/거래명세서_템플릿_base.xlsx` 신규 생성 (가변 셀 초기화 + ShrinkToFit 스타일 포함)
  - `generate_거래명세서_excel()` 함수에서 win32com 제거, zipfile+inlineStr 방식 적용
  - sharedStrings.xml 미수정 → Excel 복구 오류 없음 확인

### 2026-06-23
- 단가 관리 UI 포커스 완성 (탭4 거래처 마스터 → 단가 관리)
  - 새 단가 추가: `st.form(enter_to_submit=False)` + JS로 Enter → 다음 필드 자동 이동 구현 ✅
  - 기존 단가 수정: data_editor 인라인 편집 → 읽기 전용 표 + 체크박스 선택 후 별도 편집 폼으로 UI 변경 ✅
    - AG Grid가 `isTrusted=false` 이벤트 무시하여 JS 포커스 제어 불가 → UI 구조 변경으로 해결
  - 단가 필드 클릭 시 기존 값 전체 선택 기능 추가 (재진입 후에도 동작) ✅
  - JS 리스너 관리: 플래그 방식 → 함수 참조 저장 후 제거+재등록 방식으로 교체
  - SKILL-08 업데이트
- 거래명세서 Excel 대표자 직인 자동 삽입 기능 완성 ✅
  - `data/직인.png` 파일을 zipfile 방식으로 Excel 내부에 직접 삽입 (openpyxl 불필요)
  - 위치: 공급자 성명(김형석) 셀(M11:N11 병합) 오른쪽, 마지막 글자에 살짝 겹치게
  - 크기: 1.5cm × 1.5cm (540,000 EMU)
  - Pillow 패키지 필요 → `패키지(3.14용)_설치.bat`에 `Pillow==11.2.1` 추가
  - `직인.png` 미존재 시 직인 없이 정상 생성 (조건부 삽입)
  - 위치 파라미터 주석으로 문서화 (colOff_from 값으로 미세 조정 가능)

### 2026-07-15
- 거래명세서 관리 탭4 발행요청목록 개편 ✅
  - 발행요청목록/발행완료 화면 렌더링 로직을 `_render_발행_섹션()` 공통 함수로 통합 (중복 제거)
  - "발행완료" 탭 신규 분리 — 발행요청목록은 발행대기 건만, 발행완료 탭은 발행완료 건만 표시
  - 발행요청목록에 "취소" 버튼 추가 (발행대기 요청 삭제 → 미발행 목록으로 복귀)
  - 발행완료 탭에 "발행 취소" 버튼 추가 (발송여부 되돌리기 → 발행대기로 복귀)
  - 두 탭 모두 사이드바 전체 필터(사업부·기간·거래처·담당자·업무명) 적용 (기존엔 거래처·담당자만 일부 적용됨)
  - `_발행상태변경_dialog()` 확인 다이얼로그 추가 (기존 `_거래처삭제_dialog()` 패턴 재사용)

### 2026-07-17
- 미발행 목록 탭 의뢰서번호 검색 기능 삭제 ✅
- 발행요청목록/발행완료 취소 기능 버그 수정 ✅
  - 취소 확인창 건수 불일치 수정: 화면에서 체크한 의뢰서 건수를 기준으로 표시하도록 변경 (기존엔 내부 이력ID 개수를 표시해 체크 개수와 달라 보임). 여러 의뢰서가 한 번에 묶여 요청된 경우 "일부 요청이 묶여있어 총 N건이 함께 처리됩니다" 안내 문구 추가
  - `TypeError: only list-like objects are allowed to be passed to isin()` 오류 수정: 거래처 마스터 탭(단가 관리 → 새 단가 추가)의 `선택_업무명` 변수가 사이드바 필터의 동명 변수(업무명 다중선택 리스트)를 덮어써서 발생 → `선택_업무명_단가`로 이름 변경해 충돌 해소
    - Streamlit은 비활성 탭 코드도 매 rerun마다 실행하므로, 모든 `with tab:` 블록에서 변수명이 서로 겹치지 않아야 함 (SKILL-09 참고)
- 미발행 목록 "선택된 업무의뢰서" 영역 의뢰서번호 복사(📋) 기능 삭제 ✅
- CHANGELOG.md 정리: "완료된 작업"/"완료된 작업 (계속)" 두 섹션을 날짜 오름차순 단일 섹션으로 병합
- 거래명세서번호 자동 채번 기능 추가 ✅
  - 형식: `D(DM사업부)/N(N사업부)-yyyymm-00001` (사업부·연월별 순번, 연월 바뀌면 리셋)
  - `거래명세서번호_카운터` 테이블 신규 생성, `거래명세서이력.거래명세서번호` 컬럼 추가 (`scripts/init_db.py` 마이그레이션)
  - `_발급_거래명세서번호()` 함수로 "거래명세서 요청" 클릭 시 채번, 이력 INSERT와 같은 트랜잭션으로 처리
  - 취소해도 번호 재사용 안 함 (카운터는 증가만 하는 결번 허용 방식)
  - 선택 항목의 사업부가 섞이면 "거래명세서 요청" 버튼 비활성화 + 안내 문구
  - 발행요청목록·발행완료 탭 표에 "거래명세서번호" 컬럼 추가, 요청 완료 메시지에 발급 번호 표시
- 발행요청목록·발행완료 화면 3단계 드릴다운 구조로 개편 ✅
  - **레벨1(요약)**: 거래명세서번호+업무명 기준 합산 요약만 표시 (의뢰서번호 개별 나열 대신 건수·합계)
  - **레벨2(목록)**: 레벨1에서 체크한 그룹의 의뢰서번호별 목록 — 기존 "상단 목록"을 그대로 재사용, 드릴다운 전용(체크박스에 액션 버튼 없음)
  - **레벨3(상세)**: 레벨2에서 체크한 의뢰서의 원본 작업내역 — 기존 "하단 상세" 그대로 재사용
  - "거래명세서 발행"/"취소"/"발행 취소" 버튼을 레벨1 기준으로 이전 (그룹 체크 → `_이력_id` 단위로 기존 로직 그대로 동작)
  - 사전 작업: 거래명세서번호 도입 이전의 과거 `거래명세서이력`(발행대기 1건 + 발행완료 9건) 및 채번 카운터 전체 삭제 후 재시작 (사용자 확인 후 진행)
  - Streamlit `AppTest`로 레벨1→레벨2→레벨3 드릴다운 동작을 실제 데이터로 검증 (예외 없음 확인 후 테스트 데이터 삭제)
- 발행요청목록 부분 취소 기능 추가 ("행 축소" 방식) ✅
  - 같은 거래명세서번호에 묶인 의뢰서 중 일부만 취소 가능 (기존엔 항상 이력 행 전체를 `DELETE`)
  - **레벨1 "취소"**: 체크한 업무명 그룹에 속한 의뢰서 전체 일괄 취소 (기존과 동일한 자리, 로직만 교체)
  - **레벨2 "취소"** (신규): 레벨2에서 체크한 개별 의뢰서번호만 선택적으로 취소
  - 취소분이 그 이력의 전체 목록과 같으면 기존처럼 `DELETE`(번호 결번), 일부만 선택하면 `업무의뢰서번호목록`에서 취소분만 제거하고 `UPDATE`(품목·담당자·공급가액·세액·합계는 `calc_공급가맵()`으로 재계산, 거래명세서번호는 유지)
  - `_부분취소_dialog()` 신규: 이력별로 "전체 취소(번호 결번)" / "일부 취소(번호 유지)"를 구분해 안내 후 확인
  - `_발행상태변경_dialog()`는 발행완료 탭 "발행 취소"(되돌리기) 전용으로 단순화 (부분 처리는 이번 범위 제외, 전체 이력 단위 그대로)
  - 장기 계획: MariaDB 전환 시 `거래명세서이력`을 의뢰서번호 단위로 정규화 예정 (`plan_1단계_MariaDB전환.md`에 반영)
  - 검증: Streamlit `AppTest`로 검색 필터를 이용해 실제 사용자 데이터(다른 이력 행)를 건드리지 않고 테스트 전용 이력만 격리해 부분/전체 취소 양쪽 분기 확인, SQL 실행 결과도 직접 대조 후 테스트 데이터 삭제
- 액션 버튼 위치 개편 — 그리드 상단 배치 + 다운로드 버튼 발행완료 전용화 ✅
  - 미발행 목록 "거래명세서 요청", 발행요청목록/발행완료 레벨1 버튼("거래명세서 발행"/"취소"/"발행 취소"), 레벨2 "취소" 버튼을 모두 그리드(표) **위쪽**으로 이동 (기존엔 표 아래에서 선택 후에만 나타남)
  - 버튼은 선택 여부와 무관하게 항상 표시, 선택 없이 클릭하면 "선택된 항목이 없습니다" 안내만 표시
  - 구현 근거: `st.button`/`st.data_editor`의 반환값은 코드 순서가 아니라 rerun 시점 상태를 반영하므로, 위젯 선언 위치만 위로 옮기고 클릭 처리 로직은 그리드 이후에 그대로 둠 (새 컴포넌트 불필요)
  - "거래명세서 다운로드" 버튼을 발행요청목록에서 제거하고 **발행완료 탭 전용**으로 이동 — "거래명세서 발행" 클릭 시 엑셀을 `t4d_excel_bytes` 등 발행완료 네임스페이스에 직접 저장하도록 변경
  - 검증: Streamlit `AppTest`로 버튼이 그리드보다 먼저 렌더링되는지, 빈 선택 클릭 시 안내가 뜨는지, 실제 발행 클릭 시 DB·세션 상태가 올바르게 갱신되는지 테스트 전용 이력으로 확인 (실사용자 데이터 24·27번 미접촉)
- 거래명세서 Excel 품목 행 가변(동적) 처리 + 템플릿 정리 ✅
  - `docs/작업현황-대시보드-생성-KNOWLEDGE.md`에 품명 작성 로직을 문서화하다가, 코드가 품목을 10줄(16~25행)까지만 쓰고 잘라버리는 제약을 발견 → 템플릿을 직접 열어보니 실제로는 16~29행(14줄)까지 준비돼 있었음 (여유 4줄을 못 쓰고 있었음)
  - `data/거래명세서_템플릿_base.xlsx`에서 **P23:Q33 메타정보 패널**(사업자등록증·법인명·발행일자 등, 코드 어디에서도 읽지 않는 죽은 데이터) 삭제 — Q30에 남아있던 mailto 하이퍼링크까지 함께 정리
  - `generate_거래명세서_excel()`(`scripts/app.py`): 품목이 14개를 넘으면 템플릿의 29행 뒤에 품목 행을 필요한 만큼 복제 삽입하고, 소계·합계·담당자 푸터·코드표·인쇄영역(Print Area)·시트 dimension을 전부 그만큼 아래로 밀어서 재계산하도록 구현 (14개 이하는 기존과 동일한 고정 30/31행 경로 그대로 유지 → 회귀 없음)
  - 검증: Streamlit `AppTest`로 실제 발행을 3가지 케이스(4건/21건/91건 품목)로 실행해 openpyxl로 재오픈 검증 — 품목 4건(정상 경로, 구조 완전 동일), 21건과 91건(동적 삽입 경로, 소계·합계·병합 셀 수·인쇄영역·dimension 전부 정확히 이동) 모두 확인 후 테스트 이력 삭제 (실사용자 데이터 24·27번 미접촉)
- 거래명세서 Excel 행 삽입 버그 수정 — A열 코드 누락 + 폰트 불일치 ✅
  - 사용자가 실제 발행 결과에서 A열(코드) 누락 및 일부 행 폰트가 다르게 나오는 것을 확인해 제보
  - 원인: 새 품목 행을 만들 때 복제 원본으로 쓴 **29행 자체가 템플릿의 "특수 행"**이었음 — A열 셀이 아예 없고(→ 복제된 모든 새 행에서 코드 공란), K/L/M열 폰트(fontId)도 다른 행과 달랐음(→ 이질적인 폰트로 표시)
  - 조사 결과 원본 템플릿의 **25~29행도 동일한 결함**을 이미 갖고 있었음(A열 없음, 25·29행 폰트 상이) — 품목이 정확히 10~14개라 이 행들을 그대로 쓰는 경우에도 동일 증상이 있었을 잠재 결함이었음 (이번에 처음 발견)
  - 조치 1: `data/거래명세서_템플릿_base.xlsx`의 25~29행을 20행(A열·폰트가 표준인 정상 행) 기준으로 재구성해 통일
  - 조치 2: `generate_거래명세서_excel()`의 행 복제 원본을 29행 → **20행**으로 변경
  - 재검증: 91건 품목(16~106행) 전체에서 A열 공란 0건, 전 행 스타일(s=43/58) 완전 일치 확인. 4건짜리 정상 케이스도 재확인 (변화 없음)

### 2026-07-18
- 거래명세서 Excel 품목 정렬 순서 변경 ✅ (plan_2단계 5-7 완료)
  - 변경 전: 품목(출력비→봉입비→출력자재비→봉입자재비→추가봉입비→삽지비) 우선, 그다음 작업명 순
  - 변경 후: 작업명을 먼저 묶고, 같은 작업명 안에서는 코드표 순서(P출력→M기계→E봉투→F용지→H수작업→BB중철제본→AB부가사업→D디자인)로 정렬
  - 코드 "M"이 겹치는 봉입비·추가봉입비·삽지비는 기존 순서(봉입비→추가봉입비→삽지비) 유지
  - `scripts/app.py` `generate_거래명세서_excel()`: `코드표순서`/`코드순서맵` 추가, `정렬행` 정렬 키 변경

### 2026-07-19
- 최종 아키텍처 목표 확정: Next.js(Vercel) + FastAPI + MariaDB로 전체 재구축 ✅
  - 결정: 서버는 사무실 PC 계속 사용 / 사내 전용 로그인 필요 / 점진적 전환(Streamlit 유지하며 백엔드부터 구축 후 탭 단위로 Next.js 교체)
  - 기존 `plan_1단계_MariaDB전환.md`(SQLite→MariaDB)와 `plan_4단계_FastAPI백엔드분리.md`(API 분리)를 병합 — "Streamlit이 DB 직접 접근"하는 중간 단계 생략, 처음부터 FastAPI 경유로 설계
  - `docs/작업현황-대시보드-생성-KNOWLEDGE.md` "최종 배포 목표"·"기술 환경" 섹션을 새 아키텍처로 갱신
  - Python 패키지 설치: `pymysql`, `fastapi`, `python-jose[cryptography]`, `passlib[bcrypt]` (`uvicorn`·`requests`는 이미 설치돼 있었음 확인)
- 1단계(MariaDB+FastAPI 백엔드) 착수 ✅
  - 사무실 PC(원격서버) 접근이 당장 불가능해, **로컬 PC에 이 프로젝트 전용 MariaDB를 먼저 설치 후 추후 사무실 PC로 이관**하는 순서로 진행 결정 (기존 사내 10.2 시스템과는 완전히 별개 인스턴스라 버전 무관 확인)
  - 로컬 PC에 MariaDB Server 11.4.12 + HeidiSQL 설치 완료, `dashboard` DB 생성 (사용자 직접 진행)
  - `scripts/db_config.py` 신규 (DB 접속정보 템플릿, `.gitignore` 등록 확인) / `scripts/init_db_mariadb.py` 신규 작성 및 실행 → `dashboard` DB에 6개 테이블 생성 확인(운영통계자료·거래처마스터·단가마스터·거래명세서·거래명세서_의뢰서·거래명세서번호_카운터)
  - 문서 정정: KNOWLEDGE.md 단가마스터 스키마에 누락돼 있던 각대대봉투단가·각대대봉투봉입단가 필드 추가 / 운영통계자료 스키마를 21컬럼→26컬럼+id로 정정(`반제품여부`·자재사용량 4종 누락 발견, `preprocess.py` 실제 출력 기준으로 재확인)
  - 거래명세서이력 MariaDB 정규화 설계 확정: 기존 SQLite의 JSON-in-TEXT(`업무의뢰서번호목록`) 1테이블 방식을 `거래명세서`(그룹 집계·상태) + `거래명세서_의뢰서`(의뢰서번호 목록, FK) 2테이블로 분리
  - `plan_1단계_MariaDB전환.md`에 [6단계] "로컬 PC → 사무실 PC 이관" 체크리스트 신규 추가 (mysqldump 백업/복원, utf8mb4 확인, db_config.py의 DB_HOST만 변경)
  - 2026-06-22 이후 커밋되지 않고 쌓여있던 약 1개월치 작업(탭4 거래명세서 기능 다수 개선분)을 이번 MariaDB 착수 변경분과 함께 일괄 커밋
  - `scripts/migrate_sqlite_to_mariadb.py` 신규 작성 및 실행 완료 — SQLite(`work/dashboard.db`) → 로컬 MariaDB(`dashboard`) 데이터 이관 (거래처마스터 7행·단가마스터 16행·거래명세서 2행/거래명세서_의뢰서 14행·거래명세서번호_카운터 1행, 재실행 안전한 UPSERT 방식). 이관 후 SQLite 원본과 건수 일치 확인 완료
  - 스키마 정정: `업무의뢰서번호`가 숫자가 아니라 문자열 식별자임을 확인 → `운영통계자료`·`거래명세서_의뢰서` 두 테이블 모두 INT → VARCHAR(20)로 정정 (`init_db_mariadb.py` 수정, 이미 이관된 `거래명세서_의뢰서` 14행은 `ALTER TABLE`로 안전 변환)
  - `scripts/preprocess.py`에 `save_to_mariadb()` 추가 — `processed.pkl` 저장은 유지한 채 `운영통계자료.xlsx` 전처리 결과를 MariaDB `운영통계자료` 테이블에도 TRUNCATE & INSERT로 반영(원래 [4단계] 예정 작업을 앞당김). 실행 확인 결과 **36,804행** 반영(기존 KNOWLEDGE.md에 남아있던 17,674행은 오래된 수치로 확인됨 — "데이터 현황" 섹션 갱신 필요, 별도 확인 예정)
  - 원본 `운영통계자료.xlsx`에 문서화되지 않은 컬럼 2개(`청구페이지-출력페이지`, `출력페이지보다 큰 청구페이지`) 존재 확인 — 사용자 확인 결과 미사용 컬럼, 코드 반영 안 함
  - KNOWLEDGE.md "데이터 현황" 섹션 최신화 (사용자 확인): 총 행수 17,674→36,804행, 데이터 기간 2026-04-01~05-29→2026-03-02~06-30, 거래처 수 51→62개, 거래처 목록 갱신 (담당자 11명은 변동 없음)
  - `자재사용현황` 테이블 추가 요청 반영 — `scripts/init_db_mariadb.py`에 7번째 테이블로 추가(`data/자재사용현황.xlsx` 라인 단위 원본 저장, 업무의뢰서번호 VARCHAR(20)), `preprocess.py`의 `load_and_merge_자재()`가 라인 단위 집계 결과도 함께 반환하도록 수정 후 `save_자재사용현황_to_mariadb()` 신규 추가. 실행 결과 5,984행 반영 확인 (MariaDB 테이블 총 7개로 갱신)
  - 당사 생산공정관리시스템 API 연동 결정 및 구현 완료 — 방향: **Push**(생산공정관리시스템→우리 FastAPI), **건별 실시간**(의뢰서 완료 시 전체 행 한 번에 전송), 필드는 현재 엑셀과 동일 컬럼, 기존 엑셀 배치는 당분간 병행
    - 가공 로직(반제품·거래처명·사업부·청구페이지·자재조인)을 `scripts/data_transform.py`로 분리해 `preprocess.py`(배치)·`api.py`(실시간) 공유 구조로 리팩터링, 리팩터링 후 `preprocess.py` 재실행해 동일 결과(36,804/5,984행) 확인
    - `scripts/api.py` 신규: FastAPI 앱 + `/health`(DB 연결 확인) + `POST /운영통계자료수신`(업무의뢰서 단위 수신, 재전송 시 DELETE 후 INSERT로 통째 교체)
    - 실제 requests 테스트로 확정청구페이지 그룹 계산(150 확인)·재전송 교체(2행→1행) 동작 검증 후 테스트 데이터 삭제
    - 버그 발견 및 수정: 자재종류 일부만 있는 소량 배치(실시간 API의 전형적 상황)에서 `봉투_사용량` 등 pivot 컬럼이 누락되어 `KeyError` 발생 — `merge_자재()`가 4종 컬럼을 항상 보장하도록 수정 (SKILL-12 신규)
    - `docs/API규격서.md` 신규 작성 — 당사 생산공정관리시스템 개발 담당자 전달용 요청/응답 스키마 문서
    - SKILL.md 갱신: SKILL-05(MariaDB 연결 패턴) 상태를 "미검증 초안" → "✅ 완료"로 갱신 (실사용 검증됨), SKILL-12 신규 추가
  - `GET /summary` API 구현 완료 (조회 API 8번 착수) — **A안**(app.py의 기존 pandas 집계 로직은 그대로 두고, 데이터 공급 방식만 pkl→API로 교체) 채택. `운영통계자료` 원본 행을 사업부 필터(선택)만 적용해 반환. 실제 호출 테스트로 전체 36,804행(약 27MB, 약 2초)·사업부 필터 결과 확인, `docs/API규격서.md`에 반영
  - 조회 API 8번 완료 — `GET /거래처마스터`(7건)·`GET /단가마스터`(16건)·`GET /거래명세서이력`(14건, 발송여부 필터 지원) 구현 및 테스트. `/미발행목록`·`/발행요청목록` 2개 계획이었으나, 정규화된 `거래명세서`+`거래명세서_의뢰서` JOIN 하나로 두 화면 모두 계산 가능해 `/거래명세서이력` 하나로 통합 (구 SQLite JSON 파싱 방식 불필요해짐). `docs/API규격서.md` 반영
  - 쓰기 API 9번 완료 — `POST·DELETE /거래처마스터`, `POST /단가마스터`·`PUT /단가마스터/{id}`·`DELETE /단가마스터`, `POST /거래명세서요청`(채번 포함)·`/거래명세서발행`·`/거래명세서발행취소`(추가) 구현
    - 발견: 공급가액 계산(`calc_공급가맵`)·Excel 생성(`generate_거래명세서_excel`)이 일반봉투/각대대봉투 구분을 위해 자재명 데이터가 필요한데 MariaDB `자재사용현황`엔 없어서, 계산·Excel 생성은 당분간 app.py에 남겨두기로 결정 (plan 6-2에 연결 이슈로 기록)
    - 실제 DB 대상 테스트: 거래처마스터 저장/삭제, 단가마스터 등록/수정/삭제, 거래명세서요청→발행→발행취소(채번 D-202607-00003) 전체 흐름 확인 후 baseline으로 정확히 복원
    - 발견(기존 동작, 신규 버그 아님): 단가마스터 UNIQUE 제약이 업무명·작업명 둘 다 NULL(기본단가)일 때 중복 저장을 막지 못함 (NULL끼리는 다르게 취급하는 DB 공통 특성)
  - 로그인 인증(10번) 완료 — **담당자별 개별 계정** 방식 채택. 신규 `사용자` 테이블(8번째), `scripts/auth.py`(bcrypt 해시·JWT 발급/검증·API 키 검증), `POST /login`, `scripts/create_user.py`(계정 생성 CLI) 추가. `/health`·`/login`·`/운영통계자료수신` 제외 모든 API에 로그인 토큰(JWT) 요구, `/운영통계자료수신`은 사람 로그인이 아닌 시스템 연동이라 별도 고정 API 키(`X-API-Key`)로 분리
    - `passlib[bcrypt]`가 최신 bcrypt(5.x)와 호환성 버그가 있어 `bcrypt` 라이브러리를 직접 사용하는 방식으로 구현
    - 실제 계정으로 로그인 성공/실패, 토큰 유효/무효, API 키 유효/무효 시나리오 전부 테스트 후 테스트 계정 삭제
    - `docs/API규격서.md`, KNOWLEDGE.md "기술 환경" 인증 항목 갱신
  - **[1단계]·[3단계] 완료** — MariaDB(로컬) + FastAPI 백엔드(조회·쓰기·연동·인증 API) 구축 마무리
  - **방향 전환 결정:** [4단계](Streamlit을 API 호출 방식으로 전환)를 건너뛰고 **Next.js 프론트엔드로 직행** — 두 작업 규모가 비슷한데 Next.js가 최종 목표에 바로 기여하기 때문. Streamlit(`app.py`)은 수정 없이 지금 상태(SQLite/pkl 직접 접근) 유지, 신규 `plan_5단계_Next.js프론트엔드.md`로 다음 세션에서 착수 예정(시작 화면: 탭1 작업현황요약). app.py를 API 방식으로 바꾸려던 초안 코드는 사용자 판단으로 반영 보류·폐기 — app.py 파일 자체는 수정되지 않음
    - ⚠️ 데이터 이원화 주의사항 기록: 당사 시스템이 MariaDB에만 실시간 반영하므로 Streamlit(SQLite/pkl)과 Next.js(MariaDB) 화면 숫자가 서서히 달라질 수 있음

### 2026-07-18
- 5단계(Next.js 프론트엔드) [1단계] 전체 완료 — 탭1(작업현황요약) 화면 완성 ✅
  - `frontend/` 신규 생성 (Next.js 16.2.10, App Router, TypeScript, Tailwind CSS, React 19.2.4). Node.js v24.14.0 설치 확인
  - 로그인 연동: httpOnly 쿠키 + BFF(Backend-for-Frontend) 패턴 채택 — Next.js Route Handler(`app/api/login`)가 FastAPI `/login`을 대신 호출하고 토큰을 Next.js 자체 도메인 httpOnly 쿠키에 저장(`lib/fastapi.ts`), FastAPI(`scripts/api.py`)는 코드 수정 없음. Vercel(Next.js)·사무실 PC(FastAPI) 간 Cross-Site 쿠키 설정이 필요 없어짐. `app/login/page.tsx` 로그인 화면 신규
  - 탭1(작업현황요약) `GET /summary` 연동 — 지표 카드 4개(출력페이지·출력자재사용량·봉입건수·청구페이지, 전월대비 %), 사업부별 비교·전월대비·전년동월대비 막대차트(Recharts, `dataviz` 스킬 검증 팔레트 적용)
  - 필터를 Streamlit 사이드바 대응 기능으로 신규 구현(원 계획엔 없었으나 사용자 요청으로 포함) — 왼쪽 LNB 배치, 버튼 없는 즉시반응형, 순서는 사업부→조회기간→담당자→거래처→업무명이며 상위 선택이 하위 옵션 목록을 캐스케이딩으로 좁힘. `components/MultiSelectCombo.tsx` 신규(칩 표시 검색형 콤보박스), 패널 접기/펼치기 토글 포함
  - 기준월 규칙 확정(사용자 지정): 이번 달은 아직 종료되지 않았으므로 항상 "가장 최근에 끝난 달"(오늘 기준 직전월)을 기준월로 사용 — Streamlit의 15일 기준 분기 로직 대신 단순화. 조회기간 기본값은 그 기준월의 1일~말일. 그래도 데이터가 없으면 데이터가 있는 가장 최근 월로 2차 대체
  - 검증: 실제 계정 로그인 성공/실패 화면 확인, 임시 테스트 계정으로 curl 종단 검증 후 계정 삭제, `npx tsc --noEmit` 통과, 브라우저 화면 테스트(다중선택·캐스케이딩·숨기기) 사용자 확인 완료

### 2026-07-19
- 5단계 [2단계] 완료 — 탭2(거래처별 현황) 화면 완성 ✅
  - `lib/useFilters.ts` 신규 — `Dashboard.tsx`에 있던 필터 상태·캐스케이딩 로직을 커스텀 훅으로 분리(탭마다 독립 필터 상태를 갖기 위한 사전 작업)
  - `components/FilterSidebar.tsx` 신규 — LNB 필터 패널을 `useFilters()` 반환값을 props로 받는 프리젠테이션 컴포넌트로 분리
  - `components/Dashboard.tsx` 리팩터링 — 탭 전환 상태(`"summary" | "clients"`)와 공통 헤더만 남기고, 각 탭 컴포넌트를 상시 마운트 + CSS(`hidden`)로 숨김 처리(언마운트 시 필터 상태 소실 방지)
  - `components/Tab1Summary.tsx`·`Tab2Clients.tsx` 신규 — 기존 탭1 내용 이동 + Streamlit `tab2`(사업부·거래처명 그룹 집계, 출력페이지·봉입건수 상위 20 랭킹) 이식
  - `components/RankedBarChart.tsx`·`lib/format.ts`(`toMillionLabel()`) 신규 — 탭2 전용으로 만들었으나 이후 탭3에서도 재사용
  - 버그 2건 발견·수정: ① `useFilters.ts`의 "상위 필터 변경 시 하위 선택값 정리" 로직이 `useEffect` 안에서 `setState`를 호출해 Next.js 16 신규 린트 규칙(`react-hooks/set-state-in-effect`) 위반 → 렌더링 중 조정하는 React 공식 패턴으로 교체, ② `RankedBarChart`의 렌더 순서가 배열 순서와 반대로 그려져(오름차순으로 보임) 원인이었던 불필요한 `reverse()` 제거해 내림차순(1위가 맨 위) 정상화
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과, 브라우저 화면 테스트(탭 전환·랭킹 정렬·필터 독립성) 사용자 확인 완료
- 5단계 [3단계] 완료 — 탭3(담당자별 현황) 화면 완성 ✅
  - `app/page.tsx`·`Dashboard.tsx`의 `운영통계행` 타입에 `등록자`(작업자)·`시간대` 필드 추가(기존 10개 필드로는 부족 — 백엔드는 이미 반환 중이라 프론트 매핑만 추가)
  - `lib/colors.ts` 신규 — 탭1·탭2에 중복돼 있던 DM/N 사업부 색상(`#2a78d6`/`#008300`) 상수를 통합, 작업자색상(`#e87ba4`, 카테고리 팔레트 3번째 슬롯) 추가
  - `components/Heatmap.tsx` 신규 — 범용 시퀀셜 히트맵 그리드(surface↔기준색조 보간, 호버/포커스 툴팁), Recharts에 없는 마크라 직접 구현. `dataviz` 스킬 기준으로 색상 결정(임의 지정 없이 카테고리 팔레트 슬롯·시퀀셜 규칙 그대로 적용)
  - `components/Tab3Staff.tsx` 신규 — Streamlit `tab3` 이식: 마케팅담당자+작업자 통합 막대차트 2개(기존 `RankedBarChart` 재사용, 신규 컴포넌트 불필요) + 시간대별(0~23시) 업무 집중도 히트맵 2개
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과, 브라우저 화면 테스트(탭 전환·히트맵 호버·색상 회귀) 사용자 확인 완료
- 5단계 [4-A] 완료 — 탭4(거래명세서 관리) 백엔드 준비: 자재형태 컬럼 신설 + 계산·Excel 생성 로직 FastAPI 이전 ✅
  - **결정 배경:** 탭4 이식의 핵심 걸림돌이던 "예상공급가액 계산"·"Excel 생성"이 자재형태(일반봉투/각대대봉투 구분) 데이터 부재로 API에 못 옮겨져 있던 문제(`plan_1단계_MariaDB전환.md` 6번)를, 사용자가 자재형태 컬럼을 실제로 채울 수 있다고 확인해줘서 이번에 완전히 해소
  - MariaDB `자재사용현황`에 `자재형태`(`VARCHAR(20) NULL`) 컬럼 추가 — `scripts/init_db_mariadb.py`에 기존 테이블도 안전하게 보정하는 `migrate()` 함수 신규(컬럼 존재 여부 확인 후 `ALTER TABLE`, 재실행해도 안전)
  - `scripts/data_transform.py`의 `merge_자재()`에 `_봉투종류(자재명)` 판별 로직 이식, 라인 상세 테이블(자재사용현황)은 자재형태까지 세분화해서 집계하고 운영통계자료의 4개 사용량 컬럼은 기존처럼 자재형태 무시하고 합산하는 2단계 집계로 분리
  - `scripts/preprocess.py` 재실행으로 과거 데이터까지 자재형태 일괄 반영(TRUNCATE&INSERT 방식이라 별도 백필 불필요) — 자재사용현황 6,316행(기존 5,984행에서 증가, 봉투가 일반/각대대 2행으로 분리된 결과), 분포는 봉투(일반 2,519·각대대 450)·용지 2,785·삽지 465·미구분 97건
  - 실시간 수신 경로(`POST /운영통계자료수신`)는 자재명이 없어 구분 불가한 봉투 행을 "일반봉투"로 기본 확정 저장(가장 흔한 케이스를 안전한 기본값으로 채택)
  - `scripts/billing.py` 신규 — `app.py`의 `calc_공급가맵()`·`generate_거래명세서_excel()`을 코드 변경 없이 그대로 이동(df_all·단가맵·자재map을 인자로 받도록만 파라미터화), `build_단가맵()`·`build_자재map()` 헬퍼 추가. `scripts/app.py`는 이 모듈을 import하는 얇은 래퍼로 교체(Streamlit 동작 완전히 동일 유지, `AppTest`로 무예외 확인)
  - `scripts/api.py` 신규 엔드포인트 2개: `GET /예상공급가액`(계산 결과를 업무의뢰서 단위로 반환, `/summary`의 A안과 동일 패턴), `GET /거래명세서엑셀/{no}`(언제든 재호출 가능한 Excel 다운로드 — Streamlit의 세션 임시저장 방식보다 개선)
  - **실제 버그 2건 발견·수정:** ① Starlette가 `{거래명세서번호}`처럼 한글 경로 파라미터명을 인식하지 못해 해당 라우트가 항상 404로 실패하는 문제 발견 — 파라미터명을 영문 `no`로 통일해 해결(다른 API의 `{id}`와 동일한 관례), ② MariaDB `DECIMAL` 컬럼이 pymysql에서 `Decimal`로 반환되는데 `float(0.0)` 누산 변수와 연산 시 `TypeError` 발생 — `build_단가맵()`에서 float 변환 추가해 해결(app.py가 SQLite 조회 후 이미 하던 처리와 통일)
  - `docs/API규격서.md` 갱신 — 신규 엔드포인트 2개, 자재형태 필드 설명과 "실시간 수신 경로에 자재명 필드가 추가되면 좋겠다"는 요청 메모, 경로 파라미터 영문 표기 관례 반영
  - 검증: `py_compile` 전체 통과, `preprocess.py` 재실행 후 자재형태 분포 확인, Streamlit `AppTest`로 탭4 포함 전체 무예외 실행 확인, 임시 테스트 계정으로 실제 각대대봉투 보유 의뢰서(91997, BC카드)의 계산값 상세 대조(각대대봉투 56개 반영 확인) 및 기존 발행 건(D-202607-00001) Excel 다운로드·openpyxl 재오픈 확인 후 테스트 계정·임시 파일 정리
  - [4-B](Next.js 미발행 목록 화면)부터는 다음 세션에서 상세 설계 예정 — `plan_5단계_Next.js프론트엔드.md` 참고
- 5단계 [4-B] 코드 구현 완료 — 탭4(거래명세서 관리) Next.js "미발행 목록" 화면 ✅ (브라우저 화면 테스트는 사용자 확인 대기)
  - **아키텍처 결정 4가지(사용자 확정):** ① 미발행 판정·집계·예상공급가액 계산은 전부 FastAPI 신규 API가 전담(프론트는 재계산 안 함) ② 필터 드롭다운은 "미발행 건에 실제 등장하는 값"만 노출 ③ 이번 스코프는 미발행 목록 화면 하나만(발행요청목록·발행완료는 [4-C]) ④ 사업부 혼합 검증을 프론트뿐 아니라 FastAPI에도 추가
  - `scripts/billing.py`에 `build_의뢰서_summary()` 신규 — app.py 동명 함수(124~143행)와 동일 집계 로직, 자재 소스만 라인 단위 DataFrame으로 파라미터화(SKILL-12 가드 포함: 자재종류 3컬럼 항상 보장)
  - `scripts/api.py`에 `GET /미발행목록` 신규 — `거래명세서_의뢰서`와 문자열 차집합으로 미발행 판정(MariaDB 업무의뢰서번호는 이미 VARCHAR(20)라 SKILL-02의 float 변환 불필요) 후 `build_의뢰서_summary()`+`calc_공급가맵()`으로 의뢰서 단위 집계+금액계산까지 끝낸 결과를 반환
  - `POST /거래명세서요청`에 사업부 혼합 서버 검증 추가(기존엔 app.py 화면단에만 있었음) — `try/except Exception` 블록 밖에서 먼저 검증해 400이 500으로 감싸이는 함정 회피
  - Next.js 신규: `lib/useInvoiceFilters.ts`(탭4 전용 필터 훅, 필드·필터개수가 달라 `useFilters` 제네릭화 대신 복제 — SKILL-10 전례), `components/InvoiceFilterSidebar.tsx`·`InvoiceSelectionTable.tsx`(프로젝트 최초 체크박스 선택 그리드)·`InvoiceSelectionSummaryBar.tsx`·`Tab4Invoice.tsx`(오케스트레이터), `app/api/거래명세서요청/route.ts`(프로젝트 최초 데이터 쓰기 프록시, `app/api/login/route.ts`와 동일 패턴)
  - `Dashboard.tsx`에 4번째 탭("거래명세서 관리") 등록, `app/page.tsx`에 `/미발행목록` 병렬 fetch 추가
  - `docs/API규격서.md` 갱신 — `GET /미발행목록`(4-3d) 섹션 신규, `POST /거래명세서요청` 400(사업부 혼합) 오류 케이스 반영
  - 검증: `python -m py_compile scripts/billing.py scripts/api.py`, `npx tsc --noEmit`·`npx eslint .` frontend 전체 통과 확인
  - **실제 버그 발견·수정:** 사용자가 화면에서 "거래명세서 요청" 클릭 시 "서버에 연결할 수 없습니다" 오류 제보 → 원인은 `app/api/거래명세서요청/route.ts`(한글 폴더명)를 Next.js(Turbopack 16.2.10) App Router가 라우트로 아예 인식하지 못해 항상 404가 나던 것(재시작·URL percent-encoding 모두 무관, `.next` 빌드 산출물에도 라우트 자체가 안 생성됨 — SKILL-13은 FastAPI 전용 규칙이라 Next.js에는 적용 안 됨을 이번에 확인). `app/api/invoice-request/route.ts`(영문)로 폴더명만 변경, 내부에서 호출하는 FastAPI 경로(`/거래명세서요청`)는 한글 그대로 유지해 해결. SKILL-15 신규 추가
  - 로컬 MariaDB에 직접 연결해 `/미발행목록` 로직 실행 결과 검증: 4,609건 반환(전체 의뢰서 4,623건 − 이미 발행요청 14건 = 4,609건, 계산 정확히 일치), 사업부 혼합 검증 SQL도 별도로 실행해 정상 동작 확인
  - 신규 라우트 수정 후 재검증: `npx tsc --noEmit`·`npx eslint .` 재통과, `curl`로 `/api/invoice-request` 직접 호출해 401(로그인 필요) 정상 응답 확인(404 아님)
  - **실제 버그 2건 발견·수정:** 사용자가 "단가 미등록" 경고 후 체크박스를 해제하려는데 반응이 느리다고 제보 → 원인은 체크박스 하나만 토글해도 `InvoiceSelectionTable`이 필터 안 걸린 미발행 건 전체(약 4,600여 행)를 매번 다시 그리던 것. 각 행을 `React.memo`로 감싼 `Row` 서브컴포넌트로 분리하고 `selected`(Set 전체) 대신 `checked`(boolean) 하나만 prop으로 넘기도록 변경, `Tab4Invoice.tsx`의 `toggleRow`·`toggleAll`도 `useCallback`으로 참조 고정 — 토글한 행 1개만 다시 그려지도록 개선
  - **필터 확장 (사용자 요청):** `useInvoiceFilters.ts`를 담당자·거래처·업무명 3단에서 탭1~3과 동일한 5단(사업부→조회기간→담당자→거래처→업무명) 캐스케이딩으로 확장 — 백엔드 변경 없이 기존 `미발행행` 필드(사업부·작업일자)만으로 구현. 조회기간 기본값은 탭1~3의 "최근 1개월"이 아니라 **전체 기간**(제한 없음)으로 결정(사용자 확인) — 이 화면은 밀린 미발행 건을 놓치지 않는 게 목적이라 기본값이 좁혀져 있으면 오래된 건을 못 보고 지나칠 위험이 있기 때문. `InvoiceFilterSidebar.tsx`에 사업부(고정 2개 옵션)·조회기간(날짜 입력 2개) UI 추가
  - **조회 건수 표시 (사용자 요청):** 표 위에 "조회 결과 N건 (전체 미발행 M건)" 텍스트 추가(Tab2Clients와 동일한 스타일)
  - **표 너비 자동화 (사용자 요청):** 16개 컬럼을 `w-full`로 화면 너비에 억지로 맞추다 보니 컬럼이 눌려 보이던 문제 → `w-full` 제거하고 `whitespace-nowrap`(table에 걸면 하위 셀에 상속됨)만 적용해 내용 길이에 맞게 컬럼 너비가 자동으로 정해지도록 변경, 넘치는 너비는 기존 `overflow-x-auto` 래퍼로 가로 스크롤 처리
  - **선택된 업무의뢰서 세부내역 추가 (사용자 요청, app.py t4a 904~950행 이식):** `components/InvoiceDetailTable.tsx` 신규 — 선택한 의뢰서의 원본 작업내역 줄(거래처명·작업일자·업무명·업무명상세·작업내역서상세·P수·장수·건수·출력페이지·청구페이지)을 표시, 의뢰서 2개 이상 선택 시 의뢰서별로 "▶ 소계" 행 삽입(1개만 선택 시 그룹이 하나뿐이라 생략 — app.py와 동일 기준). 이 목적을 위해 `운영통계행` 타입(`Dashboard.tsx`)에 `업무의뢰서번호`·`업무명상세`·`작업내역서상세`·`P수` 4개 필드 추가, `app/page.tsx` 매핑 확장(백엔드 API는 이미 `/summary`가 전체 컬럼을 반환하고 있어 변경 없음, `python -c`로 실제 응답 필드명 직접 확인). `Dashboard.tsx`가 탭1~3이 쓰던 `rows`(전체 운영통계행)를 탭4에도 `detailRows`로 함께 전달
    - **실제 버그 발견·수정:** 사용자가 소계 행 필드가 한 칸씩 왼쪽으로 밀려 보인다고 제보 → 11개 컬럼 중 "업무명상세·작업내역서상세·P수" 3칸을 건너뛰어야 하는 `colSpan`을 2로 잘못 써서 발생. `colSpan={3}`으로 수정
  - **선택 합계 표 형태로 변경 (사용자 요청):** `InvoiceSelectionSummaryBar.tsx`를 파이프(`|`)로 한 줄에 나열하던 방식(가독성 낮다는 피드백)에서 `InvoiceDetailTable`과 동일한 스타일의 표(헤더 행+값 1행)로 변경
  - **[4-B] 최종 확인 완료:** 로그인 → 탭4 진입 → 필터(사업부·조회기간·담당자·거래처·업무명) → 체크박스 선택 → 선택 합계·세부내역 확인 → 거래명세서 요청까지 전체 흐름을 사용자가 브라우저에서 직접 확인 완료
- 5단계 [4-C] 완료 — 탭4 "발행요청목록"·"발행완료" 화면 + `POST /거래명세서부분취소` 신규 API ✅ (브라우저 화면 테스트까지 사용자 확인 완료)
  - **핵심 설계 결정(사용자 확정):** ① 레벨1(요약) 그룹 기준은 `(거래명세서번호, 업무명)` — 같은 업무명끼리 합산 1행(SQLite 시절 `_이력_id` 개념은 MariaDB의 `거래명세서번호` PK로 대체돼 더 이상 불필요). 단 "발행"/"되돌리기"는 `발송여부`가 번호 전체 단일 컬럼이라 항상 번호 전체에 적용됨(부분취소만 의뢰서 단위로 정확) — "함께 처리됩니다" 안내는 유지 ② 레벨1/레벨2 모두 청구페이지·장수·자재수량·예상공급가액까지 표시(`/미발행목록`과 동일 정보량) ③ 필터는 미발행목록과 동일한 5단(사업부→조회기간→담당자→거래처→업무명)
  - **상태 소유권 리팩터링:** `Tab4Invoice.tsx`가 `useState(initialRows)`로 비제어 상태를 스스로 들던 걸 controlled로 전환(`rows`/`setRows`/`onIssued` props) — 안 그러면 "미발행 목록에서 요청 성공 → 발행요청목록에 즉시 안 보임" 같은 화면 간 불일치가 생김. 신규 `components/Tab4.tsx`가 `invoice`(미발행행[])·`issued`(발행행[]) 두 state를 소유하고 자식 3개(미발행목록/발행요청목록/발행완료)에 controlled로 내려줌
  - `scripts/api.py`에 `GET /발행목록` 신규 — `/미발행목록`과 대칭 구조(판정 방향만 반대), `build_의뢰서_summary()`+`calc_공급가맵()` 재사용, 거래명세서번호·발송여부 필드 추가. 발송여부 쿼리파라미터 없이 대기+완료 전체 반환(두 서브탭이 상시 마운트돼 있어 한 번에 받는 게 단순함) — 예상공급가액은 저장된 헤더값이 아니라 항상 재계산(그룹 단위 표시가 필요해서 쪼갤 수 없고, `/거래명세서엑셀/{no}`와 통일성 유지)
  - `scripts/api.py`에 `POST /거래명세서부분취소` 신규 — 남을 의뢰서가 0건이면 `거래명세서` DELETE(FK `ON DELETE CASCADE`로 `거래명세서_의뢰서` 자동 정리 → 미발행 목록 자연 복귀), 1건 이상 남으면 취소분만 삭제 후 품목·담당자·공급가액·세액·합계 재계산 UPDATE. 이미 발행완료(발송여부=1)면 취소 차단(서버 방어), 검증은 `/거래명세서요청`과 동일하게 `try/except` 밖에서 먼저 수행(HTTPException이 500으로 감싸이는 함정 회피)
  - Next.js 신규: `lib/useIssuedFilters.ts`(useInvoiceFilters 복제 — 발행행에 `useInvoiceFilters(rows: 미발행행[])`를 그대로 쓰면 반환 타입이 미발행행으로 좁혀져 거래명세서번호·발송여부 접근이 TS 에러가 남, SKILL-10 전례), `lib/issuedGrouping.ts`(레벨1 그룹핑 순수 함수), `components/InvoiceIssuedLevel1Table.tsx`·`InvoiceIssuedLevel2Table.tsx`(`InvoiceSelectionTable.tsx`의 `React.memo` Row 성능 패턴 재사용), `components/ConfirmDialog.tsx`(프로젝트 첫 모달 — Streamlit `@st.dialog`의 불릿 리스트+"복구 불가" 경고에 대응, `window.confirm()`으로는 부족해서 신규 제작), `components/Tab4IssuedList.tsx`(Streamlit `_render_발행_섹션(발송여부_target,key_prefix,action_mode)`에 대응하는 `mode:"대기"|"완료"` 파라미터화 컴포넌트, 레벨1→레벨2→레벨3 드릴다운), `components/Tab4.tsx`(서브탭 오케스트레이터)
  - 레벨3(원본 작업내역 상세)은 [4-B]에서 만든 `InvoiceDetailTable.tsx`를 신규 컴포넌트 없이 그대로 재사용, 선택 합계도 `InvoiceSelectionSummaryBar.tsx` 재사용 가능 확인(둘 다 거래명세서번호·발송여부를 안 읽어 구조적으로 호환)
  - `app/api/invoice-publish`·`invoice-unpublish`·`invoice-cancel` 3개 Route Handler 신규(전부 영문 폴더명 — SKILL-15 준수), `app/page.tsx`에 `/발행목록` 3번째 병렬 fetch 추가
  - `docs/API규격서.md` 갱신 — `GET /발행목록`(4-3e), `POST /거래명세서부분취소` 섹션 신규
  - 검증: `python -m py_compile scripts/api.py scripts/billing.py`, `npx tsc --noEmit`·`npx eslint .` frontend 전체 통과(경고 0건까지 정리 — `Tab4IssuedList.tsx`의 구조분해 미사용 변수 경고를 명시적 객체 생성으로 수정)
  - **실제 이슈 발견·수정:** `GET /발행목록`·`POST /거래명세서부분취소`를 실행 중이던 기존 FastAPI 서버(`python scripts/api.py`, `--reload` 미사용)가 코드 추가를 인식 못 해 404를 반환 → 서버 재시작으로 해결(사용자 확인 후 진행). 이 과정에서 `curl`이 Windows Git Bash 환경에서 한글 URL을 깨뜨려 정상 라우트도 404로 오인했던 것도 함께 확인(`python requests`로 재확인해 실제로는 정상 401임을 검증) — 다음에 유사 증상 재현 시 curl보다 `requests`를 우선 사용할 것
  - 로컬 MariaDB에 직접 연결해 전체 흐름 실측 검증: 미발행 의뢰서 1건으로 `거래명세서요청`→`발행목록`에 반영 확인→`거래명세서부분취소`(전체취소, action=delete)→`미발행목록`으로 정확히 복귀→`거래명세서`·`거래명세서_의뢰서` 완전 삭제(CASCADE) 확인까지 원상복구 상태로 종료
  - **실제 버그 발견·수정:** 사용자가 발행요청목록에서 취소한 항목이 미발행 목록의 정렬된 위치가 아니라 맨 마지막 행에 붙는다고 제보 → `Tab4.tsx`의 `handleIssued`/`handleReturnToUnissued`가 옮겨온 항목을 단순 append만 하고 재정렬을 안 해서, FastAPI(`/미발행목록`·`/발행목록`)가 내려준 작업일자 내림차순이 깨지던 것. 두 콜백 모두 병합 후 작업일자 내림차순으로 재정렬하도록 수정
  - **조회기간 시작일 기본값 개선 (사용자 요청):** 미발행목록/발행요청목록/발행완료 세 화면 모두 조회기간 시작일을 빈 칸(전체 기간) 대신 **그 목록에 있는 가장 이른 작업일자**로 채워두도록 변경 — 빈 칸이면 필터가 적용된 건지 헷갈리고, 그렇다고 탭1~3처럼 최근 1개월로 좁히면 오래된 건을 놓칠 수 있어 절충. `useInvoiceFilters.ts`·`useIssuedFilters.ts`에 `최소작업일자()` 헬퍼 추가(`useState` 초기값 및 "필터 초기화" 리셋값으로 사용, 탭1~3의 `defaultRange()`와 동일한 패턴). 종료일은 계속 공란 유지
  - **제목 영역 sticky 고정 + 선택 합계 위치 변경 (사용자 요청, 향후 화면에도 표준 패턴으로 적용 예정):** `Tab4Invoice.tsx`·`Tab4IssuedList.tsx`의 제목·부제·배너·액션 버튼 묶음을 `sticky top-0 z-10 bg-background` 블록으로 분리해 아래 표를 스크롤해도 화면 위쪽에 계속 보이도록 변경(`bg-background`는 Tailwind v4 `@theme inline`으로 등록된 `--color-background` 토큰이라 라이트/다크 모드 실제 배경색과 항상 정확히 일치). 미발행 목록의 "선택 합계"(`InvoiceSelectionSummaryBar.tsx`)는 표 아래 별도 블록 대신 제목 우측 빈 공간에 인라인으로 배치, 중복되던 "선택 합계" 라벨은 "선택 N건" 캡션으로 축약. SKILL-16으로 패턴 기록
  - **표 헤더(thead)까지 이어서 고정 시도 (사용자 추가 요청) — ⚠️ 미해결 상태로 세션 종료:** 제목 블록만 고정하고 표 컬럼 헤더는 제외했던 걸, `frontend/lib/useElementHeight.ts`(신규, `ResizeObserver` 기반 범용 훅)로 제목 블록의 실제 렌더링 높이를 측정해서 `InvoiceSelectionTable`·`InvoiceIssuedLevel1Table`·`InvoiceIssuedLevel2Table`·`InvoiceDetailTable` 4개 표 컴포넌트에 `stickyTop` prop으로 전달, 각 `<thead>`를 `sticky` + `style={{top: stickyTop}}`로 이어붙이도록 시도. 이어서 표를 감싸는 `overflow-x-auto` wrapper div에 `overflow-y-clip`도 추가(CSS 스펙상 세로축도 자체 스크롤 컨테이너가 되어버려 위치가 이중으로 밀린다는 이론으로 접근, `.next` 빌드 산출물에서 `overflow-y:clip` 규칙이 실제로 컴파일된 것까지 확인).
    - **사용자가 두 번째 스크린샷으로 재확인: 두 시도 모두 효과 없음, 증상 동일하게 재현됨(제목 밑이 아니라 화면 중간에 헤더가 뜸).** "nested overflow-x-auto가 원인"이라는 이론은 틀렸거나 불충분한 것으로 판명 — 코드 추론만으로는 한계에 도달, 실제 브라우저 개발자도구로 직접 확인이 필요한 단계. 상세 증상·시도 내역·다음 확인 사항은 `.claude/plans/bug_sticky_thead_위치오류.md`에 별도 기록(다음 세션에서 이어서 진행 예정, [4-D] 착수 전 우선 해결하기로 합의). 코드는 롤백하지 않고 그대로 둠(제목 블록 자체 sticky는 정상 동작)
- 표 헤더(thead) sticky 위치 버그 최종 해결 ✅ ([4-D] 착수 전 최우선 처리)
  - 코드 추론만으로 2회 실패했던 전례에 따라, 이번엔 `frontend`에 Playwright(개발용 devDependency, 작업 완료 후 제거)를 설치해 실제 Chromium으로 화면을 렌더링하고 `getBoundingClientRect()`를 스크롤 전/후 비교하는 방식으로 진단
  - **근본 원인 확정:** CSS `position: sticky`는 "가장 가까운 overflow-非visible 조상"을 기준으로 계산되며 그 조상이 실제로 스크롤되는지와 무관하다는 스펙 동작 때문에, 표를 감싼 `overflow-x-auto` wrapper가 thead의 sticky 기준이 되어버렸음 — wrapper 자신은 내부 스크롤이 없어 thead가 wrapper 상단에서 고정 오프셋(88px)만큼 떨어진 채 창 스크롤을 그대로 따라 흘러갔던 것(스크롤 400px 전/후 모두 오프셋이 정확히 88px로 유지됨을 실측 확인). 부가로 `ResizeObserver`의 기본 `contentRect`가 padding·border를 제외해 그 88px 자체도 제목 블록 실제 높이(121px)보다 33px 부족했던 것도 함께 발견
  - **해결 방식(사용자 확인 후 채택, "표 전용 박스 스크롤"):** JS로 스크롤 위치를 직접 계산해 페이지 전체 스크롤 느낌을 유지하는 대안 대신, 표 wrapper를 `max-h-[60vh] overflow-auto`(가로+세로 모두)로 바꿔 표 자신을 독립 스크롤 박스로 만들고 thead를 그 박스 기준 `top:0`으로 붙이는 방식 채택 — CSS만으로 안정적으로 해결됨. 표 영역이 페이지와 하나로 이어진 스크롤이 아니라 표마다 자체 스크롤바를 갖게 되는 트레이드오프는 사용자 확인 후 수용
  - `InvoiceSelectionTable.tsx`·`InvoiceIssuedLevel1Table.tsx`·`InvoiceIssuedLevel2Table.tsx`·`InvoiceDetailTable.tsx` 4개 표 컴포넌트 수정(`stickyTop` prop 제거, wrapper·thead 클래스 교체), `Tab4Invoice.tsx`·`Tab4IssuedList.tsx`에서 `titleRef`/`titleHeight`/`useElementHeight` 배선 전부 제거, 더 이상 쓰이지 않는 `frontend/lib/useElementHeight.ts` 삭제
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과, Playwright로 미발행목록(표 내부 스크롤 300px 시 헤더 고정 유지 확인)·발행요청목록·발행완료 3개 화면 스크린샷 재확인. 디버깅용 Playwright devDependency·임시 로그인 계정(`_debug_sticky`)·임시 스크립트 전부 정리(사용자 확인 후 제거 — package.json 변경 없음으로 원상복구)
  - `SKILL.md` SKILL-16 갱신(표 헤더 확장 섹션을 "미해결"에서 "해결"로 교체, 최종 코드 패턴·진단 방법 기록), `.claude/plans/bug_sticky_thead_위치오류.md` 해결 완료로 갱신, `plan_5단계_Next.js프론트엔드.md` 상태 갱신
- 미발행 목록 "선택 합계" 표 위치 최종 확정 ✅ ("거래명세서 요청" 버튼 바로 아래에 세로로 쌓기)
  - 최초엔 제목 오른쪽 끝에 우측정렬로 배치돼 있었는데, 제목 영역 폭이 매우 넓다 보니(사이드바 제외 전체 폭) 합계표가 화면 오른쪽 먼 곳에 위치해 사용자가 "합계표가 아예 생성이 안 된 줄" 알았을 정도로 눈에 안 띄는 문제가 있었음(진짜 원인은 이번에 재확인 후 확정)
  - 1차 시도(버튼 옆 가로 배치)는 화면 폭이 부족하면 줄바꿈되면서 제목-버튼 사이에 불필요한 빈 공간이 생기는 부작용이 있어 되돌렸었음(같은 날 시행착오)
  - **최종 해결**: 버튼과 합계표를 `<div className="space-y-2">`로 묶어 버튼 바로 아래에 세로로 쌓음 — 화면 폭과 무관하게 항상 버튼 근처(사용자가 이미 보고 있는 위치)에 고정되어 두 문제(눈에 안 띔 / 줄바꿈으로 인한 빈 공간) 모두 해소. `Tab4Invoice.tsx` 제목 블록 구조 변경, `InvoiceSelectionSummaryBar.tsx` 캡션 정렬을 우측→좌측으로 변경
  - 계획 수립 시 Explore·Plan 서브에이전트로 대안(CSS Grid 2단 배치, absolute 오버레이) 검토 후 세로 쌓기로 최종 확정(사용자 선택) — 검증: `npx tsc --noEmit`·`npx eslint .` 통과
- 5단계 [4-D] 완료 — Next.js "거래처 마스터" 화면 신규(5번째 최상위 탭) ✅
  - **설계 결정(사용자 확정, Explore·Plan 서브에이전트로 조사 후 계획):** 기존 Streamlit(`st.data_editor` 인라인 편집, 저장 시 전체 교체)·기존 `POST /거래처마스터`(전체 교체) 방식 대신 **"목록 + 추가/수정 팝업창"** 구조로 신규 제작. 거래처명은 `단가마스터`·`거래명세서`·`운영통계자료`가 전부 FK 없이 문자열로만 참조하고 있어(프로젝트 전체에 FK 자체가 `거래명세서_의뢰서→거래명세서` CASCADE 하나뿐), **생성 후 절대 수정 불가로 고정**(API 요청 바디에 필드 자체를 안 둬서 API 레벨에서부터 강제) — 다음 단계 [4-E] 단가 관리 화면이 여기서 등록된 거래처명을 드롭다운으로 불러와 쓰게 될 예정이라, 등록 시점의 정확성이 중요함
  - `scripts/api.py`: `POST /거래처마스터`를 전체교체 → 단건생성으로 교체(`POST /단가마스터`와 동일 패턴, 중복은 `IntegrityError` catch로 409), `PUT /거래처마스터/{name}` 신규(사업자등록번호·수신이메일·비고만 수정, `PUT /단가마스터/{id}`와 동일 구조). `GET`·`DELETE`는 그대로 유지. 실제 MariaDB로 생성→중복409→수정→404(존재안함)→삭제까지 실측 검증(임시 계정·임시 거래처로 진행 후 원상복구)
  - `docs/API규격서.md` §4-4 갱신 — POST/PUT 설명 및 오류 응답 표(400/409/404) 추가
  - `frontend/components/ConfirmDialog.tsx`에 `dangerText?: string` prop 추가(기본값은 기존 3곳과 동일 유지, 하위 호환) — 기존 "취소 후 복구할 수 없습니다." 문구가 삭제 문맥엔 어색해서 거래처 삭제 시엔 "삭제 후 복구할 수 없습니다."로 오버라이드
  - Next.js 신규: `components/ClientMaster.tsx`(오케스트레이터, 로컬 state만 소유 — 다른 탭과 공유 없음), `components/ClientMasterTable.tsx`(`InvoiceSelectionTable.tsx`의 React.memo 행 패턴 + SKILL-16 표 전용 박스 스크롤 재사용), `components/ClientFormDialog.tsx`(**프로젝트 첫 입력 폼 모달** — `ConfirmDialog.tsx`의 모달 뼈대에 실제 입력 필드 추가, 거래처명은 수정 모드에서 읽기전용 표시), `app/api/client-create`·`client-update`·`client-delete` Route Handler 3개(SKILL-15 영문 폴더명). `app/page.tsx`에 `/거래처마스터` 병렬 fetch 추가, `Dashboard.tsx`에 5번째 탭 등록
  - **실제 버그 발견·수정:** `ClientFormDialog.tsx`가 다이얼로그 재사용 시 `initial` prop 변경을 `useEffect` 안에서 `setState`로 재동기화하려다 Next.js 16 `react-hooks/set-state-in-effect` 규칙 위반(2026-07-19 앞선 세션에서 이미 겪은 것과 동일 패턴) → `ClientMaster.tsx`가 다이얼로그를 열 때마다 증가시키는 `formKey`를 `<ClientFormDialog key={formKey}>`로 넘겨 리마운트시키는 "key 기반 리셋" 방식으로 교체(useEffect 불필요, React 공식 권장 패턴)
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과. Playwright를 임시 재설치해 실제 로그인 후 "거래처 마스터" 탭 진입 → 신규 추가 → 중복 이름 시도(409 오류 배너 확인) → 수정(거래처명 읽기전용 확인, 비고 변경) → 선택 삭제(확인 다이얼로그 → 실제 DB 반영까지 확인)까지 스크린샷과 DB 직접 조회로 종단 검증 후 Playwright·임시 계정·임시 스크립트 전부 정리(`package.json` diff 없음으로 원상복구 확인)
  - `plan_5단계_Next.js프론트엔드.md`의 [4-D] 상태를 "다음 세션에서 상세 설계" → 완료로 갱신, 다음은 [4-E] 단가 관리 화면
- 5단계 [4-E] 완료 — Next.js "단가관리" 화면 신규(4단계 전체 완료) ✅
  - **메뉴 구조 결정 변경(사용자 확정):** [4-A] 설계 당시엔 "단가는 추후 권한 관리 대상"이라는 이유로 거래처 마스터·단가 관리를 각각 독립 최상위 탭으로 분리하기로 했었으나, 이번에 "거래처 마스터" 탭 하위에 "거래처관리"·"단가관리" 2개 메뉴로 묶는 구조로 재확정(권한 분리가 필요해지면 하위 메뉴 단위로도 숨김 가능하므로 최상위 분리가 필수는 아니라고 판단). `Dashboard.tsx`의 최상위 `TABS`는 5개 그대로 유지, 신규 `components/ClientMasterSection.tsx`가 `Tab4.tsx`와 동일한 상시마운트+hidden 하위 메뉴 허브 역할(`ClientMaster.tsx`↔`PricingMaster.tsx` 전환)
  - **Streamlit 원본 동작 확인 후 이식:** 거래처 1곳을 먼저 선택 → 그 거래처 단가만 표시/추가(전체를 한 표에 보여주면 항목 과다), 업무명→작업명 후보는 `단가마스터`가 아니라 **운영통계자료(실제 발생한 작업 데이터)**에서 추출(`운영통계행` 타입에 `작업명` 필드 신규 추가 — 백엔드는 이미 반환 중이었으나 프론트 타입에만 없었음), 8개 단가 필드(출력·봉입·추가봉입·용지제작·봉투제작·삽지제작·각대대봉투·각대대봉투봉입단가, 마지막 필드는 화면에 "수작업 단가"로 표시) + 비고
  - **사용자 확정으로 Streamlit보다 완화된 부분:** 업무명·작업명을 기존 값 선택뿐 아니라 **자유 입력으로 새로 등록**할 수 있게 허용(아직 실적 없는 신규 업무 유형에도 미리 가격 매김 가능) — HTML `<input list>`+`<datalist>` 조합으로 구현(별도 콤보박스 컴포넌트 불필요)
  - `scripts/api.py` 보완 2가지: ① `POST /단가마스터` 응답에 신규 `id` 반환 추가(`cur.lastrowid`) — 프론트가 서버 재조회 없이 로컬 목록에 바로 추가하기 위해 필요. ② **실제 버그 발견·수정:** 업무명·작업명이 둘 다 NULL(거래처 전체 기본단가)인 경우만 중복 방지를 넣으려다, Playwright 실측 테스트 중 업무명은 있고 작업명만 NULL인 "업무명 단위 기본단가"도 같은 문제(NULL끼리는 DB UNIQUE 제약이 다른 값으로 취급)를 겪는 것을 발견 — 작업명이 NULL인 모든 경우로 체크를 일반화(`업무명 <=> %s` NULL-세이프 비교)해 재검증 후 해결
  - `docs/API규격서.md` §4-4 표·기본단가 중복 방지 섹션 갱신(기존 "알려진 한계" 서술을 "해결됨"으로 교체)
  - Next.js 신규: `components/ClientMasterSection.tsx`(하위 메뉴 허브), `components/PricingMaster.tsx`(오케스트레이터, `ClientMaster.tsx`와 동일 골격이나 거래처 선택 드롭다운 추가·선택은 id 기준), `components/PricingMasterTable.tsx`(12컬럼 표, SKILL-16 패턴), `components/PricingFormDialog.tsx`(업무명·작업명 datalist 입력, 수정 모드에선 읽기전용), `app/api/pricing-create`·`pricing-update`·`pricing-delete` Route Handler 3개(SKILL-15). `app/page.tsx`에 `/단가마스터` 병렬 fetch 추가, `Dashboard.tsx`에 `단가행` 타입·`pricingRows` prop 추가. 기존 `ClientMaster.tsx` 제목을 "거래처 마스터 [거래처관리]"로 1줄 수정(하위 메뉴 맥락 표시, `Tab4Invoice.tsx`의 "상위 탭 [하위 메뉴]" 제목 관례와 통일)
  - 검증: `python -m py_compile scripts/api.py`, 로컬 MariaDB 실측(기본단가 생성→중복409 확인→일반화 전 버그 재현 및 수정 확인→수정→404→삭제, 테스트 데이터 원상복구), `npx tsc --noEmit`·`npx eslint .` 통과, Playwright 임시 재설치로 브라우저 종단 검증(거래처관리→단가관리 하위 메뉴 전환→단가 추가→중복 오류 배너→수정(업무명 읽기전용 확인)→삭제→거래처관리 회귀 확인까지 스크린샷 확인) 후 Playwright·임시 계정·임시 스크립트 전부 정리
  - **[4단계] 전체 완료** — 다음은 [4-F] 통합 검증(Streamlit과의 병행 운영 방식 결정 등)
- 단가관리 업무명·작업명 입력 UX 개선 ✅ (사용자 피드백: 값을 선택한 뒤 다시 클릭해도 전체 후보가 다시 안 뜨고 지워야만 목록이 보였음)
  - 원인: `<input list>`+`<datalist>`(브라우저 기본 자동완성)는 이미 값이 채워진 입력을 다시 클릭해도 전체 후보를 보장해서 보여주지 않는 한계가 있음
  - `frontend/components/EditableCombo.tsx` 신규 — 단일 선택+자유 입력 콤보박스(`MultiSelectCombo.tsx`의 열림/바깥클릭 패턴 재사용). 클릭·포커스 시 기존 값과 무관하게 항상 전체 후보를 보여주고(값은 전체 선택돼 바로 새로 타이핑 가능, SKILL-08 관례), 타이핑을 시작해야 그때부터 필터링됨. `PricingFormDialog.tsx`의 업무명·작업명 필드(생성 모드)를 이 컴포넌트로 교체
  - **실제 버그 2건 발견·수정(Playwright 실측):** ① 옵션 클릭 시 입력 blur를 막으려고 `onMouseDown`에 `preventDefault`를 걸었더니, 이미 포커스된 입력을 다시 클릭해도 브라우저가 `focus` 이벤트를 새로 안 쏴서 목록이 안 열림 → `onClick`에도 동일한 "전체 목록 열기" 로직을 추가해 해결. ② `<label>` 태그 안에 드롭다운 `<ul>`까지 함께 들어있어서, 목록이 열리면 label의 접근성 텍스트가 "업무명"+후보 항목 전체로 합쳐져 버림(Playwright `getByLabel(exact:true)`가 타임아웃 나면서 발견) → 드롭다운이 있는 필드는 `<label>` 감싸기 대신 `<div>`+`<span>` 조합과 `aria-label`로 구조 변경(스크린리더 접근성도 함께 개선됨)
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과, Playwright로 "첫 클릭→전체 9개 후보 표시→선택→다시 클릭(지우지 않음)→전체 9개 후보 다시 표시→다른 항목 클릭→즉시 교체" 전 과정 실측 확인 후 Playwright·임시 계정·임시 스크립트 정리
  - **후속 요청(사용자):** 단가 8개 필드·비고 입력창도 마우스 클릭 시 Tab 이동과 동일하게 값 전체가 선택되도록 개선 — `onFocus`+`onClick` 둘 다에 `.select()` 적용(EditableCombo에서 실측으로 확인한 것과 동일한 이유: onFocus만으론 이미 포커스된 입력을 재클릭할 때 재적용이 안 됨). 동일하게 검증된 패턴이라 Playwright 재설치 없이 `npx tsc --noEmit`·`npx eslint .` 통과로 확인
- `자재사용현황` 봉투/용지 수량 부풀림 버그 수정 ✅ (사용자 제보: 의뢰서번호 97232의 봉투/용지 수량이 DB 실제값과 웹 화면 표시값이 다름)
  - **원인 확정:** MariaDB `자재사용현황` 테이블에 애초에 "작업명" 컬럼이 없어서, `scripts/api.py`의 `_자재map_조회()`가 운영통계자료와 (업무의뢰서번호,작업내역서번호)로만 JOIN해 작업명을 추정하고 있었음 — 작업내역서번호 하나에 작업명이 여러 개 걸린 경우(실사용례 확인, 97232의 작업내역서 90916에 작업명 3개) 자재 수량이 그 개수만큼 그대로 복제됨(105→315, 119→357, 정확히 ×3). 이 값은 미발행목록 화면 표시뿐 아니라 `billing.py`의 실제 청구금액 계산(`calc_공급가맵`)·거래명세서 Excel 생성에도 그대로 쓰여 **표시 문제가 아니라 청구 금액이 부풀려질 수 있는 버그**였음
  - **Streamlit이 문제없었던 이유:** `app.py`의 `load_자재_summary()`는 원본 엑셀(`data/자재사용현황.xlsx`)을 직접 읽는데, 이 엑셀은 자재 사용량 각 줄에 "작업이름" 컬럼이 이미 있어 추측이 필요 없었음 — MariaDB로 옮기는 과정에서 이 컬럼이 누락된 DB 설계 당시의 문제였음
  - **영향 범위 확인:** 작업내역서 2,536건이 작업명 2개 이상, 그중 자재 기록이 있어 실제로 수량이 부풀려지는 건 1,023건. 이미 발행완료(발송여부=1)된 거래명세서 중 영향받은 건은 **0건**(아직 실제 청구된 금액 피해는 없었음)
  - **해결:** `자재사용현황`에 `작업명 VARCHAR(200) NULL` 컬럼 추가(`scripts/init_db_mariadb.py`, 자재형태 추가 때와 동일한 마이그레이션 패턴 — 컬럼 존재 확인→인덱스 제거→컬럼 추가→UNIQUE KEY에 작업명 포함해 재생성), 배치 경로(`scripts/data_transform.py`의 `merge_자재()`·`scripts/preprocess.py`의 `load_자재_raw()`)가 원본 엑셀의 작업이름을 그대로 저장하도록 수정, `_자재map_조회()`를 저장된 작업명을 우선 쓰고 작업명이 없는 행(실시간 수신 건)만 기존 추정 JOIN으로 보완하는 UNION ALL 구조로 재작성. `billing.py`는 변경 없음(정확한 값이 들어오면 자동으로 정확해지는 구조)
  - `python scripts/preprocess.py` 재실행으로 배치 데이터 전체 백필 — 자재사용현황 6,316행→**9,989행**(작업명 단위로 세분화되며 증가). 재검증 결과 97232가 정확히 105/119로 나옴, 영향받았던 다른 의뢰서(91975·91984·91997) 샘플도 확인
  - 실시간 수신 경로(`POST /운영통계자료수신`)는 여전히 작업명을 안 보내주고 있어(자재명 필드와 동일한 기존 한계) `자재행` 모델에 `작업명: Optional[str] = None` 필드를 미리 열어둠(외부 시스템이 나중에 보내주면 자동 반영). 실제 테스트 요청으로 작업명 없이 보내도 기존과 동일하게(회귀 없이) 저장되는지, 작업명 여러 개 걸린 상황이 재현될 때 fallback JOIN이 정상 동작하는지 확인 후 테스트 데이터 정리
  - `docs/API규격서.md`에 자재명과 동일한 스타일로 작업명 필드 요청 메모 추가, `docs/작업현황-대시보드-생성-KNOWLEDGE.md`의 자재사용현황 테이블 구조·행수 갱신
  - **잔여 한계(고지):** 실시간 API로만 들어온 자재 행은 외부 시스템이 작업명을 보내주지 않는 한 여전히 추정(JOIN) 방식에 의존 — 배치 재적재 이후로는 영향 범위가 "실시간으로만 들어온 건"으로 좁혀짐
  - 검증: `python -m py_compile scripts/api.py scripts/data_transform.py scripts/preprocess.py` 통과, 로컬 MariaDB로 스키마 마이그레이션→배치 백필→FastAPI 재시작→97232 실측→실시간 수신 회귀 테스트까지 전체 흐름 확인
- 5단계 [4-F] 완료 — Next.js 6개 탭 통합 검증 ✅ (**5단계 Next.js 프론트엔드 전체 완료**)
  - 작업 현황 요약·거래처별 현황·담당자별 현황·거래명세서 관리(미발행목록/발행요청목록/발행완료)·거래처 마스터(거래처관리/단가관리) 6개 최상위 탭 전체를 사용자가 직접 클릭하며 종합 확인
  - **Streamlit(`app.py`) 병행 운영 방식 결정(사용자 확정):** 당장 Streamlit을 퇴역시키지 않고 당분간 계속 병행 사용 — 기존에 문서화된 데이터 이원화 주의사항(당사 생산공정관리시스템이 MariaDB에만 실시간 반영, Streamlit은 여전히 `data/운영통계자료.xlsx` 수동 업로드 기반이라 두 화면 숫자가 서서히 달라질 수 있음, `plan_5단계_Next.js프론트엔드.md` 참고)은 그대로 유효
  - `plan_5단계_Next.js프론트엔드.md` 상태를 5단계 전체 완료로 갱신
- 생산공정관리시스템 전달용 API 문서 분리 + 실시간 API의 자재형태(봉투 구분) 수신 지원 추가 ✅
  - `docs/API규격서.md`(내부 전체 문서)에서 생산공정관리시스템이 실제로 호출하는 부분만 추려 `docs/API규격서_생산공정관리시스템전달용.md` 신규 작성(연동 방식 개요·`GET /health`·`POST /운영통계자료수신`·자동 계산 값 4개 섹션만 포함), 웹 아티팩트(HTML)로도 발행해 공유 가능한 링크 제공
  - 문서 작성 중 실제 결함 발견: `POST /운영통계자료수신`의 자재 행 요청 스키마(`scripts/api.py`의 `자재행` 클래스)에 `자재명`·`자재형태` 필드가 아예 없어, 생산공정관리시스템이 그 값을 보내기 시작해도 FastAPI가 조용히 버려 계산에 전혀 반영되지 않는 상태였음(반면 `scripts/data_transform.py`의 `merge_자재()`는 이미 `자재명` 기반 자동 분류 로직을 갖고 있어 배치 경로만 정상 동작 중이었음)
  - 사용자가 향후 지금의 "일반봉투/각대대봉투" 2종 구분을 넘어 **소·중·대봉투**처럼 세분화하고 싶다는 방향을 밝혀, `자재형태`를 특정 값으로 제한하지 않는 자유 문자열로 설계 — `자재행`에 `자재명`·`자재형태` 선택 필드 추가, `merge_자재()`는 ① 요청에 `자재형태`가 이미 있으면 그대로 존중 → ② 없고 `자재명`이 있으면 기존처럼 자동 분류 → ③ 둘 다 없으면 기존과 동일하게 "일반봉투" 기본값의 우선순위로 보완(배치 경로는 `자재형태` 컬럼을 안 주므로 동작 변화 없음)
  - 검증: `python -m py_compile scripts/api.py scripts/data_transform.py` 통과, `merge_자재()`를 4가지 케이스(자재명만/자재형태 직접/둘 다 없음/자재형태+자재명 동시 — 자재형태 우선 확인)로 함수 단위 테스트해 전부 기대값과 일치 확인(DB·서버 재기동 없이 로직만 검증 — 로컬에 실행 중인 FastAPI/MariaDB가 없어 실제 API 호출 종단 테스트는 이번 세션에서 진행하지 못함, 다음 실사용 시점에 확인 필요)
  - **오늘 범위를 의도적으로 제한:** 단가마스터 스키마(`각대대봉투단가` 등 2종 고정)·`calc_공급가맵()`·거래명세서 Excel 생성·Next.js 단가관리 화면은 손대지 않음 — 즉 값은 저장되지만 소/중/대별 실제 단가 차등은 아직 반영 안 됨. 이 사실을 내부 문서·전달용 문서·웹 아티팩트 3곳 모두에 명확히 표기(오해 방지)
  - 향후 단가 반영 작업은 `.claude/plans/plan_봉투크기단가확장.md`(신규)에 Phase 2로 기록 — 실제 소/중/대 값이 DB에 쌓이기 시작하면 그 시점에 착수
- Vercel 배포 시점 결정(로컬에서 더 다듬은 뒤 배포) — `docs/작업현황-대시보드-생성-KNOWLEDGE.md`·`plan_5단계_Next.js프론트엔드.md`에 반영 ✅
- 거래명세서 미리보기 기능 완성 — 미발행 목록 "거래명세서 요청" 클릭 시 즉시 저장 대신 품목·수량·단가·금액·합계를 팝업으로 먼저 보여주고, "확정"을 눌러야 실제 저장 ✅ ([4단계] 종료 후 보류돼 있던 요청, 2026-07-20 재착수)
  - `scripts/billing.py`: `generate_거래명세서_excel()` 안에 뒤섞여 있던 품목 계산 블록(품목순서·정렬행 만들기)을 `build_품목행()`으로 분리 — Excel 생성과 신규 미리보기 API가 공용으로 사용. 순수 리팩터링임을 `git show HEAD`로 뽑은 리팩터링 전 버전과 실제 발행 건(D-202607-00001, 의뢰서 9건)으로 Excel을 각각 생성해 `hashlib` 바이트 비교로 확인(sha256 완전 일치)
  - `scripts/api.py`: 신규 `POST /거래명세서미리보기` — 아직 채번 전인 `의뢰서번호_목록`을 받아 `build_품목행()`으로 품목·합계만 계산해 JSON 반환(DB 쓰기 없음, Excel 생성 없음). `GET /거래명세서엑셀/{no}`와 DB 조회 패턴 동일, 대상만 채번된 번호 대신 화면에서 방금 체크한 목록
  - Next.js 신규: `components/InvoicePreviewDialog.tsx`(`ConfirmDialog.tsx` 모달 뼈대 재사용, 내용은 품목 표), `app/api/invoice-preview/route.ts`(`invoice-request/route.ts`와 동일한 BFF 프록시 패턴, 영문 폴더명 SKILL-15). `components/Tab4Invoice.tsx`의 `handleRequest()`를 `handlePreviewClick()`(검증 후 미리보기 호출·팝업 오픈)과 `handleConfirmSubmit()`(팝업 "확정" 클릭 시 실제 저장, 기존 로직 그대로 이동)으로 분리
  - 검증: `python -m py_compile`, 실제 서버로 `POST /거래명세서미리보기` 호출(BC카드 실제 의뢰서 9건 — 인증 없이 401, JWT 발급 후 200과 정상 품목·합계 확인), `calc_공급가맵()` 의뢰서 단위 합산과 `build_품목행()` 품목 단위 합계가 정확히 일치함을 별도 대조. `npx tsc --noEmit`·`npx eslint .` 통과(경고 0건)
  - Playwright(임시 설치) + 임시 로그인 계정(`_debug_preview`, DB insert 없이 JWT만 발급하는 방식이 아니라 실제 계정 생성 후 로그인 폼까지 통과)으로 브라우저 종단 검증: 로그인 → 탭4 → 거래처 필터(BC카드) → 의뢰서 체크 → "거래명세서 요청" 클릭 → 미리보기 팝업(품목표+합계) 확인 → "취소" → 저장 안 됨(성공 메시지 없음) 확인 → 재요청 → "확정" → 실제 거래명세서번호 발급(D-202607-00008)·미발행 목록 반영까지 확인. 테스트로 생성된 거래명세서·임시 계정은 즉시 삭제, Playwright·임시 스크립트도 제거해 `package.json` diff 없음으로 원상복구
  - `docs/API규격서.md`에 §4-3f로 신규 엔드포인트 문서화(내부 전용 — 생산공정관리시스템 전달용 문서·웹 아티팩트는 대상 아님)
- Next.js "발행완료" 화면에 거래명세서 다운로드 버튼 추가 ✅ (사용자 제보: Streamlit 시절엔 있었는데 Next.js 이전 과정에서 빠짐 — 2026-07-17 CHANGELOG "다운로드 버튼... 발행완료 탭 전용으로 이동" 결정이 [4-C] 마이그레이션에서 누락됐던 것)
  - `frontend/app/api/invoice-excel/[no]/route.ts` 신규 — 프로젝트 첫 "바이너리 파일" 프록시. `GET /거래명세서엑셀/{no}`가 JWT로 보호돼 있어 브라우저가 FastAPI 주소를 직접 열 수 없는 문제를, 다른 프록시와 동일하게 `fastapiFetch()`로 우회하되 `res.json()` 대신 `arrayBuffer()`로 그대로 스트리밍
  - `components/InvoiceIssuedLevel1Table.tsx`에 `showDownload` prop 추가 — true면 마지막 열에 거래명세서번호별 다운로드 링크 표시. 체크박스 선택과 무관하게 행 단위로 바로 받게 해서, 여러 그룹(거래명세서번호+업무명)을 선택했을 때 "어느 파일이 받아지는지" 모호해지는 문제를 애초에 피함(Streamlit의 "선택 후 다운로드 버튼" 방식과 다른 설계)
  - `components/Tab4IssuedList.tsx`에서 `showDownload={mode === "완료"}`로 배선 — 발행요청목록(mode="대기")에는 안 보이고 발행완료에만 표시(Streamlit 배치 그대로 재현)
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과. 임시 로그인 계정으로 Next.js 자체 `/api/login` → `/api/invoice-excel/{no}` 실제 호출해 실제 발행 건(D-202607-00001) xlsx 바이트(68,140bytes, 앞선 리팩터링 회귀 테스트와 동일 크기) 정상 다운로드 확인, 존재하지 않는 번호는 404 확인. 임시 계정은 즉시 삭제
- MariaDB + FastAPI를 로컬 PC → 사무실 PC(원격서버)로 이관 (`plan_1단계_MariaDB전환.md` [6단계]) — 사무실 PC 쪽 실제 조작은 사용자가 원격 데스크톱으로 직접 실행, Claude는 로컬 PC 작업(백업 생성·검증)과 단계별 명령어 안내를 담당
  - 로컬 PC에서 `mysqldump.exe --default-character-set=utf8mb4`로 `dashboard` DB 전체 백업(8개 테이블, 약 13.2MB) 생성 후 8개 테이블 전부 포함 확인. 이관 전 로컬 DB에 테스트 계정·테스트 거래명세서가 전혀 없는 깨끗한 상태임을 미리 확인해 별도 정리 없이 그대로 이관(운영통계자료 36,804행·자재사용현황 9,989행 등 8개 테이블)
  - 사무실 PC에 MariaDB 11.4.x 설치 → `dashboard` DB(utf8mb4) 생성 → 백업 파일 복원 → 8개 테이블 행수가 로컬과 정확히 일치함을 확인
  - `scripts/db_config.py`는 로컬 파일을 그대로 복사해 사용 — `DB_HOST`가 이미 `127.0.0.1`이라 애초에 수정이 필요 없었음(원래 계획엔 "DB_HOST만 변경"이었으나, MariaDB·FastAPI를 같은 사무실 PC에 함께 배치하기로 하면서 아예 무수정으로 단순화됨)
  - **실제 이슈 발견·해결:** 사무실 PC에 이미 떠 있던 다른 서비스("ibx dashboard", `httpd.exe`)가 포트 8000을 선점하고 있어 `API서버_실행.bat`(포트 8000 고정)이 `WinError 10048`로 기동 실패. 정체를 모르는 기존 서비스라 함부로 종료하지 않고, 대신 FastAPI를 **포트 8001**로 우회 실행(`uvicorn scripts.api:app --host 0.0.0.0 --port 8001`)하기로 결정 — 이 사무실 PC 전용 `API서버_실행_사무실PC.bat` 신규 작성
  - 사무실 PC 방화벽에 포트 8001 인바운드 허용 규칙 추가(포트 3306/MariaDB는 열지 않음 — FastAPI와 같은 PC라 로컬 접속만으로 충분, 보안상 이 편이 원래 계획보다 더 안전함)
  - 로컬 PC `frontend/.env.local`의 `FASTAPI_URL`을 `http://192.168.30.201:8001`(사무실 PC LAN IP)로 변경, Next.js 재시작. 로컬 PC에서 사무실 PC API로 직접 LAN 연결(`curl http://192.168.30.201:8001/health`) 성공 확인(`{"status":"ok","db":"connected"}`), 로그인 화면 등 Next.js 페이지도 새 설정으로 정상 응답 확인
  - 로컬 MariaDB·기존 API서버(포트 8000)는 삭제하지 않고 안전장치로 그대로 유지
  - 사용자가 실제 브라우저로 로그인해 최종 확인 완료(2026-07-20) — 작업 현황 요약·거래명세서 관리 화면 데이터가 이관 전(로컬 기준)과 동일하게 표시됨(데이터를 그대로 복사 이관했으므로 "동일하게 보이는 것"이 정확한 성공 기준). **`plan_1단계_MariaDB전환.md` [6단계] 전체 완료**
- 발행완료/발행요청목록 화면 "레벨1 합계 ≠ 레벨2 상세" 불일치 버그 수정 ✅ (사용자 제보: 실제 발행 후 화면에 표시된 봉입건수가 실제와 다름)
  - **실제 버그 확인:** D-202607-00011(의뢰서 14건, 업무명 2종)을 실사용 데이터로 직접 검증 — 화면엔 교육청(전기료) 그룹 "의뢰서건수 2, 봉입건수 2,440"으로 떴지만 실제로는 5건·합계 4,932여야 정상이었음(레벨2를 펼치면 5건이 다 나와 더 헷갈렸음)
  - **원인:** `frontend/components/Tab4IssuedList.tsx`의 `level2Rows`가 필터가 적용된 `filters.base5`가 아니라 필터 미적용 `scoped`에서 계산되고 있었음 — 레벨1 요약(`groups`)은 필터링된 일부만 합산하는데 레벨2 상세는 필터와 무관하게 해당 거래명세서번호+업무명의 전체 의뢰서를 보여줘서, 사용자에게는 "레벨1 합계가 틀렸다"로 보였음. `level2Rows`를 `filters.base5` 기준으로 수정해 레벨1·레벨2가 항상 같은 데이터 범위를 쓰도록 함
  - **중요:** 이 버그는 화면 표시 로직에만 있었고, 실제 저장된 청구 금액(`거래명세서.합계`)은 발행 요청 시점에 선택한 값 그대로 저장되므로 이 버그와 무관 — 이미 발행된 거래명세서의 실제 청구액이 잘못 계산된 것은 아님
  - **부가 원인 발견 및 함께 해결:** `frontend/lib/useIssuedFilters.ts`의 시작일 기본값이 "이 목록의 가장 이른 작업일자"를 `useState` 초기값으로만 한 번 계산해두는 방식이었는데, React의 `useState` 초기값은 최초 마운트 시점에만 반영되고 이후 `rows`가 늘어나도(예: 새로 발행 완료돼 더 이른 날짜의 의뢰서가 추가된 경우) 재계산되지 않아 오래된 의뢰서가 조용히 필터에서 밀려나는 문제가 있었음 — 사용자 요청에 따라 시작일 기본값을 빈 칸(전체 표시)으로 단순화, 사용자가 직접 원하는 기간을 지정하는 방식으로 변경(발행요청목록·발행완료 전용, 미발행 목록은 대상 아님)
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과. 실사용 데이터(D-202607-00011)로 원인을 수치까지 정확히 대조 확인(교육청 그룹 2,440 = 97227+97228의 정확한 합, 종합법인 그룹 95,003 = 97100+97099+97098의 정확한 합 — 필터링으로 일부만 집계되고 있었음을 역산으로 검증)

### 2026-07-22
- 거래명세서 "고객사별 청구 규칙" 엔진 신규 ✅ (`plan_거래명세서편집_규칙엔진.md`, 사용자 요청: 고객사마다 실제 제출 양식이 달라 자동 계산 결과를 그대로 못 쓰는 문제 — 처음 계획했던 "그때그때 수동 편집"에서, 사용자가 스크린샷으로 보여준 조건식 기반 재사용 규칙 엔진으로 방향 확장)
  - 미리보기 화면이 **좌(시스템 자동계산 원본, 읽기전용) / 우(고객사 청구 명세서, 규칙 적용+수동 편집 가능)** 2단 구성으로 전면 개편. 오른쪽 표의 품명 셀을 클릭하면 조건식 편집창(AND/OR 그룹 조합, 예: "공정 코드 == P")이 뜨고, 조건에 맞는 왼쪽 원본 행들을 자동으로 합산해 하나의 청구 행을 만듦
  - **규칙은 거래처+업무명 조합별로 저장·재사용** — 한 번 만들어두면 같은 거래처+업무명의 다음 명세서부터 자동 적용됨. 규칙으로 만들어진 행도 수량·단가·금액 직접 수정, 새 행 추가가 추가로 가능(수동 편집은 규칙에 반영되지 않고 이번 건에만 적용 — "조건식 편집창으로 만든 행=규칙", "수량 칸을 직접 고친 것=이번 건만"으로 구분)
  - 조건 비교는 코드는 일치함(`==`)만, 품목명·작업명은 포함함(`contains`)까지 지원. 조건 그룹을 하나도 안 만들면(빈 값) "전체 합산" 규칙(코드 구분 없이 전부 한 행으로) — 원본 행은 순서가 빠른 규칙부터 검사해 처음 매칭되는 규칙 하나에만 속함(이중 청구 방지)
  - 코드표 `추가봉입비`를 `"M"`에서 `"MM"`으로 변경(봉입비 `"M"`과 구분되도록) — `scripts/billing.py`
  - DB 신규: `청구품목규칙`(거래처+업무명별 재사용 규칙, 조건은 JSON 컬럼), `거래명세서_품목`(거래명세서별 원본·최종 품목 스냅샷 — 편집 이력 보존), `거래명세서.편집여부` 컬럼 추가(`scripts/init_db_mariadb.py`, 로컬 PC 반영 완료 — **사무실 PC는 배포 시 별도 실행 필요**)
  - `scripts/billing.py`: `평가_조건()`·`적용_규칙()`(조건 트리 평가·배타적 규칙 적용) 신규, Excel 작성부를 `write_거래명세서_excel()`로 분리(`generate_거래명세서_excel()`은 이 함수에 위임 — Streamlit 경로는 완전히 그대로 유지, 실제 발행 건으로 리팩터링 전후 바이트 동일 확인)
  - `scripts/api.py`: `GET/PUT /청구품목규칙` 신규, `POST /거래명세서미리보기`가 저장된 규칙을 자동 적용한 `규칙적용결과`·미분류 항목까지 반환하도록 확장, `POST /거래명세서요청`이 `품목_최종`(편집 결과)·`규칙`을 받아 서버가 재계산한 원본과 비교해 `편집여부` 자동 판정 + `거래명세서_품목`에 원본·최종 이력 저장, `GET /거래명세서엑셀/{no}`는 편집된 건이면 저장된 최종 품목을 그대로 사용(재계산 안 함, 단가마스터가 나중에 바뀌어도 발행 당시 금액 유지), `POST /거래명세서부분취소`는 편집된 거래명세서의 부분취소를 막되(전체취소는 그대로 허용)
  - Next.js 신규: `components/ConditionRuleModal.tsx`(조건식 편집창), `lib/billingRules.ts`(`billing.py`의 조건 평가·규칙 적용 로직을 프론트에도 그대로 복제 — 규칙 저장 시 서버 왕복 없이 즉시 화면에 반영), `app/api/billing-rules/route.ts`(BFF 프록시). `InvoicePreviewDialog.tsx` 좌우 2단으로 전면 개편, `Tab4Invoice.tsx`가 미리보기 시 저장된 규칙을 함께 불러오고 확정 시 편집된 최종 품목·규칙을 함께 전송하도록 연동
  - 검증: `평가_조건`·`적용_규칙` 단위 테스트(AND/OR 조합·포함함·전체 합산·이중 청구 방지 전부 통과), 리팩터링 회귀 확인(SKILL-20, 코드표 변경만 반영한 상태로 바이트 비교 — sheet2.xml sha256 완전 일치), 실제 로컬 DB로 규칙 저장→재조회 시 자동 적용→수동 수정과 규칙 분리 저장→편집여부 판정→Excel 다운로드(저장된 스냅샷 사용)→부분취소 차단/전체취소 허용까지 11단계 API 종단 테스트 전부 통과, Playwright(이번에 devDependency로 신규 설치)로 실제 브라우저에서 로그인→탭 이동→행 선택→미리보기→조건 규칙 저장→규칙 결과 반영(수량·금액 정상 계산)→수동 수정→새 행 추가→취소까지 실제 클릭 흐름 확인(취소로 닫아 DB에는 아무것도 안 남김)
  - React `react-hooks/set-state-in-effect` 린트 대응(SKILL-24) — prop 변경 시 로컬 state를 리셋하는 패턴을 `useEffect`+`setState` 대신 부모의 `key` 재마운트 + `useState` 초기화 함수로 구조 변경
- 위 규칙 엔진 실사용 피드백 반영 3건 ✅ (사용자가 실제 화면으로 규칙을 만들어보며 제기)
  - **미리보기 화면 확대**: `InvoicePreviewDialog.tsx`를 고정 `max-w-6xl`에서 `w-[96vw] max-w-[1600px] h-[92vh]`로 확대
  - **조건식 편집창을 전체화면 모달 → 오른쪽 도킹 패널로 변경**: "조건식 화면이 고정이다 보니 원본 내역을 확인할 수 없음" 피드백 — `ConditionRuleModal.tsx`가 더 이상 화면 전체를 덮는 `fixed inset-0` 모달이 아니라, 부모 다이얼로그 안에서 `absolute inset-y-0 right-0`로 오른쪽에만 도킹되는 패널이 되어 왼쪽 원본 표를 계속 보면서 조건을 만들 수 있음
  - **원본 값 자동완성으로 오타 방지**: "원본의 품명을 복사할 수 있으면 좋겠어 — 직접 키인은 오타 위험" 피드백 — 조건 값 입력칸(코드/품목/작업명)과 최종청구품명 입력칸에 지금 미리보기 중인 명세서의 원본 표에 실제 등장하는 값을 `<datalist>`로 제안(목록 선택도, 직접 타이핑도 가능 — "포함함" 조건은 부분 문자열도 입력해야 해서 완전 강제 선택으로 막지 않음)
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과
- 편집된 거래명세서 "원본 vs 최종" 비교 이력 팝업 신규 ✅ (사용자 질문: "원본과 수정본의 차이 이력관리는 어떻게 관리하지?" → 위 규칙 엔진이 `거래명세서_품목`에 이미 저장은 하고 있었지만 조회 화면이 없다는 걸 확인하고 "지금 팝업을 만들어 줄래"로 요청)
  - `scripts/api.py`: `GET /발행목록` 응답에 `편집여부` 필드 추가, 신규 `GET /거래명세서품목이력/{no}` — `거래명세서_품목`에서 구분(원본/최종)별로 조회해 반환(이 기능 이전에 발행됐거나 편집 없이 원본 그대로 발행된 건은 둘 다 빈 배열)
  - Next.js 신규: `app/api/invoice-history/[no]/route.ts`(BFF 프록시), `components/InvoiceHistoryDialog.tsx`(원본/최종 좌우 비교, 읽기 전용 — `InvoicePreviewDialog.tsx`의 2단 레이아웃 재사용)
  - `InvoiceIssuedLevel1Table.tsx`의 거래명세서번호 옆에 `편집여부=1`인 건만 클릭 가능한 "편집됨" 배지 추가(발행요청목록·발행완료 양쪽 다 노출 — 편집 여부는 발송 상태와 무관), `Tab4IssuedList.tsx`에 다이얼로그 상태 배선
  - 데이터 fetch effect도 `react-hooks/set-state-in-effect`에 걸릴 수 있음을 추가로 확인(SKILL-24 보강) — effect 맨 앞에서 동기적으로 `setLoading(true)`를 부르면 fetch 자체가 비동기라도 걸림, 초기값을 애초에 `useState(true)`로 잡고 그 줄을 지워서 해결
  - 검증: `python -m py_compile`, `npx tsc --noEmit`·`npx eslint .` 통과. 로컬 DB에 편집된 거래명세서 1건·편집 안 된 거래명세서 1건을 실제로 만들어 `/발행목록`의 `편집여부` 값과 `/거래명세서품목이력/{no}`가 두 케이스(정상 데이터 있음 / 빈 배열) 모두 정확히 반환하는지 7단계 API 종단 테스트로 확인, 테스트 데이터 정리
  - **사무실 PC 배포 완료(2026-07-22)** — 이번 라운드(화면 개선 3건 + 이력 팝업)에서 바뀐 `scripts/api.py`를 사무실 PC에 반영, FastAPI 재시작까지 사용자가 직접 완료
  - 원본·최종 차이를 배경색으로 강조하는 안은 시도했다가 보류(같은 날) — 전체 합계 차이 배너 + 강조를 만들었으나, 사용자가 실제로 원한 건 "행별" 금액 차이였고, 규칙으로 원본 여러 행이 최종 한 행으로 합쳐지는 경우 행 단위 대응이 애매해진다는 점을 재확인한 뒤 "편집됨 배지로 이미 알 수 있어 배경색은 의미 없다"고 판단해 되돌림 — 이후 별도 요청 시 재검토
- 각대대봉투(큰 봉투) 단가 미등록 시 청구 계산 버그 수정 ✅ (사용자 제보: 의뢰서 95073에서 미발행목록 "봉입건수"(6,090)와 거래명세서 미리보기 "봉입비" 수량(7,670)이 다르게 표시됨 — 실제 사무실 PC 데이터로 원인 추적)
  - **원인:** `scripts/billing.py`의 `build_품목행()`(미리보기·Excel 발행용)과 `calc_공급가맵()`(미발행목록 예상금액용)이 각대대봉투 단가 미등록 상황을 서로 다르게(둘 다 잘못) 처리하고 있었음 — `build_품목행()`은 각대대봉투 단가가 등록돼 있어도 무조건 일반 단가로 합산, `calc_공급가맵()`은 각대대봉투 단가가 미등록이면 무조건 0원(무상) 처리. Streamlit 시절부터 있던 계산식(최근에 생긴 버그 아님), 아직 개발 중(실사용 전)이라 과거 발행 건 점검은 불필요하다고 사용자 확인
  - **사용자 확정 규칙:** 일반단가·각대대단가 둘 다 등록 → 각자 단가로 분리 계산 / 둘 중 하나만 등록 → 그 등록된 단가로 합산 계산 / 둘 다 없음 → 무상(0원)
  - `scripts/billing.py`에 공용 헬퍼 `_혼합단가_행목록(일반수량, 대대수량, 일반단가, 대대단가)` 신규 — 위 규칙대로 (수량,단가) 조합 리스트를 반환. `calc_공급가맵()`·`build_품목행()`의 "봉입비"·"봉입자재비/봉투제작비" 계산을 이 헬퍼 기반으로 통일(두 함수가 이제 항상 같은 결과를 냄). `build_품목행()`에서 분리 계산이 되는 경우 같은 품목명·작업명이지만 단가가 다른 행이 자연스럽게 2개 생성됨(기존 `(품목,작업명,단가)` 딕셔너리 키 구조 그대로 활용, 구조 변경 없음)
  - 검증: `_혼합단가_행목록()` 5가지 케이스 단위 테스트 통과. 95073 실제 데이터로 재구성해 재계산 — 수정 후 `calc_공급가맵()` 합계가 `build_품목행()` 합계(2,500,431원, 실제 관측된 미리보기 총합계와 정확히 일치)와 같아짐을 확인, 수정 전 `calc_공급가맵()`이 냈던 값(2,443,551원, 실제 미발행목록에 떴던 값)과의 차이(56,880원)가 이번 버그 때문이었음을 재현으로 확인. 각대대단가를 임시로 등록한 가상 케이스로 분리 계산(두 행)도 정상 동작 확인. 회귀 확인 — 각대대봉투 미사용 실제 발행 건(D-202607-00009) 재계산 시 총합계 172,869.74원(사용자가 이전에 보여준 화면 172,870원과 일치) — 각대대봉투=0인 경우는 수정 전후 계산식이 수학적으로 동일함을 확인
  - (아래 후속 수정으로 이어짐 — `scripts/billing.py` 사무실 PC 배포는 후속 수정과 함께 한 번에 진행됨)
- (같은 날, 후속 수정) "봉입비"에서 각대대봉투 가산 자체를 제거 ✅ (사무실 PC에 위 수정 반영 후 사용자가 다시 확인 — "봉입비 수량이 여전히 7,670으로 나오는데 이 숫자가 잘못된 것 같다")
  - **재확인된 원인:** 각대봉투 1,580 + 창봉투(일반봉투) 4,510 = 6,090으로 "봉입건수"(6,090)와 정확히 일치 — 즉 봉입건수 자체가 이미 각대대봉투 작업까지 포함한 전체 건수였는데, 계산식이 여기에 각대대봉투 수량을 또 더해서(6,090+1,580=7,670) 이중으로 계산하고 있었음
  - **사용자가 설명한 올바른 개념:** "봉입비" = 봉입건수(기계작업) × 봉입단가 + 수작업건수 × 각대대봉투봉입단가("수작업 단가" — 각대대봉투는 기계가 아니라 사람이 손으로 처리해야 해서 별도 수작업 비용이 필요함). 다만 "수작업건수"는 개별 작업 건 단위 봉투형태 데이터가 있어야 계산 가능한데 **지금은 그 데이터를 받아오지 못해 계산 자체가 불가능** — 데이터가 들어오기 시작하면 그때 봉입건수(기계작업)와 수작업건수를 분리해서 각각 표기하기로 함(향후 작업, 이번 범위 아님)
  - `scripts/billing.py`의 `build_품목행()`·`calc_공급가맵()` 둘 다 "봉입비"를 다시 `봉입건수 × 봉입단가`만의 단순 계산으로 되돌림(직전 라운드에서 적용했던 `_혼합단가_행목록()` 분리/합산 규칙은 "봉입비"에서는 제거). "봉입자재비"/"봉투제작비"(실물 봉투 자재비)는 그 규칙 그대로 유지 — 공임(봉입비)과 자재비(봉입자재비)는 서로 다른 개념이라 이번엔 공임 쪽만 고침
  - 검증: 95073 데이터로 재확인 — 봉입비 수량 6,090(단가 14원, 금액 85,260원)으로 정상화, 봉입자재비는 6,090(변화 없음) 그대로 유지, `calc_공급가맵()`·`build_품목행()` 합계 여전히 일치(2,478,311원). 각대대봉투 미사용 건(D-202607-00009) 재확인 시 총합계 172,869.74원 그대로(회귀 없음)
  - 향후 작업(수작업건수 데이터 수신 시 봉입비 기계작업/수작업 분리 표기)은 `.claude/plans/bug_각대대봉투_봉입비_계산.md`에 기록
  - **사무실 PC 배포 완료(2026-07-22)** — `scripts/billing.py`(위 1차 수정분 포함) 사용자가 직접 반영, FastAPI 재시작까지 완료
- 거래명세서 미리보기 "새 행 추가"에 단가 입력 + 금액 자동계산 추가 ✅ (사용자 요청: "새행 추가 시 단가도 표기할 수 있게 해줄래 수량과 단가표기 후 금액란도 자동으로 계산해주고")
  - `InvoicePreviewDialog.tsx`의 `addManualRow()` — 새 행의 단가 초기값을 `null`(입력칸 대신 "—" 표시)에서 `0`(바로 입력 가능)으로 변경. `null`은 규칙으로 여러 원본 행이 합쳐졌는데 단가가 서로 달라 표시 불가한 경우 전용이라, 새로 추가하는 행에는 애초에 해당하지 않는 상태였음(막혀 있던 게 빠뜨린 부분)
  - `updateField()` — 수량 또는 단가를 고치면 단가가 있는(=병합으로 "—"가 아닌) 행에 한해 금액을 `수량×단가`로 자동 재계산. 금액 칸 자체는 여전히 직접 수정 가능(특수 케이스 대응), 이후 수량·단가를 다시 건드리면 그 시점 값으로 재계산됨
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과

### 2026-07-23
- 로그인 직후 첫 화면 로딩 최적화 — Suspense 스트리밍 적용 ✅ (`plan_로그인화면로딩최적화.md`, 사용자 확정: "운영모드에서도 필요한 작업, 진행해달라")
  - **원인:** `frontend/app/page.tsx`가 로그인 직후 5개 API(`/summary`·`/미발행목록`·`/발행목록`·`/거래처마스터`·`/단가마스터`)를 `Promise.all`로 전부 기다린 뒤에야 화면 전체를 그려서, 지금 보려는 탭과 무관한 데이터까지 다 기다려야 첫 화면이 떴음(실측 8.2초)
  - Next.js 16.2.10 공식 스트리밍 패턴(`node_modules/next/dist/docs/01-app/02-guides/streaming.md`) 적용: `page.tsx`는 가장 가벼운 `/거래처마스터`만 await해서 401 인증 체크 처리, 나머지 4개는 `loadSummary()`·`loadInvoice()`·`loadIssued()`·`loadPricing()` async 함수로 감싸 await 없이 호출해 Promise만 `Dashboard.tsx`에 전달(5개 fetch 자체는 여전히 동시에 시작 — 네트워크 병렬성은 유지)
  - `Dashboard.tsx`: `Suspense`+`use()`로 각 탭 데이터를 풀어쓰는 얇은 wrapper 5개(`SummaryTab`·`ClientsTab`·`StaffTab`·`InvoiceTab`·`ClientsMasterTab`) 신규, `TabLoading`(공용 로딩 표시) 신규. 탭1~3·탭4(일부)·탭5(일부)가 같은 `summaryPromise`를 공유해도 fetch가 중복 실행되지 않음(React가 동일 Promise 참조를 재사용). 기존 "탭 항상 마운트+CSS(hidden)" 패턴, `Tab1Summary`~`ClientMasterSection` 내부 구현은 전혀 변경 없음
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과, `npm run build` 성공. 프로덕션 환경(실서비스 DB)은 건드리지 않고 **로컬 격리 환경**(로컬 FastAPI 8000+로컬 MariaDB, `FASTAPI_URL`만 임시 오버라이드)에 임시 계정을 만들어 실제 로그인→응답 스트림 검증 — 정적 뼈대가 0.26초 만에 도착함을 확인(기존엔 5개 API 전부 끝나야 첫 바이트가 나가던 구조), 서버 로그 오류 없음, 응답 본문에 로딩 폴백과 실제 데이터가 정상 포함됨을 확인. 검증 후 임시 계정 삭제, 임시 서버 전부 종료
  - **잔여 항목:** 사무실 PC 실서비스 환경에서 사용자가 직접 로그인해 체감 속도 확인은 이번 세션에서 진행하지 않음(로컬 검증까지만) — `plan_로그인화면로딩최적화.md` 참고
- "미발행 목록" 표 가상 스크롤(virtualization) 적용 ✅ (사용자 제보: 거래명세서 관리 탭에서 사업부→담당자 필터를 연달아 선택하면 43초간 화면이 멈춤)
  - **원인 재현·확정:** 로컬 격리 환경(로컬 FastAPI 8000+로컬 MariaDB, 실제 사무실 PC는 건드리지 않음 — 미발행 4,605건으로 실제 프로덕션 4,613건과 거의 동일 규모)에서 Playwright로 운영 모드(빌드) 기준 재현 — "거래명세서 관리" 탭 클릭 → 미발행 목록 최초 표시까지 **약 9초**(개발 모드였다면 더 길었을 것), 반면 필터 선택 자체(사업부→담당자)는 0.3~0.6초로 원래 빨랐음. `frontend/components/InvoiceSelectionTable.tsx`가 필터링된 건수 전체(최대 4,600여 건)를 가상 스크롤 없이 실제 `<tr>`로 한꺼번에 그리는 구조였던 것이 원인 — 사용자가 겪은 43초는 이 9초짜리 렌더링이 끝나기 전에 필터를 연달아 눌러 무거운 재렌더링이 겹쳐 쌓인 결과로 판단
  - 신규 `frontend/lib/useVirtualRows.ts` — 고정 행 높이 기반 커스텀 가상 스크롤(윈도잉) 훅. 외부 라이브러리(react-window 등) 없이 직접 구현(이 프로젝트의 기존 `MultiSelectCombo.tsx` 등과 동일한 방식). `scroll`·`resize` 이벤트로 현재 보여야 할 행 범위만 계산(overscan 8행), 위/아래 안 보이는 부분은 스페이서 `<tr>` 1개씩으로 대체해 스크롤바 크기·sticky 헤더(SKILL-16)는 그대로 유지. 행 높이는 하드코딩하지 않고 콜백 ref로 실제 첫 행을 실측해 자동 보정. 필터 변경(rowCount 변화) 시 스크롤을 맨 위로 리셋해 빈 화면 노출 방지
  - `InvoiceSelectionTable.tsx`: `<tbody>`를 전체 `rows.map()` 대신 `rows.slice(range.start, range.end)`만 렌더링 + 스페이서 2개. 기존 `React.memo` Row 최적화·"전체 선택" 로직(전체 `rows` 기준)·`selected`(Set) 선택 상태 관리는 전혀 변경 없음 — 화면에 실제로 그려지는 행 개수만 줄임
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과(초기 useEffect 무한갱신 경고는 콜백 ref 방식으로 재설계해 해결), `npm run build` 성공. 같은 로컬 격리 환경에서 재검증 — DOM에 실제 렌더링된 `<tr>` 개수가 4,605건이 아니라 31~42개로 유지됨을 확인, 필터 선택 반영 시간이 72ms·68ms로(기존 641ms·348ms 대비 5~9배) 개선, 스크롤 시 새 데이터가 정상적으로 나타남, 체크박스 선택("선택 1건" 요약)이 스크롤 후에도 유지됨, "전체 선택" 체크박스가 정확히 동작함을 확인. 검증 후 임시 계정 삭제, 임시 서버 전부 종료
  - **잔여 한계(고지):** 이번 수정은 "필터를 바꿀 때마다 표를 다시 그리는" 비용만 해결했고, "거래명세서 관리 탭을 처음 여는 순간"의 약 9초는 거의 그대로 남아있음(가상 스크롤 적용 전후 8.5~10초로 유의미한 차이 없음) — 이 9초는 DOM 렌더링이 아니라 `Tab4`가 `summaryPromise`·`invoicePromise`·`issuedPromise` 3개가 전부 끝나야 화면을 그리는 Suspense 대기 시간(실제 데이터 조회·전송 시간)이 원인으로 추정되며, 이전 세션의 "로그인 로딩 최적화"와 같은 계열이지만 아직 손대지 않은 부분 — 다음 논의 필요 시 별도 작업으로 진행 예정
- 미발행 목록 단가 미등록 경고 문구 간소화(번호 나열 → 건수 요약) ✅ (사용자 제보: 단가 미등록 의뢰서 수백 건의 번호를 전부 쉼표로 나열해 읽을 수 없는 문자 벽이 됨)
  - **실제 서로 다른 두 위치에 같은 문제가 있었음:** ① `Tab4Invoice.tsx`의 `handlePreviewClick()` — "거래명세서 요청" 버튼 클릭 시 검증 배너, ② `InvoiceSelectionSummaryBar.tsx` — 항목 선택 즉시 뜨는 요약 표 아래 경고(실제 사용자가 본 것은 이쪽이었음, 1차 수정에서 ①만 고쳐 재현이 안 됐다가 재확인 후 ②를 찾아 함께 수정)
  - 두 곳 모두 `${배열.map(r => r.의뢰서번호).join(", ")}` 형태의 번호 나열을 `${배열.length}건이 포함되어 있습니다` 형태의 건수 요약으로 변경, 표의 "단가" 열에 이미 있는 건별 "⚠️ 미등록" 표시를 확인하라는 안내 추가
  - 검증: `npx tsc --noEmit`(JSX 안에 큰따옴표를 직접 쓰면 `react/no-unescaped-entities` 에러 발생 → `&quot;`로 치환) · `npx eslint .` 통과
- 거래명세서 관리 필터 변경 시 이전 선택(체크) 자동 초기화 ✅ (사용자 제보: 업무의뢰서를 체크한 뒤 사업부·담당자 등 필터를 바꾸면, 화면에는 안 보이는 이전 선택이 계속 남아있어 집계표만 안 맞는 것처럼 보이는 혼선 발생 — 원래 "필터를 바꿔도 선택 유지"가 의도된 설계였으나, 안내도 초기화 버튼도 없어 실사용 중 혼란으로 이어짐)
  - **사용자 확정(2026-07-24):** 여러 필터를 오가며 선택을 이어가는 기능보다 "화면에 보이는 것과 집계표가 항상 일치"하는 단순한 동작을 우선 — 필터가 바뀌면 이전 선택을 자동으로 비움
  - `frontend/lib/useFilters.ts`에 공용 훅 `useResetOnFilterChange(filterKey, onReset)` 신규 — 기존 `usePrunedSelection`과 동일한 "렌더링 중 상태 조정" 패턴(`useEffect` 대신 렌더 중 이전 값과 비교해 즉시 `setState`)이라 깜빡임·`react-hooks/set-state-in-effect` 린트 경고 없음
  - `Tab4Invoice.tsx`(미발행 목록의 `selected`)·`Tab4IssuedList.tsx`(발행요청목록/발행완료의 `selected1`·`selected2` 함께 초기화) 양쪽에 동일 패턴 적용 — 사용자가 겪은 건 미발행 목록이지만 발행요청목록·발행완료도 같은 필터+선택 구조라 동일 혼선 가능성이 있어 함께 수정
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과. 로컬 격리 환경(로컬 FastAPI 8000+로컬 MariaDB, 사무실 PC는 건드리지 않음)에 임시 계정을 만들어 Playwright로 실제 확인 — 항목 체크 → "선택 1건" 요약 표시 확인 → 사업부 필터 변경 → 체크박스 0개·요약 문구 사라짐·전체선택 미체크 상태로 정확히 초기화됨을 확인. 검증 후 임시 계정 삭제, 임시 서버 전부 종료
- 거래명세서 미리보기 왼쪽(원본) 표에 "오른쪽 표에 반영됨" 체크 표시 추가 ✅ (사용자 요청: "선택된 항목은 체크가 되어 식별이 쉬웠으면 좋겠어" — 지금까지는 "안 걸린 원본 항목" 목록으로 미분류만 알 수 있고, 이미 반영된 항목은 왼쪽 표만 봐서는 구분이 안 됐음)
  - `InvoicePreviewDialog.tsx`: 기존에 "미분류"만 계산하던 `적용_규칙()` 결과를 재사용해 "매칭됨" 집합도 함께 파생(참조 동등성으로 미분류의 여집합 계산 — `lib/billingRules.ts` 수정 없이 그대로 재사용). 왼쪽 표 맨 앞에 체크(✓) 열 추가, 규칙에 매칭된 행에만 초록색 체크 표시
  - 검증: `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과
- 거래명세서 미리보기 오른쪽(고객사 청구 명세서) 표 행 순서 변경 기능 추가 ✅ (사용자 요청, 드래그 앤 드롭 대신 위/아래 화살표 버튼 방식 선택 — 더 단순하고 실수할 일이 적음)
  - `InvoicePreviewDialog.tsx`: 신규 `moveRow(idx, direction)` — 배열 위치만 교환하는 단순 스왑(순서 변경은 수량·단가·금액 재계산과 무관해 `원본행_recompute` 호출 불필요). 각 행 마지막 열에 ▲▼ 버튼 추가(첫 행은 위로, 마지막 행은 아래로 비활성화), 최종 확정 시 전송되는 `품목_최종` 배열 순서 그대로 반영되므로 Excel 출력 순서도 함께 바뀜
  - 검증: `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과
- "추가봉입비"에서 삽지 분리 + "동봉물삽입비" 신규 항목 추가 ✅ (사용자 제보: 집계표 "추가용지"와 미리보기 "추가봉입비" 수량 불일치 조사 → `추가봉입비 = (삽지_사용량_합 + 추가용지) × 추가봉입단가`가 원래부터 삽지와 추가용지를 한 항목에 합쳐 청구하던 기존 확정 공식이었음을 확인. 사용자가 실제 업무 개념 확인 후 "추가봉입비는 순수 추가용지만, 삽지는 별도 동봉물삽입비로 청구돼야 함"으로 확정)
  - **사용자 확정(2026-07-24):** 동봉물삽입비는 기존 추가봉입단가 재사용이 아니라 단가마스터에 **신규 "동봉물삽입단가" 필드 추가**. 기존 "삽지비"(삽지제작단가, 자재비 성격)는 그대로 유지 — 동봉물삽입비(노무비 성격)는 별개 항목. **마이그레이션 시 기존 거래처는 "동봉물삽입단가"를 "추가봉입단가"와 동일한 값으로 자동 백필**해 변경 직후 총 청구액이 그대로 유지되도록 함(0으로 두면 삽지분이 청구에서 누락돼 즉시 청구액이 줄어드는 위험 회피)
  - `scripts/init_db_mariadb.py`: `단가마스터`에 `동봉물삽입단가 DECIMAL(10,2)` 컬럼 추가(신규 설치용 DDL + 기존 설치용 `migrate()` 마이그레이션, `UPDATE 단가마스터 SET 동봉물삽입단가 = 추가봉입단가`로 백필). 로컬 PC 반영 완료(13건 전부 정확히 백필 확인) — **사무실 PC는 배포 시 별도 실행 필요**
  - `scripts/billing.py`: `calc_공급가맵()`·`build_품목행()` 둘 다 "추가봉입비" 수량을 추가용지만으로 변경, 신규 "동봉물삽입비"(수량=삽지, 단가=동봉물삽입단가) 추가. `build_품목행()`의 `품목순서`·`코드맵`(신규 코드 "SI")·`코드표순서`에도 반영
  - **실제 버그 발견·수정:** 위 작업 검증 중 `billing.build_단가맵()`이 단가 컬럼 목록을 8개로 하드코딩해두고 있어(`단가컬럼 = [...]`), DB에는 이미 추가된 "동봉물삽입단가" 컬럼이 결과 dict에서 조용히 누락되는 버그를 실사용 데이터 검증 과정에서 발견·수정(신규 필드를 목록에 추가) — 이 수정이 없었으면 화면·API는 정상으로 보여도 실제 계산에는 항상 0원으로 적용될 뻔했음
  - `scripts/api.py`: `단가마스터_신규`·`단가마스터_수정` Pydantic 모델 + `POST/PUT /단가마스터` SQL문에 필드 추가
  - 프론트엔드: `Dashboard.tsx`(`단가행` 타입)·`app/page.tsx`(매핑)·`PricingFormDialog.tsx`(입력 필드)·`PricingMaster.tsx`(`handleUpdated` 타입 — 이것도 별도로 있는 걸 발견해 함께 수정)·`PricingMasterTable.tsx`(표 컬럼) 전부 기존 각대대봉투단가 추가 때와 동일한 패턴으로 반영
  - 검증: `python -m py_compile`, `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과. 로컬 DB 마이그레이션 실행 후 실제 백필 값 SQL로 직접 확인. **회귀 검증(SKILL-20 방식, `git show`로 이전 코드 추출)**: 실제 의뢰서(93429, KB국민카드)로 계산 — 분리된 추가봉입비(42,729원)+동봉물삽입비(78,970원) 합이 이전 결합값(121,699원)과 정확히 일치함을 확인, `calc_공급가맵()`과 `build_품목행()` 두 함수의 총합계(3,641,707원)도 서로 일치함을 확인. 로컬 격리 환경(로컬 FastAPI+MariaDB, 사무실 PC는 미접촉)에서 API 응답·단가관리 화면(표 컬럼 표시, 수정 폼 값 1.5 정상 로드) Playwright로 확인. 검증 후 임시 계정 삭제, 임시 서버 전부 종료
  - `docs/작업현황-대시보드-생성-KNOWLEDGE.md`의 공급가액 공식·Excel 품명 작성 로직 섹션 갱신
  - **사무실 PC 배포 완료(2026-07-24)** — `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` 3개 파일 반영 + `python scripts/init_db_mariadb.py` 마이그레이션 + FastAPI 재시작까지 사용자가 직접 완료. 실제 KB국민카드 이용대금명세서(2026-06-01~06-30)로 미리보기를 열어 "추가봉입비"와 "동봉물삽입비"가 정상적으로 분리되어 표시됨을 사용자가 직접 확인
  - **배포 논의 중 추가로 발견·수정한 위험(2026-07-24, 같은 날):** Streamlit(`scripts/app.py`)도 `billing.py`의 계산 함수를 공용으로 쓰지만, 단가마스터를 읽어 단가맵으로 만드는 코드는 MariaDB용(`billing.build_단가맵`)과 완전히 별개로 SQLite용을 자체 보유(`load_단가마스터()` 73행, 탭4 공용 단가맵 653행) — 둘 다 단가 컬럼을 하드코딩해두고 있어 "동봉물삽입단가"가 빠진 채였음. 그대로 뒀으면 로컬 PC에서 Streamlit을 실행하는 즉시(별도 배포 없이 바로 영향) 삽지분이 어디에도 청구되지 않아 총액이 조용히 줄어들 뻔했음
    - `scripts/init_db.py`: SQLite `단가마스터`에도 `동봉물삽입단가` 컬럼 추가(`create_tables()` DDL) + `migrate_단가마스터_컬럼()`에 마이그레이션 항목 추가(MariaDB와 동일하게 `추가봉입단가`로 백필). 로컬 실행 완료, 16건 전부 정확히 백필 확인
    - `scripts/app.py`: 하드코딩된 단가 컬럼 리스트 2곳에 필드 추가, 단가관리 화면(수정 폼 column_config·입력창, 새 단가 추가 폼)에도 동일하게 입력란 추가 + INSERT/UPDATE SQL문 반영(Next.js `PricingFormDialog.tsx`와 동일한 패턴)
    - 검증: `python -m py_compile scripts/app.py scripts/init_db.py`, `python scripts/init_db.py` 재실행해 SQLite 마이그레이션 확인, `billing.build_단가맵()`으로 SQLite 데이터를 직접 읽어 새 필드가 정상 포함됨을 확인

### 2026-07-28
- 거래명세서 미리보기 조건식 편집창에 "단가" 필드 추가 ✅ (사용자 요청: 코드·품목명·작업명 외 단가도 조건에 넣고 싶다 — 각대대봉투 단가 미등록 버그 수정(`_혼합단가_행목록()`, 2026-07-22)으로 같은 품목명·작업명인데 단가만 다른 원본 행이 실제로 생기는데, 지금까지는 이 둘을 조건으로 구분할 방법이 없었음)
  - 조건 필드 옵션에 "단가" 추가, 비교 방식은 **일치함(=)만** 지원(사용자 확정 — 이상/이하 같은 범위 비교는 이번 범위 아님)
  - **문자열 비교 대신 숫자 비교 필요:** 기존 조건 평가(`billing._평가_조건단일`·`billingRules.평가_and그룹`)는 필드값을 전부 문자열로 바꿔 비교하는데, 단가는 숫자라 그대로 쓰면 파이썬 `str(14.0)`→`"14.0"`과 JS `String(14)`→`"14"`처럼 표현이 달라 매칭이 깨질 위험이 있었음 — `field === "단가"`일 때만 숫자로 변환 후 **소수점 셋째 자리부터 절삭(반올림 아님, 예: 14.567→14.56)** 비교하는 분기를 양쪽에 대칭으로 추가(`billing._절삭2()`·`billingRules.절삭2()`, 둘 다 `Math.floor(x*100+1e-9)/100`과 동일한 로직 — 부동소수점 오차로 `14.29*100`이 `1428.9999...`가 되는 경우를 보정값으로 방지)
  - `frontend/components/ConditionRuleModal.tsx`: 필드옵션에 "단가" 추가, 연산자옵션은 코드와 동일하게 일치함만, 값 입력칸은 `field==="단가"`일 때만 `type="number" step="0.01"`로 전환, `단가옵션` prop·datalist 추가
  - `frontend/components/InvoicePreviewDialog.tsx`: 기존 `코드옵션`·`품목옵션`과 동일한 패턴으로 `단가옵션` 계산해 전달 — `toLocaleString()`은 쓰지 않음(천단위 콤마가 들어가면 `type="number"` 입력값 파싱이 깨짐)
  - DB·API 변경 없음 — `청구품목규칙.조건`이 이미 MariaDB `JSON` 컬럼이고 `scripts/api.py`의 `청구품목규칙_행.조건`도 필드명 제약 없는 일반 `dict`라 새 필드값이 그대로 들어감
  - 검증: `python -m py_compile scripts/billing.py`, `_평가_조건단일()` 단가 케이스 단위 테스트(완전 일치·소수·파이썬 float와 문자열 "14" 비교·절삭 경계값·부동소수점 오차값 등 10가지, 기존 코드·품목·작업명 필드 회귀 포함) 전부 통과, JS로 동일 로직 대조 테스트해 파이썬·TS 결과 일치 확인, `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과, 실제 운영 함수 `적용_규칙()`에 각대대봉투 단가 분리 사례(봉투제작비 단가 20원/120원 두 행)를 넣어 단가 조건으로 정확히 분리되고 무관한 품목(출력비)은 미분류로 남는지 확인
- 거래명세서 Excel 부가세(VAT) 표기 버그 수정 + 거래처별 "포함/별도" 구분 신규 ✅ (사용자 제보: 마케팅팀이 수기로 작성하는 실제 거래명세서와 우리 시스템이 생성한 Excel을 비교해보니, 마케팅 양식은 공급가액·부가세·합계 3개 숫자를 보여주는데 우리 Excel은 공급가액 하나만 계산해서 그 값을 그대로 "합계" 칸에 넣고 "부가세 포함"이라 라벨링하고 있었음 — 부가세가 전혀 더해지지 않은 채 라벨만 "부가세 포함"인 버그)
  - **원인 확인:** DB(`거래명세서.세액`·`합계`, 발행요청목록/발행완료 화면용)에는 부가세 10%가 이미 올바르게 계산돼 저장되고 있었지만, **실제 다운로드되는 Excel 파일**(`billing.write_거래명세서_excel()`)만 이 값을 안 쓰고 품목 합계(공급가액)를 반복해서 넣고 있었음
  - **거래처별 예외 확인(사용자 확정, 2026-07-28):** 일부 거래처는 계약상 단가 자체에 부가세가 이미 포함돼 있어 추가로 더하면 안 됨 → `단가마스터`에 `부가세구분`(`포함`/`별도`, 기본값 `별도`) 컬럼 신규. 부가세 취급은 개별 품목이 아니라 거래처와의 계약 조건이므로, 한 거래명세서 전체에 대해 **그 거래처의 기본단가 행(업무명·작업명이 둘 다 없는 행)의 구분값 하나**로만 판단(업무명·작업명별 예외단가 행에 다른 값이 들어 있어도 흔들리지 않도록)
  - **Excel 표시 위치(사용자 확정):** 소계 줄(공급가액이 찍히는 줄)에 원래 비어있던 "비고" 칸(스크린샷에서 사용자가 표시한 빈 칸)을 그대로 재활용 — 별도 줄이나 템플릿 구조 변경 없이 `"부가세 {금액}"` 텍스트만 채워 넣음(포함 거래처는 그대로 비워둠). 상단 "총계"(K14·한글 금액 D14)와 하단 "합계"(J{합계_행})는 항상 부가세 포함 최종금액으로 통일(지금까지 둘 다 사실상 공급가액이었던 버그 수정)
  - `scripts/billing.py`: `build_단가맵()`에 `부가세구분`(숫자 아닌 문자열이라 기존 단가컬럼과 분리 처리) 반영, 신규 `부가세_계산(거래처명, 단가맵, 공급가액)`(거래처 기본단가 행의 구분값으로 세액·합계 산출 — 이 함수 하나로 여기저기 흩어져 있던 `세액 = round(공급가액 * 0.1)` 하드코딩 5곳을 전부 대체), `write_거래명세서_excel()`에 `세액` 파라미터 추가(비고 칸 표기 + 상단/하단 최종금액 반영), `generate_거래명세서_excel()`은 내부에서 `부가세_계산()`을 호출하도록 연동(시그니처 변경 없음 — app.py 호출부 무수정)
  - `scripts/api.py`: `GET /예상공급가액`·`POST /거래명세서부분취소` 재계산이 `부가세_계산()` 사용하도록 교체, `POST /거래명세서미리보기` 응답에 `부가세구분` 추가(프론트가 `*0.1`을 무조건 가정하지 않도록), `단가마스터_신규`·`단가마스터_수정` Pydantic(`Literal["포함","별도"]`)과 INSERT/UPDATE SQL에 필드 반영, `GET /거래명세서엑셀/{no}`의 편집 스냅샷 경로는 이미 저장된 `거래명세서.세액`을 그대로 사용(재계산 안 함, 기존 "발행 당시 금액 유지" 원칙과 동일)
  - `scripts/init_db_mariadb.py`·`scripts/init_db.py`: `단가마스터`에 `부가세구분` 컬럼 추가(`ENUM('포함','별도') DEFAULT '별도'` / SQLite `TEXT DEFAULT '별도'`) — `DEFAULT` 자체가 기존 행에도 자동 반영되어 별도 백필 UPDATE 불필요(지금까지 모든 거래처가 사실상 "별도"였으므로 이 기본값이 곧 기존 동작과 동일)
  - 프론트엔드: `Tab4Invoice.tsx`(`handleConfirmSubmit()`가 `previewData.부가세구분` 반영), `InvoicePreviewDialog.tsx`(좌/우 "합계" 한 줄 → 공급가액/부가세/합계 3줄로 확장, 미리보기 화면과 실제 Excel 숫자가 어긋나지 않도록), `PricingFormDialog.tsx`(부가세구분 선택 드롭다운 + "거래처 기본단가 행 값이 실제 계산에 쓰입니다" 안내), `PricingMasterTable.tsx`(표 컬럼)·`PricingMaster.tsx`(`handleUpdated` 타입)·`Dashboard.tsx`(`단가행` 타입)·`app/page.tsx`(매핑) 기존 필드 추가 패턴 그대로 반영
  - `scripts/app.py`(Streamlit): `load_단가마스터()`·탭4 공용 단가맵(자체 보유한 중복 구현) 양쪽에 `부가세구분` 반영, 거래명세서 요청 처리·`_취소_변경계획()` 부분취소 재계산을 `billing.부가세_계산()` 사용하도록 교체, 단가관리 화면(수정 폼·새 단가 추가 폼)에 부가세 선택란 추가 + INSERT/UPDATE SQL 반영
  - 검증: `python -m py_compile`(전체 수정 파일), `부가세_계산()` 단위 테스트(포함/별도/구분값 없음/거래처 자체가 단가맵에 없는 경우 4케이스) 전부 통과. **회귀 검증(SKILL-20 방식, `git show HEAD`로 이전 코드 추출)**: 별도(기본값) 조건에서 신·구 코드의 `write_거래명세서_excel()` 출력을 행 단위로 전수 대조 — 의도한 3개 행(14·30·31, 부가세 반영분)만 다르고 나머지(품목행 16~29 등)는 완전히 동일함을 확인. **로컬 MariaDB 실제 데이터**(BC카드, 기본단가 행 id=29)로 종단 테스트: 별도 상태로 생성(공급가액 5,120원 · 비고 칸 "부가세 512" · 합계 5,632원) → `부가세구분`을 임시로 "포함"으로 변경해 재생성(비고 칸 공란 · 합계 5,120원으로 부가세 미가산 확인) → 다시 "별도"로 원상복구 후 재생성해 첫 결과와 완전히 동일함을 확인, 테스트로 바꾼 DB 값은 즉시 복구. `billing.build_단가맵()`으로 로컬 SQLite(`work/dashboard.db`) 데이터도 직접 읽어 `부가세구분` 필드가 정상 포함됨을 확인. `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과
  - `python scripts/init_db_mariadb.py`(로컬)·`python scripts/init_db.py` 재실행 — 로컬 MariaDB 13건, SQLite 16건 전부 `'별도'`로 정확히 채워졌음을 SQL로 직접 확인
- `PUT /단가마스터/{id}` 등 UPDATE 계열 API가 값이 실제로 안 바뀌면 "해당 id가 없습니다"로 잘못 응답하는 버그 수정 ✅ (사용자 제보: KB증권 단가에서 부가세구분을 포함→별도로 두 번째 저장할 때만 이 오류가 뜸 — 실제 원인은 그 시점에 프론트가 사무실 PC FastAPI를 보고 있었는데 부가세구분 기능이 아직 그쪽엔 배포 전이라 서버가 이 필드를 무시했고, 같은 날 두 번째 저장이라 "수정일"조차 안 바뀌어 결과적으로 아무 값도 안 바뀐 것 — 이 과정에서 원래부터 있던 별개의 잠재 버그를 우연히 걸러냄)
  - **원인:** `pymysql` 기본 설정에서는 `cur.rowcount`가 "조건에 매칭된 행 수"가 아니라 "실제로 값이 바뀐 행 수"를 반환함(MariaDB/MySQL 고유 동작 — SQLite는 이 문제 없음). `단가마스터_수정_요청()`(및 `거래처마스터`·`거래명세서발행`·`거래명세서발행취소` 총 4곳)이 `if cur.rowcount == 0: raise 404("없습니다")`로 존재 여부를 판정하고 있어서, id는 멀쩡해도 제출값이 DB와 완전히 같으면(같은 날 재저장 등) 0으로 잘못 판정되는 구조였음
  - `scripts/api.py`의 `get_db()` 연결 시 `client_flag=CLIENT.FOUND_ROWS` 추가 — 연결 설정 한 곳만 고쳐서 `rowcount`가 "조건에 매칭된 행 수"를 반환하도록 통일(위 4곳 전부 자동으로 해결, 코드 4곳을 개별 수정할 필요 없음)
  - 검증: `python -m py_compile scripts/api.py`, 로컬 MariaDB로 재현 시나리오 직접 실행 — 값이 동일한 no-op UPDATE가 수정 전엔 `rowcount=0`(버그 재현), 수정 후엔 `rowcount=1`(정상)로 바뀜을 확인. 진짜 존재하지 않는 id(999999)는 수정 후에도 여전히 `rowcount=0`으로 정확히 판정돼 404가 정상 동작함을 함께 확인(과잉수정 아님)
- **발행완료 화면에서 거래명세서 다운로드가 항상 실패하는 회귀 버그 수정** ✅ (사용자 제보: 다운로드 클릭 시 브라우저가 `D-202607-00029.json` 파일을 받으려다 "사이트를 사용할 수 없음"으로 3번 반복 실패 — 편집된 거래명세서였음)
  - **원인:** 어제(2026-07-28) 부가세 기능 추가 때 `billing.write_거래명세서_excel()`에 `세액` 파라미터를 새로 추가하면서(순서상 3번째 인자), `GET /거래명세서엑셀/{no}`의 **편집된 거래명세서(구분='최종' 스냅샷) 다운로드 경로**가 옛 5개 인자 그대로 호출하도록 남아있던 것을 놓침 — 인자 개수가 안 맞아 `발행일` 자리에 값이 비어 서버가 500 오류를 던졌고, 그 오류 응답을 브라우저가 파일 다운로드로 처리하려다 실패를 반복한 것(레거시 재계산 경로 쪽 `generate_거래명세서_excel()` 호출부는 함수 시그니처가 안 바뀌어 문제없었음 — 그래서 편집 없이 발행된 오래된 건은 영향 없었고, 규칙엔진으로 편집된 건만 다운로드가 깨짐)
  - `scripts/api.py`의 해당 호출부를 이미 저장돼 있는 `거래명세서.세액`(발행 요청 시점에 확정된 값)을 그대로 넘기도록 수정 — 재계산하지 않음(편집 이후 단가마스터가 바뀌어도 발행 당시 금액을 유지하는 기존 원칙과 동일)
  - 검증: `python -m py_compile scripts/api.py`. 로컬 MariaDB에 편집된 거래명세서 상황을 그대로 재현하는 테스트 데이터(거래명세서+거래명세서_의뢰서+거래명세서_품목 구분='최종')를 만들어 `거래명세서엑셀()` 핸들러를 실제로 호출 — 수정 전엔 예외로 실패했을 인자 배치였음을 확인, 수정 후 200 OK로 정상 생성되고 소계(공급가액 1,000,000)·비고 칸("부가세 100,000")·합계(1,100,000)가 저장된 값과 정확히 일치함을 확인. 테스트 데이터 정리 완료
- 거래명세서 Excel 부가세 표기 미세조정 ✅ (사용자 피드백: "부가세" 문구는 지우고 금액만 공급가액 폰트 크기처럼 보이게 해달라 — 비고 칸이 좁아서 "부가세 1,385,112" 텍스트가 작게 눌려 보였음)
  - `scripts/billing.py`의 `write_거래명세서_excel()` — 비고 칸(N{소계_행})에 "부가세 " 라벨 없이 **금액 숫자만** 쓰도록 변경, 스타일도 원래 비고 칸 스타일이 아니라 **공급가액 칸(K{소계_행})과 완전히 동일한 스타일(글꼴 크기·정렬·천단위 서식)을 그대로 복사**해서 적용 — 텍스트(`inlineStr`)가 아니라 진짜 숫자 셀로 써서 공급가액과 시각적으로 나란히 맞아떨어지게 함
  - 검증: `python -m py_compile scripts/billing.py`, 실제 로컬 MariaDB 데이터(BC카드)로 재생성 — 비고 칸이 `s="111"`(공급가액과 동일 스타일)의 순수 숫자(512)로 표기되고 "부가세" 문구가 없음을 확인. "포함" 거래처(세액=0)는 여전히 빈 칸으로 유지됨을 함께 확인(회귀 없음)
- **사무실 PC 배포 완료(2026-07-29)** — 2026-07-28~29 이틀치 변경분(단가 조건식 "단가" 필드, 부가세 표기 버그 수정+거래처별 포함/별도 구분, 단가마스터 UPDATE "id 없음" 오판 버그, 편집된 거래명세서 재다운로드 회귀 버그, 부가세 폰트 조정) 전체를 `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` 반영 + `python scripts/init_db_mariadb.py` 마이그레이션(`단가마스터.부가세구분` 컬럼) + FastAPI 재시작까지 사용자가 직접 완료. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음
- 거래명세서 미리보기 "조(작업명 기반 그룹)" 분리 발급 + 통합 엑셀(다중 시트) 생성 ✅ (`.claude/plans/plan_거래명세서_조별분할발급_통합엑셀.md`, 계획 승인 후 1~5단계 전체 구현 — **로컬 PC(MariaDB) 반영·검증까지 완료, 사무실 PC 미배포**)
  - **DB 스키마**: `청구품목규칙.조 VARCHAR(50)`, `거래명세서.묶음번호`·`시트명 VARCHAR` 컬럼 추가(`scripts/init_db_mariadb.py` migrate() — 기존 `편집여부` 추가 때와 동일 패턴). SQLite(`init_db.py`)는 청구품목규칙·정규화된 거래명세서 테이블 자체가 없어(옛 `거래명세서이력`만 보유) 해당 사항 없음을 확인 후 스킵
  - `scripts/billing.py`: `적용_규칙()` 결과에 `조` 키 추가(규칙에 실린 값 그대로 전달). 신규 `combine_거래명세서_시트들()` — `write_거래명세서_excel()`이 만든 완성된 단일-시트 xlsx 여러 개를 표지 시트("요청내용")·셀 코멘트(`comments1.xml`)·`vmlDrawing` 없이 데이터 시트만 한 워크북에 이어붙임. 모든 입력이 같은 템플릿·같은 직인 이미지에서 나온다는 전제로 styles·sharedStrings·theme·이미지·printerSettings는 첫 파일 것을 그대로 공유 재사용(재넘버링 불필요 — 구현 전 zipfile로 템플릿 내부 구조 실제 덤프해 확인). Print_Area는 시트마다 원래 파일의 범위를 그대로 옮기되 localSheetId만 새 시트 인덱스로 교정, 시트명 중복 시 "(2)" 자동 접미사
  - `scripts/api.py`: `POST /거래명세서요청` — `품목_최종` 각 행의 "조"로 그룹핑해 조가 2개 이상이면 그룹별로 공급가액·세액(`billing.부가세_계산()`)·합계를 각각 계산해 거래명세서를 그룹 수만큼 채번·저장(모두 같은 `의뢰서번호_목록` 전체를 공유, 묶음번호는 첫 그룹 번호 재사용) — 조가 1개(또는 미지정)면 기존과 완전히 동일한 단일 거래명세서 경로 그대로(하위호환). `GET /거래명세서엑셀/{no}`는 조회한 거래명세서에 묶음번호가 있으면 같은 묶음의 형제 전부를 각각 엑셀로 만든 뒤 `combine_거래명세서_시트들()`로 합쳐 반환(어느 조 번호로 다운로드해도 동일한 통합 파일). `_규칙_저장`·`청구품목규칙_행`에 조 컬럼 반영
  - 프론트엔드: `ConditionRuleModal.tsx`에 "조(시트명)" 입력 필드 추가 — "작업명 == X" 단일 조건이면 조 입력칸이 비어 있는 동안 X로 자동 채움(직접 입력하면 그 값을 그대로 존중). `billingRules.ts`(`적용_규칙()` 결과에 조 필드, billing.py와 대칭) → `InvoicePreviewDialog.tsx`(오른쪽 표를 조 단위로 그룹 표시 + 그룹별 소계, 조가 2개 이상일 때만 그룹 헤더 노출 — 조를 안 쓰면 기존 화면 그대로)
  - **계획에 없었던 추가 발견·수정**: `GET /발행목록`이 의뢰서번호당 거래명세서 1개만 있다고 가정한 dict 매핑이라, 분할발급된 의뢰서(거래명세서 여러 개가 같은 의뢰서를 공유)가 조용히 하나만 남고 나머지가 발행요청목록/발행완료 화면에서 안 보이는 문제를 발견 — 사용자 확인 후 "의뢰서를 거래명세서별로 각각 표시"로 확정, `발행맵`을 리스트 기반으로 교체해 의뢰서가 속한 거래명세서 수만큼 행을 중복 표시하도록 수정. 그 결과 `Tab4IssuedList.tsx`·`InvoiceIssuedLevel2Table.tsx`의 레벨2 선택 상태(`Set<string>`)와 React `key`가 의뢰서번호 단독 키라 동일 의뢰서가 2행으로 나타나면 서로 충돌해(체크 하나가 둘 다 체크되거나, 부분취소 시 의도보다 많은 의뢰서가 선택될 위험) 안전 문제로 판단 — `거래명세서번호::의뢰서번호` 복합키(`레벨2키()`)로 교체
  - 검증: `python -m py_compile`(수정 파일 전체), `combine_거래명세서_시트들()` 단위 테스트(3개 파일 병합 → openpyxl로 시트 3개·시트명 중복 접미사·Print_Area·도장 이미지 2개 정상 확인), 로컬 MariaDB 종단 테스트(임시 테스트 거래처·의뢰서 2건으로 실제 2조 분할 발급 → 같은 묶음번호 공유·의뢰서 전체 공유 확인 → 통합엑셀 다운로드 시 시트 2개 정상 확인, 테스트 데이터 정리), `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과
  - **라이브 브라우저 검증**: 처음엔 격리 환경 구성이 어려워(3000번 포트 개발서버가 사무실 PC를 바라보고 있고, Next.js가 같은 프로젝트 폴더에서 개발서버 중복 실행을 막음) 생략하려 했으나, 사무실 PC API 서버가 다시 연결되면서 사용자가 실제 그 화면(사무실 PC 백엔드는 아직 이전 코드 — 프론트엔드만 새 코드)에서 직접 조건 규칙 추가를 테스트
  - **실사용 중 발견한 버그**: 조건 규칙을 추가해도 오른쪽 "고객사 청구 명세서" 표에 그 행이 안 보이는 문제 제보(원본 53건 중 51건이 "어느 규칙에도 안 걸린 항목" 박스에 나열되는 상황) — **원인**: 안 걸린 항목이 많을 때(51줄) 그 목록 박스가 세로 공간을 다 차지해버려, 정작 규칙이 반영된 표 영역이 화면 밖으로 밀려나는 flex 레이아웃 문제(표 컨테이너에 `flex-1`이 없어 남은 공간을 확보하지 못하고 있었음). 왼쪽 원본 표에 이미 ✓ 체크로 매칭 여부를 보여주고 있어(2026-07-24 기능) 이 목록 자체가 중복 정보라는 사용자 피드백에 따라 **미분류 목록 박스를 완전히 제거**하고, 좌/우 표 컨테이너 모두에 `min-h-0 flex-1`을 추가해 항상 남은 공간을 온전히 차지하도록 수정(체크 판정용 내부 계산 로직 자체는 그대로 유지, 화면 표시만 제거)
  - 수정 후 사용자가 같은 화면에서 새로고침 → 재테스트해 정상 동작(규칙 행이 표에 정상 표시됨) 확인. `npx tsc --noEmit`·`npx eslint .`·`npm run build` 재통과
  - **사무실 PC 배포 완료(2026-07-29)** — `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` 3개 파일 반영 + `python scripts/init_db_mariadb.py` 마이그레이션(`청구품목규칙.조`·`거래명세서.묶음번호`·`시트명` 컬럼 추가) + FastAPI 재시작까지 사용자가 직접 완료. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음
- 배포 후 실사용 중 발견한 취소 처리 중복 버그 수정 ✅ (사용자 제보: 발행요청목록에 같은 의뢰서번호가 여러 건으로 보여 전부 취소했더니, 미발행목록에 그 의뢰서번호가 중복으로 나타나고 화면이 느려짐 — 의뢰서 97101·97100 등)
  - **원인:** `Tab4IssuedList.tsx`의 `executeCancel()`이 취소 성공 후 화면 상태를 되돌릴 때 **의뢰서번호만으로** 판단하고 있었음 — 조별 분할발급 설계상 같은 의뢰서번호가 서로 다른 거래명세서번호에 동시에 걸릴 수 있는데(모든 조가 같은 의뢰서번호_목록을 공유), 사용자가 그 여러 건을 각각 취소하면 `onReturnToUnissued()`가 취소한 횟수만큼 같은 의뢰서를 미발행 목록에 중복으로 밀어넣었음(일부만 취소한 경우엔 반대로, 아직 남아있는 다른 거래명세서의 같은 의뢰서까지 화면에서 사라지는 문제도 함께 있었음)
  - `Tab4IssuedList.tsx`의 `executeCancel()` — "거래명세서번호+의뢰서번호" 복합키(`레벨2키()`, SKILL-30에서 이미 도입)로 정확히 제거하고, 취소 후에도 같은 의뢰서번호가 **다른** 거래명세서번호에 남아있으면 아직 발행 상태이므로 미발행으로 되돌리지 않도록 수정, 완전히 사라진 의뢰서번호만 중복 없이 한 번씩 되돌리도록 변경
  - `Tab4Invoice.tsx`의 `handleConfirmSubmit()`도 같은 종류의 잠재 문제 확인 후 함께 수정 — 분할발급 확정 시 서버가 돌려주는 `거래명세서번호_목록`(그룹 수만큼) 전부를 반영해 의뢰서마다 그 개수만큼 발행행을 만들도록 변경(기존엔 첫 번호 하나로만 태그해, 새로고침 전후로 화면에 보이는 건수가 달라지는 불일치가 있었음 — 분할 안 된 경우는 목록 길이가 1이라 기존과 동일하게 동작)
  - 검증: `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과. 사용자가 로컬 PC 화면(수정된 프론트엔드, 사무실 PC 백엔드 연결)에서 새로고침 후 재확인해 중복·랙 문제 모두 해소됨을 확인
  - **이번 수정은 프론트엔드 전용**(`Tab4IssuedList.tsx`·`Tab4Invoice.tsx`)이라 사무실 PC에 별도로 배포할 것이 없음 — Next.js는 로컬 PC에서 실행되고 사무실 PC는 API 서버(백엔드)만 담당하는 구조라, 로컬 PC에 코드가 반영된 시점에 이미 적용 완료

