# 작업현황 대시보드 생성 — 작업 이력 (CHANGELOG)

> 완료된 작업은 ✅ 로 표시합니다. (to-do·예정 작업은 .claude/plans/ 플랜 파일에 기록)
> 날짜는 YYYY-MM-DD 형식으로 기록합니다.
> 날짜순으로 정렬하며, 새 항목은 파일 맨 아래(최신)에 추가합니다.

---

## ✅ 완료된 작업


> 📦 2026-08-01 이전 이력은 `docs/CHANGELOG_archive.md`로 분리되어 있습니다.

### 2026-08-01
- 거래명세서 "조별 분할발급"을 여러 건 채번 → **거래명세서 1건 + 조별 시트 분할** 방식으로 변경 ✅ (사용자 제보: BC카드 종합법인처럼 조가 은행별로 6개나 되는 실제 청구 규칙에서, 발행요청목록에 번호가 6개씩 생기고 업무명이 섞이면 12줄로 보여 불편함)
  - **DB 스키마**: `거래명세서_품목`에 `조 VARCHAR(50)` 컬럼 신규(`scripts/init_db_mariadb.py`) — 품목 한 줄 한 줄마다 자기 조를 기억해야 확정 시 레코드는 1건이면서도 다운로드 시 조별로 시트를 나눌 수 있음. `거래명세서.묶음번호`·`시트명`은 삭제하지 않고 그대로 유지(예전 방식으로 이미 발행된 건 하위호환용)
  - `scripts/api.py`: `POST /거래명세서요청`에서 조가 2개 이상이면 그룹별로 각각 채번·저장하던 분기를 통째로 제거하고 **항상 거래명세서 1건**만 생성(품목_최종의 각 행에 실린 조는 `거래명세서_품목`에 그대로 저장만 함). 신규 `_거래명세서_엑셀_시트목록()` — `GET /거래명세서엑셀/{no}`가 묶음번호 없는(=새 방식) 건은 이 함수로 저장된 최종 품목을 조 값으로 그룹핑해 시트별 엑셀을 만들고, 묶음번호 있는(=예전 방식) 건은 기존 형제-합치기 로직 그대로 사용(하위호환, 기존 발행 건 전혀 영향 없음)
  - 프론트엔드: `InvoicePreviewDialog.tsx`의 "조 N개 등장" 안내 문구를 "거래명세서가 N건 발급" → "거래명세서 1건이 발급되고 시트 N개짜리 통합 엑셀" 로 수정. `Tab4Invoice.tsx`는 응답이 이미 단일 번호만 반환하므로 기존 폴백 로직(`data.거래명세서번호_목록 ?? [data.거래명세서번호]`)이 그대로 호환돼 별도 수정 불필요
  - 검증: 로컬 MariaDB 종단 테스트 — 조 2개+미지정 1개를 섞은 품목으로 확정 시 거래명세서가 실제로 1건만 생성되는지, 각 품목이 자기 조 값을 정확히 저장하는지, 다운로드 시 시트 3개로 정확히 나뉘는지, 예전 묶음번호 방식(2건)도 여전히 정상적으로 합쳐지는지까지 `openpyxl`로 직접 확인 후 테스트 데이터 정리. `python -m py_compile`·`npx tsc --noEmit`·`npx eslint .` 통과
  - **사무실 PC 배포 완료** — 사용자가 직접 반영 후 실제 화면에서 거래명세서 1건만 생성됨을 확인
- 발행요청목록/발행완료 레벨1 표시를 "거래명세서번호+업무명" → **"거래명세서번호" 단독** 기준으로 변경 ✅ (위 변경으로 거래명세서가 항상 1건이 됐는데도, 화면 그룹핑 기준이 업무명까지 포함하고 있어 번호 1개가 업무명 개수만큼 여러 줄로 쪼개져 보이는 문제가 남아있었음 — 사용자 확인 후 요청)
  - `frontend/lib/issuedGrouping.ts`의 `build레벨1그룹()` — 그룹 키를 `거래명세서번호` 단독으로 변경, `업무명` 필드는 그룹에 등장한 고유 업무명을 담당자처럼 쉼표로 합쳐 표시. `Tab4IssuedList.tsx`의 레벨2 필터 키도 동일하게 조정(레벨2 선택 키인 `레벨2키()`는 원래도 의뢰서번호 기준이라 영향 없음)
  - 검증: `npx tsc --noEmit`·`npx eslint .` 통과. 프론트엔드 전용 변경이라 사무실 PC 배포 불필요(로컬 PC 새로고침만으로 반영)
- 거래명세서 미리보기 "통합조건식"(거래처+업무명 여러 개 조합별 재사용 규칙, `청구품목통합규칙` 테이블) 기능 시도 후 원복 ✅ — 원래 "여러 업무명이 섞인 선택에도 저장된 개별조건식이 정상 적용되도록" 만들었으나(대표 업무명 1개로만 규칙을 조회하던 기존 로직이 다른 업무명에 저장된 규칙을 놓치는 버그가 시작점), 실사용 검증 중 부작용이 여럿 발견됨(병합된 규칙 중 매칭 0건인 것까지 수량0·금액0 "쓰레기 줄"로 노출, 통합조건식이 저장돼 있으면 단일 업무명 요청 자체가 차단되는 등) — 복잡도 대비 실익이 낮다고 판단해 **같은 날 코드 원복**(`scripts/api.py`의 `/거래명세서미리보기`·`/거래명세서요청`을 원래 방식대로 되돌리고 관련 헬퍼·`billing.업무명조합_키()` 삭제, 프론트엔드 `InvoicePreviewDialog.tsx`·`Tab4Invoice.tsx`도 원복). DB의 `청구품목통합규칙` 테이블은 그 사이 저장된 실제 규칙 데이터가 있을 수 있어 삭제하지 않고 남겨둠(코드에서는 더 이상 참조하지 않음) — `scripts/init_db_mariadb.py`도 신규 설치 시 더 이상 이 테이블을 만들지 않도록 수정
  - 이 과정에서 발견해 남긴 개선 하나는 원복하지 않고 유지: `/거래명세서미리보기`가 매칭 0건인 규칙을 결과에서 걸러내는 로직(통합조건식과 무관하게 일반적으로 유용), 그리고 `규칙목록`을 응답에 직접 포함해 프론트가 별도로 `GET /api/billing-rules`를 재조회하지 않도록 한 단순화
  - 상세 배경은 `docs/작업현황-대시보드-생성-KNOWLEDGE.md` 2026-08-01 항목, `.claude/plans/plan_거래명세서_조별분할발급_통합엑셀.md` 상단 안내 참고

### 2026-08-04
- 부가세(VAT) 포함/별도 판정 기준을 "거래처 기본단가 행" → **실제로 청구된 작업명 기준**으로 변경 ✅ (사용자 제보: 첨부한 거래명세서 스크린샷에서 "부가세 포함"이라 라벨링돼 있는데 실제론 10%가 계산·가산돼 있음 — KB국민카드로 재확인해보니 아예 기본단가 행(업무명·작업명 둘 다 빈 행) 자체가 없고 업무명별 예외 단가 행 6건만 있는데 전부 "포함"으로 저장돼 있었음)
  - **원인:** `billing.부가세_계산()`이 딱 `단가맵.get((거래처명, None, None))` 한 곳만 조회 — 기본단가 행이 없는 거래처는 조회가 항상 실패해 기본값 "별도"로 계산되고 있었음(실제로는 예외 행 전부 "포함"이었는데도)
  - **사용자 확정 설계:** 거래명세서에 실제로 청구되는 각 작업명이 매칭되는 단가마스터 행의 부가세구분을 보고, 전부 같으면 그 값으로 계산 — **포함/별도가 섞여 있으면 발급 자체를 차단**하고 단가마스터를 통일하도록 안내(DB 스키마 변경 없음, 필요한 데이터는 기존 값에서 전부 재도출)
  - `scripts/billing.py`: `부가세_계산(거래처명, 단가맵, 공급가액)` → 역할 분리해 `결정_부가세구분(부가세구분맵)`(작업명별 값이 섞이면 ValueError)과 `부가세_계산(부가세구분, 공급가액)`(이미 결정된 값으로 계산만) 2개로 교체. `build_품목행()`·`calc_공급가맵()`이 이미 갖고 있던 (거래처,업무,작업) 3단계 폴백 조회 루프에서 매칭된 각 작업명의 부가세구분을 함께 모아 반환(`부가세구분맵` 추가 반환값)
  - `scripts/api.py`: `POST /거래명세서미리보기`가 혼재 시 `부가세구분: null` + `부가세오류` 메시지를 내려줌(발급은 막되 미리보기는 계속 보여줌). `POST /거래명세서요청`은 `품목_최종` 유무와 무관하게 항상 원본을 재계산해 검증하도록 넓혀 혼재 시 400으로 발급 자체를 차단(예전엔 `품목_최종`이 없으면 이 검증 자체를 건너뛰고 있었음). `_거래명세서_엑셀_시트목록()`의 조별 재계산은 "최종" 품목엔 작업명이 없어(정렬행_원본목록 특성상) 대신 같이 저장된 "원본" 품목의 작업명으로 인보이스 전체 기준 한 번만 판정 후 조 그룹마다 동일 적용. `POST /거래명세서부분취소`도 남는 의뢰서들의 부가세구분을 합쳐 검증, 혼재 시 400. `GET /예상공급가액`(미발행 목록 예상치)은 아직 발급 전이라 혼재여도 막지 않고 "별도"로 예상 표시만
  - `scripts/app.py`(Streamlit): "거래명세서 요청" 버튼·부분취소(`_취소_변경계획()`/`_부분취소_dialog()`)도 동일하게 API와 같은 신호(혼재 시 경고 후 중단)로 반영
  - 프론트엔드: `InvoicePreviewDialog.tsx`(`부가세구분`이 null일 수 있는 타입으로 변경, 혼재 시 빨간 경고 배너 + "확정" 버튼 비활성화), `Tab4Invoice.tsx`(이중 안전장치로 제출 직전에도 한 번 더 체크)
  - **범위 밖:** 단가관리 화면의 부가세 선택 UI(모든 단가 행에 개별 설정 가능)는 이번 설계가 그대로 인정하는 방향이라 변경 불필요. 엑셀 템플릿의 "부가세 포함" 고정 라벨(N31 셀 문구)도 사용자 확인대로 그대로 유지
  - 검증: `python -m py_compile scripts/billing.py scripts/api.py scripts/app.py`, `npx tsc --noEmit`·`npx eslint .` 통과. 로컬 MariaDB의 실제 KB국민카드 데이터(단가마스터 6건, 실제 의뢰서 93429 — 화면에서 본 것과 동일한 작업명 4종 포함)로 직접 테스트 — 전부 "포함"인 지금 상태는 세액 0으로 정확히 계산되고, 하나만 "별도"로 바꾸면 `결정_부가세구분()`이 정확히 차단함을 확인
  - **사무실 PC 배포 완료** — `scripts/billing.py`·`scripts/api.py` 반영 + FastAPI 재시작까지 사용자가 직접 완료(`app.py`는 사무실 PC에서 안 쓰여 배포 불필요). 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음

### 2026-08-08
- 거래명세서 청구 규칙 — **다중 업무명 규칙조회 버그를 "통합조건식" 기능으로 재설계·구현** ✅ (`.claude/plans/plan_거래명세서_다중업무명_규칙조회.md`에 남겨뒀던 8/1 원복 이력을 이어받아, 이번엔 계획 승인 후 처음부터 다시 설계 — **로컬 PC 반영·검증까지 완료, 사무실 PC 미배포**)
  - **버그 재확인:** 미발행 목록에서 서로 다른 업무명을 여러 개 함께 체크해 거래명세서를 요청하면, `billing.build_품목행()`이 여러 업무명 중 첫 번째로 등장한 것 하나만 "대표 업무명"으로 골라(데이터 순서 의존, 비결정적) 그 하나로만 청구품목규칙을 조회·저장 — 다른 업무명에 저장된 규칙이 조회·저장 양쪽에서 전부 누락되던 버그
  - **사용자 확정 설계 (8/1 시도와의 차이점):** "거래처+업무명 조합"별 재사용 규칙(통합조건식)을 만들되, 8/1처럼 조합마다 미리 다 등록해둬야 하는 방식이 아니라 **선택된 업무명이 1개면 개별조건식, 2개 이상이면 통합조건식**에 저장하고, 통합조건식이 없으면 개별조건식을 자동 병합해서 보여줌(폴백). 저장된 통합조건식의 업무명조합이 지금 선택과 정확히 다르면(부족: 등록된 업무명 일부가 빠짐 / 초과: 지금 선택이 더 많음) 화면이 확정을 막고 사용자에게 "제외/추가하고 확정"을 선택하게 한 뒤, 그 선택대로 **기존 통합조건식을 UPDATE로 재조정**(레코드 삭제가 아니라 소속 업무명조합만 교체) — 1개까지 줄어든 통합조건식도 그대로 허용(개별조건식으로 자동 전환 안 함)
  - **DB:** 사무실 PC에 8/1 시도 때 만들어졌다가 코드에서만 참조가 끊겼던 `청구품목통합규칙` 테이블(id·거래처명·업무명조합·순서·최종청구품명·조건·조·등록일·수정일)을 `scripts/init_db_mariadb.py`에 정식 `CREATE TABLE`로 복원. 그 테이블에 남아있던 실 데이터(BC카드 관련 48행)는 사용자 확인 결과 **8/1 기능 테스트 중 남은 leftover**(실사용 발행에 쓰인 정식 데이터 아님)라 재사용하지 않고, 사무실 PC 배포 시 정리(삭제) 후 빈 테이블로 새로 시작하기로 확정(로컬 PC는 처음부터 빈 테이블)
  - `scripts/billing.py`: 신규 `업무명조합_키(업무명_목록)`(dedupe+정렬해 파이프 결합, 선택 순서 무관하게 항상 같은 문자열). `build_품목행()`이 대표 업무명 1개 대신 **`업무명_목록`(정렬된 리스트)**을 반환하도록 변경(반환 튜플 4번째 자리, 호출부 `generate_거래명세서_excel()`은 Excel 표시용으로 `", ".join()`해서 사용 — 저장/조회 키로는 안 씀)
  - `scripts/api.py`: `_규칙_조회`/`_규칙_저장` 옆에 통합조건식 전용 헬퍼 5개 신규(`_통합규칙_조회`·`_통합규칙_저장`·`_통합규칙_조합목록`·`_통합규칙_업무명조합_수정`·`_개별규칙_병합조회`) + 판정 함수 `_통합조건식_판정()`(선택된 업무명 집합 기준 정확일치/부족·초과/무관 3가지로 분류, 부족·초과 후보가 여럿 겹치는 드문 경우엔 차이가 가장 적은 것 우선·동률이면 축소보다 확장 우선). `POST /거래명세서미리보기`가 `업무명_목록`을 받아(선택사항, 서버 재계산값과 다르면 400) 응답에 `업무명_목록`·`규칙출처`·`업무명조합_사용중`·`통합조건식_불일치` 추가, 거래처명 혼합도 사업부처럼 400 방어 추가. `POST /거래명세서요청`은 `업무명`(단수) 필드를 `업무명_목록`으로 완전히 교체하고 `업무명조합_사용중`·`통합조건식_해결` 추가, 저장 분기를 업무명 개수·해결 여부에 따라 개별/통합 저장으로 나눔(신규업무명조합은 항상 서버가 `billing.업무명조합_키()`로 재계산 — 프론트가 직접 조립하지 않아 정규화 어긋날 위험 없음). 통합조건식_해결 처리 중 드물게 나는 `IntegrityError`(목표 조합이 이미 별도로 존재)는 500 대신 409로 안내
  - 프론트엔드: `InvoicePreviewDialog.tsx`(`미리보기결과.업무명`→`업무명_목록` 등 타입 변경, 부족/초과 상황별 안내 배너+선택 버튼, 확정 버튼에 `통합조건식_불일치 && !resolution` 비활성 조건 추가), `Tab4Invoice.tsx`(미리보기 요청에 `업무명_목록` 추가 + 거래처명 혼합 방어, 확정 payload를 `업무명_목록`/`업무명조합_사용중`/`통합조건식_해결` 기준으로 교체 — 예전엔 `previewData?.업무명`(대표 1개)만 저장 키로 썼던 게 저장 버그 지점이었음)
  - 검증: `python -m py_compile`(수정 파일 전체), `npx tsc --noEmit`·`npx eslint .` 통과. 로컬 MariaDB에 신한카드 실데이터로 `build_품목행()` 다중 업무명 반환을 직접 확인 + 임시 테스트 거래처 5개로 판정/저장/UPDATE 로직 19개 항목 단위 검증(정확일치·부족·초과·1개짜리 통합조건식 허용·IntegrityError 방어 등, 전부 통과) — 검증 후 임시 데이터·임시 API 서버 전부 정리. **브라우저 클릭 테스트는 이번 세션 환경에 Playwright 등 브라우저 자동화 도구가 없어 수행하지 못함** — 사무실 PC 배포 전 사용자가 직접 화면에서 확인 필요(체크리스트는 작업 완료 보고 시 별도 안내)
  - `docs/API규격서.md`의 `POST /거래명세서미리보기`·`POST /거래명세서요청` 섹션 요청/응답 예시 갱신
  - **사무실 PC 미배포 — 배포 시 필요한 절차(사용자 재확인 필요):** ① `SHOW CREATE TABLE 청구품목통합규칙;`로 실제 제약조건이 로컬 설계와 일치하는지 최종 대조 → ② 기존 48행 leftover 데이터 삭제 → ③ `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` 배포 + 마이그레이션 실행(스키마만 반영, 데이터는 비운 채로 유지) + FastAPI 재시작 → ④ 빈 상태에서부터 실사용 검증 시작

### 2026-08-09
- 통합조건식 실사용 버그 수정 ✅ (사용자가 로컬 PC에서 직접 브라우저로 검증하다 발견 — 2026-08-08에 "브라우저 클릭 테스트 미수행"으로 남겨뒀던 부분을 이번에 실제로 진행함): 업무명 **1개만** 선택했을 때 `api._통합조건식_판정()`이 `len(S) < 2`면 곧바로 "개별"을 반환해버려, 기존에 등록된 통합조건식이 있어도(예: BC카드 "교육청(전기료) 이용대금명세서"가 원래 "종합 법인 이용대금명세서"와 묶인 통합조건식의 일부인 경우) 부족 안내 자체가 안 뜨는 문제였음. "저장은 1개면 개별조건식"이라는 규칙과 "조회 시 관련 통합조건식이 있으면 부족/초과를 알려준다"는 규칙을 같은 걸로 취급한 게 원인 — `len(S) < 2` 조기 반환을 제거해 1개를 선택해도 기존 통합조건식과의 부족/초과 비교를 그대로 수행하도록 수정(1개짜리 통합조건식과의 정확 일치도 정상적으로 잡힘). `scripts/api.py`만 수정, 아직 사무실 PC 미배포(위 2026-08-08 항목과 함께 배포 예정)
- 거래명세서 미리보기 화면 UI 개선 다수 (전부 `frontend/components/InvoicePreviewDialog.tsx` 등 프론트엔드 전용 — **사무실 PC 배포 대상 아님**, 로컬 PC 반영만으로 끝):
  - **"전체 합산(조건 없음)" 규칙 저장 시 겹침 확인 오탐 수정**: 조건 그룹을 하나도 안 만든 규칙(선택 안 된 나머지 전부를 받는 용도)은 정의상 항상 다른 모든 규칙과 "조건상 겹치는" 게 정상인데, 겹침 확인 기능이 이 경우도 매번 경고를 띄우던 걸 수정(`handleModalSave()`에서 `조건.or.length===0`이면 겹침 확인 자체를 건너뜀)
  - **겹침 확인창 문구 순화**: "다른 규칙과 겹치는 항목이 있습니다 / 그래도 저장하시겠습니까?"(오류처럼 보인다는 피드백) → "겹치는 항목 안내 / 먼저 등록된 규칙이 우선 적용되므로 나머지만 가져갑니다"로 안내 톤으로 변경, 버튼도 "조건 다시 보기"/"계속 진행"으로 순화
  - **오른쪽 표 컬럼 폭 조정 + 천단위 콤마**: "작업구분" 칸이 너무 좁아 긴 이름이 잘려 보이던 것을 넓히고(80px→144px), "단가" 칸은 반대로 좁힘(96px→64px, 실제 필요한 자릿수 기준). 수량·금액 칸에 왼쪽 원본 표처럼 천단위 콤마 표시 추가(`type="number"`→`type="text"`+표시/파싱 헬퍼, 편집은 그대로 가능)
  - **"조" 필드를 화면에 "작업구분"으로 표기**: 다운로드 시 엑셀 시트명으로 쓰인다는 실제 용도를 더 잘 드러내는 이름으로 화면 라벨만 변경(표 헤더·그룹 제목·안내 문구·조건 편집창 라벨) — 내부 변수명·DB 컬럼명·API 필드명은 그대로 `조` 유지(화면 표시만 바꾼 것이라 백엔드 영향 없음)
  - **행 복사 / 작업구분(그룹) 전체 복사 신규**: 조건식 행 옆에 "복사" 버튼 추가(복사본은 원본과 조건이 같아 자동으로 편집창이 열림, 저장 시 겹침 확인 재사용, 편집창에서 취소하면 복사 자체가 취소됨). 작업구분이 여러 항목을 묶고 있을 때는 그 작업구분 전체(여러 규칙)를 한 번에 복사하는 "작업구분 복사" 버튼도 추가(새 작업구분 이름은 "{원래이름}-복사"로 자동 부여, 각 항목은 개별적으로 조건 조정)
  - **작업구분이 1개뿐이어도 그룹 제목·복사 버튼이 보이도록 표시 조건 변경**: 원래는 작업구분이 2개 이상 등장해야만 그룹 표시가 나타났는데(1개짜리는 플랫 목록), "작업구분 복사"를 쓰려면 애초에 1개짜리 상태에서도 그룹 제목이 보여야 하므로 조건을 분리(`그룹표시`는 조가 1개라도 붙어있으면 활성화, 다운로드 시 "시트 N개짜리 통합 엑셀" 안내 문구는 기존대로 2개 이상일 때만)
  - **원본 항목 건수 요약 표기**: 왼쪽 "시스템 자동계산 원본" 제목 옆에 "총 N건 | 선택 N건 | 미선택 N건" 추가
  - **조건식 저장 시 다른 규칙과 겹치는 항목 확인창 신규**: 새/수정 조건이 이미 다른 규칙이 가져간 항목과 겹치면(먼저 등록된 규칙이 우선이라 실제로는 못 가져감) 저장 전에 어느 규칙과 몇 건 겹치는지 알려주고 계속할지 확인받음
- 거래처 마스터 화면 개선 2건:
  - **"신규 거래처 추가" 거래처명 입력을 자동완성 콤보박스로 변경** (`ClientFormDialog.tsx`, 단가관리의 업무명·작업명 입력과 동일한 `EditableCombo` 재사용) — 실제 운영통계자료에 등장하지만 아직 거래처마스터에 미등록인 이름만 후보로 보여줘 오타 방지, 직접 타이핑도 그대로 가능
  - **신규 거래처 등록이 단가관리 화면에 안 보이던 버그 수정**: "거래처관리"·"단가관리" 두 하위 탭이 거래처 목록을 각자 로컬 사본으로 따로 들고 있어서 생긴 문제 — `ClientMaster.tsx`가 자체 로컬 상태 대신 부모(`ClientMasterSection.tsx`)가 소유한 `rows`/`setRows`를 그대로 쓰도록 변경(Tab4Invoice.tsx의 rows/setRows 패턴과 동일)
- **다중 사용자 실시간 반영(신규)**: 여러 담당자가 각자 다른 PC에서 동시에 쓴다는 점 확인 후, 같은 화면 안 상태 공유만으로는 "다른 PC에서 등록한 것"까지는 못 잡는다는 한계를 보완 — 아래 3개 최상위 탭을 "다시 클릭"할 때마다 서버에서 최신 데이터를 조용히 재조회(실패해도 기존 화면 유지, 사용자에게 오류 표시 안 함):
  - "거래명세서 관리" 탭 → `GET /미발행목록` 재조회(다른 탭에서 등록한 단가가 반영된 예상공급가액 실시간 반영)
  - "거래처 마스터" 탭 → `GET /거래처마스터`+`GET /단가마스터` 재조회
  - 신규 Next.js API 라우트 3개(`app/api/invoice-list`·`client-list`·`pricing-list`, 전부 SKILL-15 패턴의 단순 FastAPI 프록시), `Dashboard.tsx`가 각 최상위 탭의 `active`(지금 보이는 중인지) 상태를 하위로 전달해 "비활성→활성 전환" 시점에만 재조회하도록 함(`Tab4.tsx`·`ClientMasterSection.tsx`에 각각 구현)
  - `app/page.tsx`(최초 로그인 직후 서버 컴포넌트 로딩)와 신규 API 라우트가 FastAPI 원본 JSON을 프론트 타입으로 바꾸는 로직이 중복되던 걸 `frontend/lib/serverMappers.ts` 공용 모듈로 정리(필드 추가 시 한 곳만 고치면 되도록, SKILL-27과 같은 종류의 문제 예방)
  - **전부 프론트엔드 전용 — 사무실 PC 배포 대상 아님**(FastAPI는 이미 있는 GET 엔드포인트를 그대로 호출할 뿐 백엔드 변경 없음)
- 미발행 목록 "선택 유지" 이후 신규 선택분 별도 통계표 (`Tab4Invoice.tsx`·`InvoiceSelectionSummaryBar.tsx`, 프론트엔드 전용): 필터 변경 후 "선택 유지"를 누르면 그 시점을 기준점으로 저장해두고, 이후 새로 체크하는 항목만 별도로 집계 — 기존 "선택 N건" 통계표 옆(좁으면 자동 줄바꿈)에 "새로 선택 N건" 통계표가 추가로 나타남. "선택 해제"·거래명세서 요청 완료 시 기준점 초기화
- 검증: 이번 세션 변경분 전체 `npx tsc --noEmit`·`npx eslint .` 통과. 브라우저 클릭 테스트는 **사용자가 로컬 PC에서 직접 진행**(이 환경엔 브라우저 자동화 도구가 없어 Claude가 직접 수행 불가) — 그 과정에서 위 "통합조건식 실사용 버그"를 실제로 발견해 그 자리에서 수정함
- **사무실 PC 배포 필요 항목**: `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` (2026-08-08 통합조건식 전체 + 오늘 수정한 `len(S)<2` 버그 포함) — 절차는 위 2026-08-08 항목의 "사무실 PC 미배포" 문단 그대로. **그 외 오늘 작업(UI 개선·거래처명 콤보박스·다중 사용자 실시간 반영·선택 유지 통계표)은 전부 프론트엔드 전용이라 사무실 PC에 배포할 파일 없음** — 로컬 PC Next.js는 이미 반영된 상태(재시작만 하면 적용)

### 2026-08-10
- **다중업무명 규칙조회(통합조건식) 코드를 사무실 PC에 배포 완료** ✅: `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` 사무실 PC로 복사 + FastAPI 재시작(2026-08-08·2026-08-09 로컬 PC 구현분 전체 반영)
  - 배포 직후 프론트에서 `Cannot read properties of undefined (reading 'join')` 오류 발생 — 새 코드의 결함이 아니라 파일이 실제로는 갱신 전 상태로 남아있던 것이 원인, 사용자가 재확인 후 파일을 다시 올려 해결
  - `docs/API규격서.md` 최상단 "최종 갱신" 날짜를 2026-07-20 → 2026-08-08로 수정(본문 내용은 2026-08-08 통합조건식 재설계분까지 이미 반영돼 있었으나 날짜 표기만 누락돼 있었음)
  - 로컬 PC에서 실사용 중 만든 통합조건식(46행)·개별조건식(8행) 이관용 SQL 생성 완료(`output/청구규칙_로컬데이터_이관_2026-08-09.sql`)
- **로컬 PC 실사용 규칙 데이터를 사무실 PC로 이관 완료 + 실제 화면 테스트까지 완료** ✅: 사무실 PC `청구품목통합규칙`의 8/1 leftover 정리 후 위 SQL 실행, 사무실 PC 브라우저에서 아래 4가지 시나리오 전부 정상 동작 확인
  - 업무명 여러 개 선택 → 통합조건식 정상 적용
  - 부족/초과 상황(선택이 등록된 조합과 다름) → 확인창이 뜨고 "제외/추가하고 확정" 처리 정상
  - 업무명 1개 선택 → 개별조건식 정상 적용
  - 거래명세서 발행 → 엑셀 다운로드까지 끝까지 정상
  - **2026-08-08에 착수한 "다중업무명 규칙조회(통합조건식)" 기능 — 코드·데이터 전부 사무실 PC 반영 및 실사용 검증 완료로 종료**

### 2026-08-11~12
- **거래명세서 Excel "구분·규격·비고" 조건식 입력 + 담당자 연락처 자동화 구현 완료** ✅ (로컬 PC만, 사무실 PC 미배포 — `.claude/plans/plan_거래명세서_구분규격비고_담당자.md` 상세)
  - 사용자가 실제 발행된 Excel을 보여주며 요청: ①청구품목규칙 조건식에서 "최종청구품명"뿐 아니라 **구분·규격·비고**도 입력해 Excel B/H/N열에 표기 ②하단 담당자 연락처(지금은 템플릿 고정 텍스트)를 거래처+업무명별로 등록해두면 자동 표기
  - 템플릿(`data/거래명세서_템플릿_base.xlsx`)을 직접 열어 확인한 결과 15행 헤더에 이미 "규 격"(H열)·"비 고"(N열)가 있었으나 코드가 안 쓰고 있었음 — 신규 셀 추가 없이 그대로 재사용. `거래명세서_품목.구분`(ENUM 원본/최종)과 이름이 겹쳐 신규 필드명은 **`구분표시`**로 명명(컬럼명 충돌 회피)
  - DB: `청구품목규칙`·`청구품목통합규칙`·`거래명세서_품목`에 `구분표시`·`규격`·`비고` 3컬럼씩 추가 + **담당자**·**담당자_담당거래처** 신규 테이블 2개("담당자 우선" 구조 — 담당자 1명 밑에 거래처+업무명을 여러 개 등록, 담당자 정보 수정은 한 곳에서만 하면 연결된 모든 거래처에 자동 반영)
  - `scripts/billing.py`(`적용_규칙()`·`write_거래명세서_excel()` 확장) · `scripts/api.py`(규칙 조회/저장/미리보기/확정저장/재다운로드 전 구간 반영 + `_담당자_조회()` 2단계 폴백 + 담당자 CRUD 엔드포인트 6개) · 프론트(`ConditionRuleModal.tsx`·`InvoicePreviewDialog.tsx` 확장 + `StaffMaster.tsx` 신규 "담당자관리" 탭) 전부 구현
  - 검증: `py_compile`·`tsc`·`eslint`·`npm run build` 통과, 로컬 MariaDB 종단 테스트(Excel 셀 값 직접 확인, 담당자 폴백 순서 등) 통과, **사용자가 로컬 PC 실제 브라우저(KB증권 거래원장)로 확인 완료**
  - 실사용 확인 중 이어서 발견·요청된 후속 개선 5건도 같은 세션에서 함께 구현·검증 완료(전부 `.claude/plans/plan_거래명세서_구분규격비고_담당자.md`의 "후속 작업" 절에 상세 기록):
    1. 미리보기 화면 가독성 개선(오른쪽 표 컬럼 3개 추가로 생긴 가로 스크롤 제거, 좌우 표 비율 40:60 조정, 조건식 편집창 폭 확대)
    2. "새 행 추가" 품명 자동완성 1차 구현
    3. **버그 수정**: 거래명세서 취소 시 `거래명세서_품목`이 FK CASCADE로 삭제되며 품명 이력도 함께 사라지던 문제 — 별도 영속 테이블 `거래명세서품명이력` 신설(취소 API가 손대지 않음)
    4. 체크박스 다중 선택 UI → "선택 없이 전월 자동 반영" 방식으로 1차 재설계(+ 조건식 품명이 이력에 안 섞이도록 `수동입력` 플래그 추가)
    5. "전월(달력 기준)" → **"그 거래처의 가장 최근 확정분(취소돼도 무관) 기준"**으로 최종 재확정 — 확정 시 사용된 품명들의 저장 시각이 함께 갱신된다는 성질을 이용해 DB 스키마 변경 없이 쿼리만 수정
  - **사무실 PC 배포 필요**: `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` + 마이그레이션(신규 컬럼 5개·테이블 3개: 담당자·담당자_담당거래처·거래명세서품명이력) — 아직 미착수, 별도 승인 후 진행 예정

### 2026-08-12
- **거래명세서 "구분·규격·비고" + 담당자 연락처 자동화 — 사무실 PC 배포 완료** ✅: 2026-08-11~12 로컬 PC 구현분(`scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py`)을 사무실 PC에 반영하고 `python scripts/init_db_mariadb.py` 마이그레이션 실행(신규 컬럼 5개: 청구품목규칙·청구품목통합규칙·거래명세서_품목의 `구분표시`·`규격`·`비고`, 신규 테이블 3개: `담당자`·`담당자_담당거래처`·`거래명세서품명이력`) + FastAPI 재시작까지 사용자가 직접 완료. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음
- **작업구분(조) 2개 이상일 때 맨 앞에 "통합 명세서" 시트 추가 — 구현·검증 완료(로컬 PC만, 사무실 PC 미배포)** ✅ (`.claude/plans/plan_거래명세서_통합요약시트.md`에 남겨뒀던 요청 착수): 전체 조의 품목을 표시품명 기준으로 합쳐(합집합 + 동일 품명 수량·금액 합산) 맨 앞 시트로 끼워 넣음. 시트명·상단 업무명(B12)은 사용자가 직접 입력하고, 같은 거래처+업무명조합으로 다음에 다시 만들 때 마지막 입력값을 자동 불러옴(신규 테이블 `통합시트설정`, `거래명세서.통합시트명`·`통합상단업무명` 컬럼). `scripts/billing.py`(`병합_통합품목행()`)·`scripts/api.py`(`GET /통합시트기본값` 신규, `_거래명세서_엑셀_시트목록()` 확장)·`InvoicePreviewDialog.tsx`(입력칸 2개) 구현, 로컬 MariaDB 종단 테스트(3개 조·품목명 중복 섞은 케이스로 병합·순서·값 검증) + `tsc`·`eslint`·`build` 통과
- **거래명세서 시트명 규칙 변경 + 발행완료 다운로드 UI 개선 — 구현·검증 완료(로컬 PC만)** ✅: 시트명을 "작업구분(조) 우선, 없으면 거래처명(업무명)"으로 통일(단일 시트 경로도 포함 — `billing._rename_single_sheet()` 신규, 표지 시트는 유지한 채 데이터 시트 탭 이름만 교체). 발행완료 탭의 "다운로드" 컬럼을 맨 앞(선택 다음)으로 이동 + 한글 텍스트 대신 인라인 SVG 아이콘으로 교체
- **부가세(VAT) "포함" 거래처 표시 방식 변경 — 구현·검증 완료(로컬 PC만)** ✅ (사용자 제보: 교보생명보험주식회사 스크린샷 — "포함"인데 부가세 칸이 0원으로만 보여 얼마가 포함돼 있는지 알 수 없었음): 미리보기 편집창(좌·우 표)과 **실제 발행되는 Excel 둘 다** "포함"이어도 총액을 역산해 공급가액·부가세 두 칸으로 실제 분리 표기(부가세=총액÷11, 공급가액=총액-부가세) — 합계(실제 청구 총액)는 그대로 유지, 표기만 분리. `scripts/billing.py`에 표시 전용 함수 `부가세_표시분리()` 신규 추가(기존 `부가세_계산()`은 손대지 않아 다른 화면·저장 로직 영향 없음), `generate_거래명세서_excel()`·`_거래명세서_엑셀_시트목록()`(조별·통합 시트 둘 다) 적용. 조사 과정에서 교보생명보험주식회사의 "동봉물삽입비"(추가동봉물) 항목이 사라진 별도 제보를 함께 확인 — 원인은 코드 버그가 아니라 사용자가 단가를 "추가봉입단가"(장수-건수 차이로 자동 계산)에서 "동봉물삽입단가"(실제 "삽지" 자재 사용 기록 필요)로 옮기면서 그 거래처엔 애초에 삽지 자재 기록이 전혀 없어(전체 247건 이력 확인) 수량이 항상 0이 된 것으로 확인(코드 변경 없음, 사용자 판단 대기)
- **발행대기(발행요청목록)에서도 다운로드 허용 + "발행가능" 승인 게이트 신규 — 구현·검증 완료(로컬 PC만)** ✅ (마케팅 직원 요청: 거래처 승인을 받고서야 발행하는 경우가 있어, 승인용으로 발행 전에도 실제 서식 그대로의 파일이 필요함): `GET /거래명세서엑셀/{no}`가 원래 발송여부와 무관하게 동작하는 걸 확인하고 발행요청목록 탭에도 다운로드 아이콘 노출(순수 프론트 변경). 신규 `거래명세서.발행가능`(기본값 1) 컬럼 + `PUT /거래명세서/{no}/발행가능` — 거래처 승인이 필요한 건은 마케팅 담당자가 꺼두고, `POST /거래명세서발행`이 409로 차단(여러 건을 순차 처리하며 실패만 모아 안내하는 기존 패턴에 자연스럽게 편입돼 그 건만 자동 제외되고 나머지는 정상 발행). 화면 설계는 사용자 피드백으로 두 차례 조정: (체크박스 disabled 시도 → "취소"까지 막혀버려 되돌림) → 최종적으로 **체크박스는 항상 활성 상태 유지, 선택에 승인대기 건이 섞이면 "거래명세서 발행" 버튼만 비활성화**하는 방식으로 확정. 확정 성공 배너의 다운로드 버튼(1차 구현)도 "발행요청목록 탭으로 자동 이동"으로 대체(2차 확정) — 확정하면 곧바로 그 탭으로 전환되어 다운로드 아이콘을 바로 쓸 수 있음
- **"새 행 추가" 자동 반영에 작업구분(조)도 포함 — 구현·검증 완료(로컬 PC만)** ✅ (기존엔 품명만 최근 확정분 기준으로 자동 복원하고 조는 매번 새로 입력해야 했음): `거래명세서품명이력`에 `조` 컬럼 추가, 확정 시 품명별 조 값도 함께 upsert(재확정 시 최신 값으로 갱신). 수량·단가 등 나머지는 기존대로 매번 새로 입력(범위 그대로 유지)
- **위 5건(통합 명세서 시트·시트명 규칙·부가세 포함 표시분리·발행가능 승인 게이트·새행추가 조 포함) 사무실 PC 배포 완료** ✅ (2026-08-12, 같은 날): `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` 반영 + 마이그레이션 실행(신규 컬럼: `거래명세서.통합시트명`·`통합상단업무명`·`발행가능`, `거래명세서품명이력.조`, 신규 테이블 `통합시트설정`) + FastAPI 재시작 + 실제 화면 확인까지 사용자가 직접 완료. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음
- **상단 GNB(전역 내비게이션) 2단 구조 개편 — 구현·검증 완료(로컬 PC만, 프론트엔드 전용, 사무실 PC 배포 대상 아님)** ✅ (사용자 요청: 최상위 탭 5개가 한 줄에 나열된 게 불편하니, 성격이 같은 메뉴끼리 대분류로 묶고 클릭 시 하위탭이 나타나는 2단 구조로 개편해달라는 요청 — 왼쪽 세로 사이드바 안도 함께 검토했으나 각 화면 왼쪽에 이미 검색 필터 패널이 있어 자리가 겹쳐 제외, 상단 가로 2단 탭 바로 확정)
  - 최상위 탭 5개(작업 현황 요약·거래처별 현황·담당자별 현황·거래명세서 관리·거래처 마스터)를 대분류 3개(통계 분석·거래명세서 관리·거래처 마스터)로 재편 — "통계 분석"은 원래 최상위 탭 3개를, 나머지 둘은 각 화면 안쪽에 이미 있던 자체 하위탭(거래명세서 관리: 미발행목록/발행요청목록/발행완료, 거래처 마스터: 거래처관리/단가관리/담당자관리)을 상단 2번째 줄로 그대로 끌어올려 세 그룹 전부 "대분류 1줄+하위탭 1줄" 동일한 모양으로 통일
  - `frontend/components/Dashboard.tsx`: 평평했던 `tab` state 하나를 `group`(대분류)+`statsSub`/`invoiceSub`/`masterSub`(그룹별 하위탭) 4개로 재설계 — 그룹마다 마지막으로 보던 하위탭을 독립적으로 기억(예: "거래명세서 관리"에서 "발행완료"를 보다가 "통계 분석"에 갔다 와도 "발행완료"가 그대로 유지). `GROUPS` 메타데이터 배열로 1줄·2줄 버튼을 렌더링, 콘텐츠는 기존과 동일하게 전부 상시 마운트+CSS(`hidden`)로 숨기는 방식 그대로 유지(필터 상태 보존 목적)
  - `frontend/components/Tab4.tsx`·`ClientMasterSection.tsx`: 각자 갖고 있던 자체 `useState<SubTabId>`+`<nav>` 렌더링을 제거하고, `subTab`(과 Tab4는 `setSubTab`도 — 거래명세서 확정 직후 "발행요청목록"으로 자동 이동하는 기존 동작 유지에 필요)을 Dashboard.tsx가 내려주는 controlled prop으로 전환. 재진입 시 서버 데이터 재조회(SKILL-34, `active` prop 기반)는 이번 변경과 무관해 그대로 동작
  - 검증: `npx tsc --noEmit`·`npx eslint .`·`npm run build` 전부 통과. **브라우저 클릭 테스트는 이번 세션 환경에 로그인 자격 증명이 없어 Claude는 미수행**(개발 서버가 사무실 PC 실제 백엔드를 바라보는 구조라 임의로 로그인 시도하지 않음) — **사용자가 로컬 PC 실제 화면에서 5가지 테스트 항목(초기 화면·그룹 전환·하위탭 기억·거래처마스터 전환·필터 유지) 전부 정상 동작 확인 완료**
  - **프론트엔드 전용 변경**(`Dashboard.tsx`·`Tab4.tsx`·`ClientMasterSection.tsx` 3개 파일) — 백엔드·DB 변경 없음, 사무실 PC 배포 대상 아님. 로컬 PC Next.js 재시작(또는 개발 서버 자동 반영)만으로 확인 가능
- **발행요청목록·발행완료 탭 "업무명" 칸 가독성 개선(같은 날, 후속)** ✅ (사용자 제보: 작업구분(조)가 여러 개 합쳐진 거래명세서는 업무명이 쉼표로 길게 이어붙어 표시되는데(`lib/issuedGrouping.ts`의 `build레벨1그룹()`이 고유 업무명을 `", "`로 합침), 표가 `whitespace-nowrap`+가로 스크롤 구조라 그 칸 하나 때문에 표 전체가 옆으로 밀려 나머지 칸(예상공급가액 등)을 보려면 매번 가로 스크롤을 해야 했음): `frontend/components/InvoiceIssuedLevel1Table.tsx`의 업무명 `<td>`에 `max-w-[220px] truncate` + `title` 툴팁 적용 — 길면 말줄임(...) 처리되고 마우스를 올리면 전체 업무명이 브라우저 기본 툴팁으로 보임. 이 컴포넌트는 발행요청목록(대기)·발행완료(완료) 두 탭이 공유하는 표라 한 번의 수정으로 둘 다 적용됨. 검증: `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과. 프론트엔드 전용, 사무실 PC 배포 대상 아님

### 2026-08-13
- **거래명세서 공급가액·부가세 직접 입력(override) + 수정 감사이력(Audit Log) 신규 — 구현·검증 완료(로컬 PC만, 사무실 PC 미배포)** ✅ (마케팅팀 요청: 원단위 절사·반올림 차이를 보정하려고 발행 전 화면에서 자동계산된 공급가액·부가세를 직접 입력해 덮어쓸 수 있어야 함. 조가 여러 개라 은행별 등으로 시트가 여러 장 나오는 거래명세서(BC카드 사례처럼 조 6개)는 개별 조 시트가 아니라 맨 앞 "통합시트"의 공급가액·부가세·합계만 조정하면 된다고 사용자 확인. 이어서 "DB 스키마를 건드리는 김에" 총계 조정뿐 아니라 기존에도 가능했던 수량·단가·금액 항목별 수정까지 포함해 "누가/언제 무엇을 바꿨는지" 남는 정식 감사이력도 함께 요청받음)
  - **조사로 발견한 기존 버그 2건**: ①`_거래명세서_엑셀_시트목록()`(다운로드 시점)이 DB에 저장된 `거래명세서.공급가액/세액/합계`를 전혀 안 읽고 매번 `거래명세서_품목`(구분='최종') 라인아이템을 다시 합산해 재계산하고 있었음 — override가 실제로 효과를 내려면 이 다운로드 경로 자체를 고쳐야 했음. ②`Tab4Invoice.tsx`의 확정 저장 로직이 부가세 "포함" 거래처에서 `InvoicePreviewDialog.tsx`가 미리보기 화면에 보여주는 총액÷11 역산 분리(`billing.부가세_표시분리()` 방식, 2026-08-12 도입)를 안 쓰고 세액=0·공급가액=원시합계로 저장하고 있어, 화면에 보이던 값과 실제 DB 저장값이 이미 어긋나 있었음 — 이번에 함께 수정
  - **DB 스키마**: `거래명세서`에 `공급가액_직접입력`·`세액_직접입력`(nullable DECIMAL, NULL=자동계산 유지) 2개 컬럼 신규 + `거래명세서_수정이력` 테이블 신규(거래명세서번호·필드명('공급가액'/'세액'/'품목목록')·이전값·이후값·비고·수정자·수정일시) — 라인아이템은 사용자가 행을 자유롭게 추가·삭제·복사할 수 있어 원본↔최종 몇 번째 행이 대응되는지 기계적으로 정의할 수 없다는 판단에 따라, 항목별(행 단위) diff 대신 "품목목록이 변경됨"(원본 N건→최종 M건) 요약 1행 + 값이 하나뿐이라 명확한 공급가액·세액은 필드 단위 이전값→이후값으로 기록하는 방식 채택
  - `scripts/api.py`: `거래명세서요청()`가 `auth.get_current_user()`로 실제 로그인 계정명을 받아(기존엔 인증 여부만 확인하고 버리고 있었음) 감사이력 `수정자`로 기록. override는 통합시트명 트리밍(조 2개 이상인데 통합시트명 없으면 무효화)과 동일한 방어 패턴 적용 + 둘 중 하나만 오면 반쪽짜리 override를 무효화하는 방어 추가. **편집여부 판정을 확장**(품목 변경뿐 아니라 총계 override만 있어도 1) — "편집됨" 배지가 떠서 감사이력을 조회할 수 있게 하는 목적 + 기존 "편집된 거래명세서는 부분취소 불가" 보호가 override 건에도 자동으로 적용되는 부수효과(override된 총액을 무시하고 부분취소가 자동재계산해버리는 걸 막아줌, 의도적으로 그대로 둠). `거래명세서부분취소()`의 UPDATE에도 override 컬럼 NULL 리셋을 추가해뒀으나, 실제로는 위 편집여부 확장으로 override 건은 부분취소 자체가 항상 막히므로 이 리셋은 방어적 코드(현재 도달 불가) — 전체취소(DELETE)는 편집여부와 무관하게 항상 허용됨. `_거래명세서_엑셀_시트목록()`는 단일 시트(조 1개 이하)·통합시트(조 2개 이상) 계산 직후 override가 있으면 그 값으로 교체하되, **개별 조 시트는 절대 건드리지 않음**. `거래명세서품목이력()`(개별 건 "편집됨" 팝업)이 감사이력 배열도 함께 반환하도록 확장 + 신규 `GET /거래명세서수정이력`(전체 거래명세서에 걸친 이력 전량 반환, "수정이력" 탭 전용)
  - 프론트엔드: `InvoicePreviewDialog.tsx` 하단 공급가액·부가세 칸을 읽기전용 텍스트에서 number input으로 변경(자동값이 placeholder, "자동값 사용" 버튼으로 해제), 조가 여러 개인데 통합시트명 미입력 시 입력칸 비활성+안내 문구. `Tab4Invoice.tsx`는 override 값이 있으면 그대로 쓰고, 없으면 "포함" 거래처는 총액÷11 역산(위 버그 수정)·"별도"는 기존 방식 유지. `InvoiceHistoryDialog.tsx`(기존 "편집됨" 팝업)에 감사이력 표 추가. **신규 "수정이력" 하위탭**(사용자 요청 — 기존 팝업은 개별 건 1건씩만 보여줘서, 특정 건을 몰라도 전체를 검색·조회할 수 있는 화면이 필요함): `Tab4EditHistory.tsx`(신규, 표+검색창, 행 클릭 시 해당 건의 기존 팝업 연결) + `app/api/invoice-edit-history/route.ts`(신규 프록시) + `Dashboard.tsx`의 "거래명세서 관리" 그룹 하위탭 4번째로 추가(`Tab4.tsx`의 `Tab4SubTabId`에 `"history"` 추가)
  - 검증: `python -m py_compile`, `npx tsc --noEmit`·`npx eslint .`·`npm run build` 전부 통과. **로컬 MariaDB 종단 테스트**(임시 테스트 거래처 1개로 진행 후 정리) — 조 없음(자동/override 각각), 조 여러 개+통합시트명 있음(통합시트만 override 반영·개별 조 시트 2개는 자동값 유지 확인), override 건 부분취소가 400으로 차단되는지, 전체취소는 편집여부와 무관하게 허용되는지, 감사이력이 공급가액·세액·품목목록 3종 필드명으로 정확히 기록되는지까지 `openpyxl`로 실제 다운로드 엑셀 셀 값(K30 공급가액·N30 세액·K14 합계)을 직접 열어 확인 — 총 20개 검증 항목 전부 통과
  - **브라우저 클릭 테스트는 이번 세션 환경에 로그인 자격 증명이 없어 미수행** — 사용자가 로컬 PC 실제 화면에서 확인 필요(아래 화면 테스트 체크리스트 참고)
  - **사무실 PC 배포 필요**(신규 컬럼 2개·테이블 1개: `scripts/init_db_mariadb.py` 마이그레이션 포함, `scripts/api.py` 반영) — 아직 미착수, 사용자가 로컬 PC 화면 확인 후 별도 승인 시 진행 예정
- **위 기능 실사용 확인 중 발견한 문제 3건 수정(같은 날, 후속)** ✅
  1. **조정 입력칸 위/아래 화살표 클릭 시 -1로 튀는 버그**: `type="number"` 입력칸이 빈 값 상태에서 화살표를 누르면 0에서 시작해 -1이 되던 문제 — 이 파일의 수량·단가·금액 입력칸이 이미 쓰던 천단위 콤마 표시 방식(`type="text"` + `콤마표시()`/`콤마숫자파싱()`, 2026-08-09 패턴)으로 통일해 근본 원인(빈 값 기반 스피너)을 제거
  2. **수정 시 전체 자리수를 새로 입력해야 하던 문제**: 입력칸이 빈 칸+placeholder 방식이었던 걸, 손대기 전엔 지금 자동계산값을 그대로 보여주고(품목 수정에 따라 계속 갱신) 그 안에서 바로 편집할 수 있도록 변경 — 한 번이라도 고치면 그 값에 고정되고 "자동값 사용" 버튼으로 되돌릴 수 있음. 화면에 보이는 값을 그대로 확정 시 전송(WYSIWYG), 서버가 자동계산값과 같으면 override로 취급하지 않으므로(편집여부·감사이력 판정) 손 안 댄 값도 안전하게 그대로 전송 가능
  3. **품목(수량·단가·금액) 수정 이력이 요약 한 줄로만 남던 것을 항목별 상세로 확장**(사용자 요청 — "이 품목은 이렇게 바뀌었다"를 구체적으로 보고 싶음): `scripts/api.py`에 `_품목_변경_이력(원본목록, 최종목록)` 신규 — 원본·최종 품목을 (코드, 표시품명) 키로 매칭해, 값만 바뀐 항목은 필드별(수량/단가/금액) 이전값→이후값을, 새로 추가되거나 삭제된 항목은 "품목추가"/"품목삭제"로 기록(신규 컬럼 없이 기존 `거래명세서_수정이력`의 이전값·이후값·비고 컬럼 재사용). 이름 자체가 바뀐 행(규칙 적용·개명)은 이 키로 매칭이 안 돼 "삭제+추가" 한 쌍으로 근사 기록됨(정확한 rename 추적은 범위 밖으로 명시적으로 둠)
  - 검증: `python -m py_compile`, `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과. 로컬 MariaDB 종단 테스트 추가 — 서버가 실제 계산한 원본 품목을 그대로 제출하되 한 항목의 수량만 바꿔 제출 → 그 항목의 수량·금액 변경만 정확히 기록되고 안 바뀐 다른 항목은 이력에 안 남는지 7개 항목 검증 통과(테스트 데이터 정리 완료)
- **실사용 확인 중 발견한 문제 3건 추가 수정(같은 날, 2차 후속)** ✅
  1. **"수정이력" 탭이 확정 직후 새로고침 없이는 새 내역을 못 보여줌**: 마운트 시 한 번만 조회하던 걸, "거래명세서 관리" 최상위 탭 재진입 시 재조회하는 기존 패턴(SKILL-34, `Tab4.tsx`)과 동일하게 이 서브탭도 "비활성→활성 전환" 시점마다 재조회하도록 `Tab4EditHistory.tsx`에 `active` prop 추가(`Tab4.tsx`가 `subTab==="history"`를 전달)
  2. **수정이력 표 "필드" 칸 표기 방식 변경**(사용자 요청): 필드명·비고를 별도 컬럼으로 나눠 보여주던 걸 "품목명(필드명)"(예: "전산출력비(수량)") 한 칸으로 합침 — 공급가액·세액은 비고가 없어 필드명만 그대로 표시. 두 화면("편집됨" 팝업 `InvoiceHistoryDialog.tsx`, "수정이력" 탭 `Tab4EditHistory.tsx`)이 공유하는 신규 `frontend/lib/auditLog.ts`(`필드표시()`)로 통일
  3. 품목별 수량·단가·금액 수정이 이력에 안 보인다는 제보는 위 1번(탭이 새로고침 안 됨) 때문에 확인이 안 됐던 것으로, 실제 기록 로직 자체는 직전 항목에서 이미 검증 완료된 상태 — 재확인 요청
  - 검증: `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과

### 2026-08-14
- **수정이력 품목 diff 기준선을 "자동계산 원본" → "조건식 적용 후 청구명세서"로 변경 + "편집됨" 배지·부분취소 게이트 분리 — 구현·검증 완료(로컬 PC만, 사무실 PC 미배포)** ✅ (사용자 제보: 실사용 화면 스크린샷에서 저장된 조건식이 "출력비"를 "전산출력비"로 자동 변환해두는데, 이 상태에서 수량 하나만 고쳐도 (코드,표시품명) 키가 안 맞아 "6개 품목 삭제+7개 품목 추가"처럼 지저분하게 기록되던 문제)
  - `scripts/api.py` `거래명세서요청()`: 원본목록 계산 직후 `POST /거래명세서미리보기`와 동일한 방식(`_통합조건식_판정()`+`billing.적용_규칙()`)으로 "조건식 적용 후 청구명세서"(기준목록)를 서버가 재계산해, 품목별 감사이력(`_품목_변경_이력()`)의 비교 기준을 원본목록 대신 이 기준목록으로 교체. **종단 테스트로 추가 발견한 사각지대**: 조건식에 안 걸린(미분류) 원본 항목이 최종본에 그대로 남아있으면 기준목록에서 빠져 있어 "품목추가"로 오탐되는 버그를 함께 수정(`규칙적용결과`+`미분류` 둘 다 기준목록에 포함하도록 반영)
  - **"편집여부"(부분취소 게이트)는 그대로 원본 vs 최종 기준 유지** — 부분취소는 확정된 품목 스냅샷(`거래명세서_품목`)을 다시 만들어주지 않으므로, 조건식만 자동 적용됐을 뿐이어도(사람 손 안 댐) 여전히 보수적으로 막아야 안전하다고 판단. 대신 **"편집됨" 배지 표시 기준을 분리**해 새로 만든 "이 거래명세서에 감사이력이 1건이라도 있는지"(`수정이력있음`)로 교체 — 사람이 실제로 뭔가 조정했을 때만 정확하게 배지가 뜨도록 함. `발행목록()`에 `수정이력있음` 필드 추가, `Dashboard.tsx`·`app/page.tsx`·`lib/issuedGrouping.ts`·`InvoiceIssuedLevel1Table.tsx`에 반영. 거래명세서 확정 응답에도 `수정이력있음`을 함께 내려줘 확정 직후 배지가 새로고침 없이 바로 반영되도록 함(`Tab4Invoice.tsx` 낙관적 업데이트)
  - **알려진(사용자 확인된) 한계**: ①조건식을 쓰는 거래처는 확정할 때마다 raw 원본과 항상 달라지므로 편집여부=1이 되어 부분취소가 사실상 거의 항상 막힘 — 이건 2026-07-22 규칙엔진 도입 때부터 있던 기존 동작이며 오늘 작업과 무관, 부분취소 개선은 별도 과제로 분리하기로 사용자와 확정. ②배지-부분취소 분리로 인해 "배지 없어도 부분취소는 막힐 수 있는" 케이스가 생기지만, 부분취소 시도 시 오류 메시지로 충분히 안내되므로 추가 UI 작업 없이 진행하기로 확정. ③규칙을 이번 확정에서 처음 추가한 경우 그 규칙은 아직 저장 전이라 기준선 재조회가 못 잡음(다음 확정부터는 정상 반영)
  - 검증: `python -m py_compile`, `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과. 로컬 MariaDB 종단 테스트(임시 거래처+조건식 규칙 1개로 스크린샷 재현 케이스 포함 4개 시나리오, 13개 항목) 전부 통과 — 규칙만 적용(편집여부=1·배지 없음)/규칙+수동수정(배지 뜸, 삭제+추가 쌍 안 잡히고 정확히 수량·금액만 기록)/규칙 없는 거래처(회귀 없음)/총계 override 추가(편집여부=1·배지 뜸, 품목 이력은 없음)
  - **사무실 PC 배포 필요**(`scripts/api.py`만, DB 스키마 변경 없음) — 아직 미착수, 사용자가 로컬 PC 화면 확인 후 별도 승인 시 진행 예정
- **수정이력 3건 보완(같은 날, 후속 세션) — 구현·검증 완료(로컬 PC만, 사무실 PC 미착수)** ✅ (`.claude/plans/plan_수정이력_3건보완.md`에서 계획 승인만 돼 있던 3건을 이어서 구현): ①"새 행 추가"로 수량·단가·금액이 전부 0인 빈 행만 만들고 확정해도 "품목추가" 감사이력과 "편집됨" 배지가 생기던 오탐 수정(`_품목_변경_이력()`에 `billing.적용_규칙()`과 동일한 0값 필터 추가, 확정 응답의 "수정이력있음"도 실제 INSERT된 항목 기준으로 재계산해 낙관적 업데이트 배지가 잠깐 떴다 사라지는 불일치까지 함께 제거) ②거래명세서를 취소하면 `거래명세서_수정이력`이 `ON DELETE CASCADE` FK로 함께 삭제돼 감사 기록 자체가 사라지던 문제 — FK 제거 + `거래처명`·`업무명` 컬럼을 등록 시점 값으로 비정규화 저장(거래명세서품명이력과 동일한 "독립 이력" 패턴), `GET /거래명세서수정이력`을 LEFT JOIN으로 전환하고 `GET /거래명세서품목이력/{no}`는 헤더가 없어도(취소된 건) 404 대신 `존재함:false`+남아있는 감사이력을 반환하도록 폴백 추가 ③발행요청목록 "취소"·발행완료 "발행 취소(되돌리기)"에 사유 입력 신규(`ConfirmDialog.tsx`에 `reasonRequired`/`reason`/`onReasonChange` 옵션 추가) — 취소는 선택 입력, 되돌리기는 서버(`거래명세서발행취소_요청.취소사유: str`, 빈 문자열은 400)·프론트(확인 버튼 비활성화) 둘 다 필수로 검증. `scripts/init_db_mariadb.py`(FK 제거는 `information_schema.TABLE_CONSTRAINTS`로 실제 제약명 조회 후 `DROP FOREIGN KEY`, 신규 컬럼 2개·인덱스 1개) 마이그레이션 로컬 MariaDB 실행 및 재실행 멱등성 확인 완료, `python -m py_compile`·`npx tsc --noEmit`·`npx eslint .`·`npm run build` 전부 통과, 로컬 MariaDB 종단 테스트(임시 거래처, `api.거래명세서요청()`등 내부 함수 직접 호출) 5개 시나리오 전부 통과 후 테스트 데이터 정리 완료. `InvoiceHistoryDialog.tsx`(존재함=false 안내 문구)·`Tab4EditHistory.tsx`(업무명 컬럼·검색 필터 추가)도 함께 반영. **사무실 PC 배포 필요**(`scripts/api.py`·`scripts/init_db_mariadb.py`, 8/13 override+감사이력 기능과 함께 배포 예정) — 아직 미착수.
  - **후속 수정(같은 날, 즉시)**: 취소·발행취소(되돌리기) 사유가 DB(`거래명세서_수정이력.비고`)엔 저장되는데 "수정이력" 탭·"편집됨" 팝업 화면에는 안 보이던 버그 — `필드표시()`(`frontend/lib/auditLog.ts`)가 품목 필드(수량·단가·금액 등)만 비고를 붙여 보여주고 "취소"·"발행취소(되돌리기)" 필드명은 비고를 그냥 버리고 있었음(사용자 제보로 발견, DB엔 처음부터 정상 저장돼 있었음 — 표시 누락만 수정). `사유필드` 집합을 추가해 이 두 필드명도 "필드명: 비고" 형식으로 표시하도록 수정, `tsc`·`eslint`·`build` 통과. 프론트엔드 전용, 사무실 PC 배포 대상 아님.
- **품목 수정으로 자동 변동된 총액도 감사이력에 기록 + "편집됨" 배지 증감 색상 + 총액 차이 표시 — 구현·검증 완료(로컬 PC만, 사무실 PC 미착수)** ✅ (사용자 요청: 지금까지는 공급가액·부가세를 화면에서 직접 입력(override)했을 때만 감사이력이 남았는데, 오른쪽 표에서 품목 수량·단가·금액을 고쳐서 총액이 "자동으로" 바뀌는 가장 흔한 편집 방식은 총액 변화 자체가 기록되지 않던 문제 — 계획 승인(`.claude/plans`에 남기지 않고 대화 중 ExitPlanMode로 즉시 승인받은 플랜, 파일명 `twinkly-foraging-gosling.md`) 후 구현): `거래명세서요청()`의 총액 변경 감지 기준을 "override 여부"에서 "기준목록(조건식 적용 후, 사람이 손대기 전) 대비 실제 저장값"으로 통일 — override든 품목 편집이든 기준 대비 달라지면 공급가액·세액 로그가 남고, **신규로 "합계" 필드명 로그도 추가**(총액이 얼마나 바뀌었는지 한 줄로 확인 가능, DB 스키마 변경 없이 기존 컬럼 재사용). 확정 응답에 부호 있는 `합계증감` 필드 신규 추가, `GET /발행목록`도 `거래명세서_수정이력`(필드명='합계')에서 같은 값을 계산해 내려줌. 프론트: "편집됨" 배지 색상을 합계증감 부호에 따라 빨강(증가)/파랑(감소)/기존 호박색(변동 없음)으로 분기(`InvoiceIssuedLevel1Table.tsx`), "편집됨" 팝업·"수정이력" 탭의 이후값 옆에 총계 3종(공급가액·세액·합계)만 색깔 있는 차이(`(+16,000)`/`(-3,200)`) 표기(`lib/auditLog.ts`에 `차이표시()`·`차이색상()` 신규, 품목류 필드는 스코프 밖 — 금액 증감까지 색칠하면 수량 변화와 헷갈릴 수 있어 제외). `합계증감` 필드를 `발행행` 타입 체인 전체(`Dashboard.tsx`·`app/page.tsx`·`Tab4Invoice.tsx` 낙관적 업데이트·`lib/issuedGrouping.ts`)에 전파. `python -m py_compile`·`npx tsc --noEmit`·`npx eslint .`·`npm run build` 전부 통과, 로컬 MariaDB 종단 테스트 4개 시나리오(품목 수량 증가→합계증감 양수+총계 3종 로그, 품목 수량 감소→합계증감 음수, override만 사용해도 회귀 없이 3종 로그 유지, `GET /발행목록` 응답값이 확정 응답과 일치) 전부 통과 후 테스트 데이터 정리 확인. **사무실 PC 배포 필요**(`scripts/api.py`만, DB 스키마 변경 없음) — 8/13~14 감사이력 관련 미배포분과 함께 배포 예정, 아직 미착수.
  - **후속 버그 수정(같은 날, 즉시)**: 사용자가 실사용 스크린샷으로 제보 — "편집됨" 팝업의 "원본(자동계산)"·"최종(확정된 내용)" 두 표 하단 공급가액·부가세·합계 요약이 항상 똑같은 숫자를 보여주고 있었음(위 상단 수정이력 표의 이전값·이후값과도 안 맞았음). 원인: `거래명세서품목이력()`이 거래명세서(그룹) 단위로 저장된 세액 값 **하나**를 원본·최종 두 표에 그대로 재사용하고 있었음 — 총계 override(2026-08-13) 도입 전까지는 "같은 인보이스면 원본·최종 부가세가 항상 같다"는 가정이 맞았지만, override나(오늘 추가된) 품목 편집으로 총액이 자동으로 바뀌는 경우엔 최종 쪽이 원본과 달라야 하는데 계속 원본과 동일한 숫자만 보여주고 있었음. `거래명세서_수정이력`에 이미 기록되는 '공급가액'·'세액' 로그(오늘 위 항목에서 추가)의 이전값·이후값을 그대로 재사용해 원본/최종 값을 정확히 분리(조정 자체가 없었던 건은 기존처럼 하나의 값을 양쪽에 재사용, 회귀 없음) — `scripts/api.py`(`거래명세서품목이력()` 응답을 `세액` 단일 필드에서 `원본세액`·`최종세액`·`원본공급가액`·`최종공급가액` 4개로 분리) + `frontend/components/InvoiceHistoryDialog.tsx`(표 렌더링 함수가 공급가액도 자체 재계산하지 않고 백엔드 값을 그대로 받아 쓰도록 변경). `tsc`·`eslint`·`build` 통과, 로컬 MariaDB 종단 테스트(override 있는 건에서 원본≠최종 정확히 분리되는지, override 없는 대조군은 기존처럼 원본=최종 유지되는지) 통과 후 테스트 데이터 정리 확인.
  - **후속 추가(같은 날, 즉시)**: 발행요청목록·발행완료 표에 "예상공급가액" 오른쪽으로 **"청구공급가액"** 열 신규 추가(사용자 요청) — "확정공급가액(차이금액)" 형식으로 표시, 차이금액은 청구(실제 확정 저장값, override 우선)와 예상(현재 시점 자동계산) 간 차이이고 폰트 색상은 "편집됨" 배지와 동일한 규칙(+면 빨강/-면 파랑, 0이면 괄호 자체를 생략). `발행목록()`이 `거래명세서.공급가액`·`공급가액_직접입력`을 함께 조회해 거래명세서번호 단위 "확정공급가액" 필드를 응답에 추가(단가마스터가 나중에 바뀌어도 실제 청구된 값은 그대로 보여야 하므로 재계산이 아니라 저장값 그대로 사용), `거래명세서요청()` 확정 응답에도 동일 필드 추가(낙관적 업데이트). `Dashboard.tsx`·`app/page.tsx`·`Tab4Invoice.tsx`·`lib/issuedGrouping.ts`에 필드 전파, `InvoiceIssuedLevel1Table.tsx`에 컬럼 렌더링 추가. `tsc`·`eslint`·`build` 통과, 로컬 MariaDB 종단 테스트(override 있는 건·없는 건 각각 확정공급가액 값 확인 + `GET /발행목록` 응답 일치) 통과 후 테스트 데이터 정리 확인.
- **8/13~14 감사이력·수정이력 관련 기능 전체 — 사무실 PC 배포 완료(2026-08-14)** ✅: `scripts/api.py`·`scripts/init_db_mariadb.py` 사무실 PC로 반영 + 마이그레이션 실행(신규 컬럼: `거래명세서.공급가액_직접입력`·`세액_직접입력`, 신규 테이블 `거래명세서_수정이력`, FK 제거+`거래처명`·`업무명` 컬럼+인덱스 추가) + FastAPI 재시작까지 사용자가 직접 완료. 이번에 함께 배포된 항목: 공급가액·부가세 직접입력(override)+감사이력(8/13), 수정이력 3건 보완(0값 새 행 오탐 제거·취소해도 로그 보존·취소 사유 기록), 취소 사유 표시 누락 버그 수정, 품목 수정 시 총액 변동 자동 기록+"편집됨" 배지 증감 색상+총액 차이 표시, "편집됨" 팝업 원본/최종 값 분리 버그 수정, 발행요청목록/발행완료 "청구공급가액" 열 추가. 실사용 확인 중 별도로 제보된 "새 행 추가 자동완성 작업구분(조) 미반영" 건은 조사 결과 코드 로직 문제가 아닌 것으로 판단되어(사용자가 직접 재확인 후 이슈 취소) 별도 수정 없음. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음.

### 2026-08-15~16
- **단가마스터 자재명 단위 정규화 + 거래명세서 원본 분할표기·조건식 통합 — [1]~[6] 전체 완료(로컬·사무실 PC 배포 및 백필까지 종료)** ✅ (사용자 요청: 같은 (거래처+업무명+작업명) 조합이라도 실제 사용된 자재(용지·봉투·삽지 종류)에 따라 단가가 달라야 하는 업무 지원 — 실사례: 업무의뢰서 95903 현대해상 자동차증권 작업에서 용지 자재코드별로 단가가 다름. 상세 설계·근거는 `.claude/plans/plan_단가마스터_자재명정규화.md` 참고, 아래는 요약)
  - **[1] DB 스키마**: 신규 테이블 `단가마스터_자재단가`·`단가마스터_자재단가_매칭`(2계층 정규화 — 여러 자재코드가 한 단가를 공유하는 다대일 구조 지원) + `자재사용현황`에 `자재코드`·`자재명` 컬럼 추가(UNIQUE 제약도 함께 확장). SQLite는 미반영(Streamlit은 이 기능부터 동결, 아래 참고)
  - **[2] 데이터 파이프라인**: `data_transform.merge_자재()`가 자재코드·자재명을 groupby 키에 포함시켜 라인 단위로 보존(예전엔 "용지/봉투/삽지" 4대 카테고리로 뭉개지며 소실되던 지점) — 운영통계자료에 조인되는 4개 `_사용량` 요약 컬럼 로직은 손대지 않아 기존 화면 회귀 없음. `preprocess.load_자재_raw()`가 엑셀의 "작업내역서자재" 컬럼을 "자재코드"로 인식. 실데이터(의뢰서 95903)로 DB까지 정확히 도달하는지 검증 완료
  - **[3] 계산 로직**: `billing._자재단가_조회()`·`_자재별_처리()` 신규 — 자재코드 우선, 없으면 자재명으로 매칭, 등록 안 된 자재는 기존 기본단가로 자동 폴백(총수량 불변 보장). `build_품목행()`의 출력자재비(F)·삽지비 계산을 자재키별 순회로 변경, 행 키를 `(품목,작업명,단가)` → `(품목,작업명,단가,자재명)`으로 확장. **봉입자재비(E)의 기존 일반/각대대봉투 하드코딩 구조는 이번엔 손대지 않고 그대로 유지**(새 자재단가 테이블에 데이터가 없는 채로 건드리면 기존 등록 거래처 계산이 깨질 위험이 있어, 데이터 이관과 함께 처리하기로 순서 조정 — 계획 대비 실행 중 안전하게 조정한 부분)
  - **[4] API**: 자재단가 CRUD 3개(`POST/PUT/DELETE 단가마스터/{id}/자재단가`, 코드는 "출력자재비"/"봉입자재비"/"삽지비" 3종만 허용 — 출력비(P)는 페이지수 기준 계산이라 자재 단위로 나눌 근거 데이터가 없어 이번엔 등록 자체를 막음, 상세 사유·재검토 조건은 계획 파일에 기록), `GET /단가마스터` 자재단가 하위목록 중첩, `POST /거래명세서미리보기` 응답에 "자재명" 추가, `GET /자재목록`(신규, 등록 화면에서 "실제 이 업무에 어떤 자재가 쓰였는지" 후보 제공 — 처음엔 자재코드·자재명을 직접 입력하게 만들었다가 "사용자가 알 방법이 없다"는 피드백으로 후보 검색 방식으로 교체)
  - **[5] 프론트엔드**: `PricingMaterialSection.tsx`(신규, 자재단가 등록 UI — 항목/단가/매칭자재 검색·추가/수정/삭제/복사), `PricingMasterTable.tsx`(자재별 단가 N종 뱃지), `InvoicePreviewDialog.tsx`(원본 표에 자재명 노출, 왼→오 비율 50:50으로 재조정, 팝업 최대 크기화, 헤더 "품목"→"품목(작업명) 자재명"), `ConditionRuleModal.tsx`/`billingRules.ts`(조건 필드 "자재명" 추가). `scripts/app.py`(Streamlit)는 "새 웹 화면을 사용하세요" 안내만 추가하고 로직 이식 안 함(이 기능부터 Streamlit 동결, 최종 배포 목표가 이미 Next.js로 확정된 상태라 사용자 확정)
  - **실사용 중 발견·보완한 이슈들(같은 세션, 후속)**: ①경로 파라미터 한글 함정 재발견(SKILL-13) — `PUT /단가마스터/자재단가/{자재단가_id}` 최초 구현 시 항상 404 나던 것 즉시 수정 ②단가관리 팝업이 자재별 단가 섹션 추가 후 세로 스크롤 없이 넘쳐 저장 버튼이 화면 밖으로 밀리던 문제 수정(`max-h-[90vh] overflow-y-auto`) ③매칭 자재 검색 드롭다운이 그 스크롤 컨테이너 경계에 잘리거나 화면 하단 밖으로 나가 안 보이던 문제 — `document.body`에 포털로 그리고 입력칸 실제 좌표(`getBoundingClientRect`) 기준 위/아래 자동 반전 배치로 해결 ④"자재별 단가" 그루핑: 이미 있던 "한 자재단가에 여러 자재 매칭" 구조가 원본 표 행 분리에는 반영이 안 되던 걸 발견해 `build_단가맵()`이 자재단가_id별 표시라벨(표시명 우선, 없으면 자재명들로 자동 생성)을 계산하도록 수정 — 신규 스키마 없이 기존 구조 재사용, 단가마스터_자재단가가 1행=1단가라 다른 단가끼리는 구조적으로 그루핑 불가능(사용자 요구사항 자동 충족). 단, 그루핑은 같은 작업명 안에서만 적용됨(작업명이 다르면 `build_품목행()`이 애초에 작업명 단위로 먼저 나눠 처리 — 사용자 확인 후 수정 안 하기로 확정, 필요하면 오른쪽 조건식으로 처리) ⑤자재단가 등록 화면에 "복사" 버튼(매칭 자재는 항상 비워서 시작 — 같은 자재가 두 그룹에 동시 매칭되는 것 방지), 같은 항목 안에서 이미 선택했거나 이미 등록된 자재는 검색 후보에서 제외(항목이 다르면 허용) ⑥미발행 목록에 "선택 취소" 버튼 추가(필터 밖 항목 포함 전체 선택 일괄 해제)
  - 검증: `python -m py_compile`, `npx tsc --noEmit`·`npx eslint .`·`npm run build` 각 단계마다 통과. 로컬 MariaDB 종단 테스트 다수(95903류 시나리오 재현, 회귀 검증 — git HEAD 대비 실거래처 5건 총합계 완전 일치, 자재단가 그루핑 합산·자동 라벨, 기본단가(업무명·작업명 NULL) 행의 자재단가 반영 등)
  - **[6] 사무실 PC 배포 — 완료** ✅: `scripts/init_db_mariadb.py`·`data_transform.py`·`preprocess.py`·`billing.py`·`api.py` 5개 파일 반영 + 마이그레이션 실행 완료(사용자 확인). 이후 실사용 테스트 중 "자재목록이 안 뜬다" 제보 — 원인은 사무실 PC `자재사용현황`의 기존 이력이 지금까지 자재명 컬럼 자체가 없어 저장 안 되고 버려지고 있었던 데이터라, 마이그레이션 이후에도 과거분은 전부 비어있고 신규 실시간 수신 건부터만 채워지는 구조적 특성(로컬 PC는 `preprocess.py`로 엑셀 원본 전체 재적재를 한 번 했었어서 과거 이력까지 채워져 있었던 것과 대비됨). 사무실 PC에서 동일하게 엑셀 재적재로 과거 이력을 백필하려 했으나 처음엔 **`openpyxl` 모듈 미설치 + 사내망에서 pip 설치 사이트 차단**으로 중단됐다가, **IT 담당자에게 요청해 사내망 pip 사이트 차단 예외 처리**를 받아 openpyxl 설치 성공 → `preprocess.py` 재적재로 과거 이력 백필까지 완료(사용자 확인, 2026-08-16). **다중업무명 자재명 정규화 기능 — 코드·데이터·검증 전부 완료로 종료**, 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음. 남은 항목은 별개 과제인 "보류 항목"(출력비 자재별 단가)·"이관 예정"(각대대봉투 봉입자재비 이관)뿐(`.claude/plans/plan_단가마스터_자재명정규화.md` 참고).
- **"자재별 단가" 편집창 UX 버그 2건 수정(같은 날, 후속 세션)** ✅ (사용자 제보: ①"+ 같은 단가로 자재 추가" 버튼을 눌러도 새로 생긴 자재명 칸이 화면 밖에 생겨 매번 손으로 스크롤해야 함 ②자재명 칸을 클릭해 후보 목록을 연 뒤 편집창 스크롤바를 드래그하면 목록이 사라지고, 그 뒤로 같은 칸을 재클릭해도 목록이 다시 안 뜸 — 로컬·사무실 PC 동일 재현). 원인: 스크롤바 드래그 시작 시 발생하는 `mousedown`이 입력칸·목록 바깥에서 일어나 "바깥 클릭 시 닫기" 로직이 목록을 닫지만, 입력칸 포커스는 그대로 유지돼 재클릭해도 `onFocus`가 다시 발생하지 않아 목록이 못 열리는 구조였음. `frontend/components/PricingMaterialSection.tsx` 1개 파일만 수정 — ①`매칭추가()`가 ref 플래그를 켜두면 `매칭.length` 변경 직후 `editorRef.current.scrollIntoView({block:"end"})`로 편집창 맨 아래까지 자동 스크롤(기존 "편집창 진입 시 스크롤" 패턴 재사용, 편집창 최초 오픈 시엔 겹치지 않도록 분리) ②`MaterialMatchRow`의 자재명 `<input>`에 `onClick={openList}` 추가(기존 `onFocus`·`onChange`는 유지) — 이미 포커스된 칸을 재클릭해도 목록이 다시 열리도록 함. DB·API 변경 없음, 순수 프론트엔드 UI 수정. `npx tsc --noEmit`·`npx eslint .` 통과. 프론트엔드 전용이라 사무실 PC 배포 대상 아님(로컬 PC Next.js 재시작만으로 반영). 상세: `.claude/plans/jolly-prancing-blum.md`
  - **후속 보완(같은 날, 즉시)**: 위 ①번 스크롤 수정이 "자재별 단가" 편집 박스(`editorRef`) 안쪽 끝까지만 스크롤해, 그 아래 있는 다이얼로그 자체의 취소/저장 버튼(`PricingFormDialog.tsx`의 `max-h-[90vh] overflow-y-auto` 스크롤 컨테이너 안 맨 아래)까지는 여전히 안 보인다는 재제보(스크린샷) — 신규 `스크롤가능부모()` 헬퍼로 실제 세로 스크롤이 걸린 조상(다이얼로그 컨테이너)을 찾아 `scrollTo({top: scrollHeight})`로 그 컨테이너의 진짜 끝까지 스크롤하도록 수정(못 찾으면 기존 `scrollIntoView` 방식으로 안전하게 폴백). `npx tsc --noEmit`·`npx eslint .` 통과.

### 2026-08-17
- **봉입자재비(E, 봉투 실물 자재비) 자재단가 이관 + 각대대봉투단가 필드 계산 제외 — 구현·검증 완료(로컬 PC만, 사무실 PC 미배포)** ✅ (사용자 제보: 업무의뢰서 97509 현대해상에서 자재(97)에 봉입자재비 자재단가를 등록했는데 미리보기에 반영이 안 됨 — 조사 결과 봉입자재비만 8/15~16 이관 때 "각대대봉투 하드코딩 구조를 건드리면 위험하다"는 이유로 의도적으로 빠져 있었던 게 원인. 최초엔 기존 "일반/각대대봉투 두 단가" 구조를 유지한 채 자재단가만 얹는 방향으로 계획했으나, 사용자가 "각대봉투 자동분류 로직 자체가 부정확하다"고 지적(자재명에 "각대"·"대봉투" 글자가 들어있는지 텍스트로만 판정하는 `data_transform._봉투종류()` — 실제 봉투 크기를 보장하지 않는 이름짓기 관행에 의존) — **각대대봉투단가라는 별도 필드 자체를 계산에서 없애고, 큰 봉투도 그냥 봉투제작단가 밑에 자재(코드) 하나로 등록하는 방식**으로 방향 전환, 결과적으로 출력자재비·삽지비와 완전히 같은 패턴이 되어 계획보다 코드가 더 단순해짐)
  - **DB 조사**: 각대대봉투단가가 0보다 큰 곳 6곳 중 실사용은 2곳뿐(한국예탁결제원 펀드보고서 120원, KB증권 거래원장 130원) — 나머지는 0원이거나 봉투제작단가와 우연히 같은 10원이라 이관 불필요(계산 결과 금액 불변)로 확인
  - `scripts/billing.py`: `build_품목행()`의 봉입자재비 계산을 `_혼합단가_행목록()`(일반/각대대 두 단가 혼합 규칙) 대신 출력자재비·삽지비와 동일한 `_자재별_처리()`로 교체, `calc_공급가맵()`의 봉투제작비도 형제 항목(용지제작비 등)과 동일한 단순 곱셈으로 교체 — 이제 어느 계산도 각대대봉투단가를 읽지 않음. 두 호출부가 마지막 사용처였던 `_혼합단가_행목록()` 함수 자체 삭제. `build_단가맵()`의 단가컬럼 목록은 유지(DB 컬럼·값 자체는 보존하기로 한 사용자 결정)
  - **데이터 이관**: 한국예탁결제원(자재코드 1435 "무창대봉투")·KB증권(자재코드 414 "E)KB증권 거래원장 각대봉투") 각각 `단가마스터_자재단가`(코드='봉입자재비', 기존 단가 그대로) 1건 + 매칭 1건씩 로컬 MariaDB에 삽입 — 기존 청구 금액이 그대로 유지되도록(사용자 재입력 불필요)
  - 프론트엔드(`PricingFormDialog.tsx`·`PricingMasterTable.tsx`): 각대대봉투단가 입력칸·목록 표 열 숨김(DB 컬럼·기존 값은 유지, 저장 payload도 그대로 왕복돼 기존 값이 0으로 리셋되지 않음 — 렌더링만 별도 필터 목록으로 분리)
  - **검증 중 추가로 발견한 심각한 기존 버그를 함께 수정**: `build_품목행()`이 여러 업무의뢰서를 한 거래명세서로 묶어 계산할 때, "일반봉투·각대대봉투·용지·삽지 합계 수량"은 의뢰서 전체에 걸쳐 정확히 누적하면서도(`+=`) **자재코드별 세부 내역("자재별")은 누적하지 않고 마지막으로 순회한 의뢰서 것만 남는 버그**(반복문 밖에서 `z` 변수 재사용)를 한국예탁결제원 5건 회귀 검증 중 발견 — 각대대봉투 자재 실사용량 14,204개 중 220개(마지막 의뢰서분)만 반영되고 있었음. **8/15 이관된 출력자재비·삽지비도 처음부터 이 버그를 그대로 갖고 있었던 것으로 확인**(다만 실제 등록된 곳은 현대해상·강남대성수능연구소 2곳뿐이라 영향 범위는 작음) — 사용자 확인 후 같이 수정. 4개 합계 수량과 동일하게 의뢰서 전체에 걸쳐 자재별 수량을 딕셔너리로 누적하도록 `build_품목행()` 수정
  - 검증: `python -m py_compile scripts/billing.py`, `npx tsc --noEmit`·`npx eslint .`(프론트엔드) 통과. 로컬 MariaDB 실제 데이터로 종단 검증 — 현대해상 97509(자재 97이 50원으로 정상 분리), 한국예탁결제원·KB증권(이관된 자재단가가 실사용량 전체에 정확히 적용되는지, z 버그 수정 전/후 수량 비교로 재확인), BC카드·신한카드 등 자재단가 미등록 거래처(회귀 없음, 자재명=None으로 기본단가 그대로 적용) 확인
  - **사무실 PC 배포 필요**(`scripts/billing.py`만, DB는 이번에 추가한 자재단가 2건 데이터 이관 SQL도 별도 실행 필요, 프론트엔드 2개 파일) — 아직 미착수, 사용자 확인 후 진행 예정
- **출력비(P)·봉입비(M) 자재사용량 기준 전환 + 수량 불일치 안내 — 구현·검증 완료(로컬 PC만, 사무실 PC 미배포)** ✅ (같은 날 후속 세션 — 사용자 요청: 출력비·봉입비도 용지·봉투처럼 자재 종류에 따라 다르게 책정돼야 함. 조사 중 "장수=용지 자재사용량", "봉입건수=봉투 자재사용량"은 물리적으로 항상 같아야 하는 값인데 어긋나는 건 전부 생산공정관리시스템 입력 단계의 휴먼 에러임을 사용자가 현대백화점·라온기획 실사례로 직접 검증. 반면 "확정청구페이지"(기존 출력비 청구 기준)는 전체 물량의 75.6%가 정확히 "장수의 2배"로, 이는 휴먼 에러가 아니라 양면인쇄의 정상적인 결과임을 확인 — 두 발견이 서로 달라 출력비·봉입비를 다른 방식으로 설계)
  - **봉입비(M)**: 봉입건수 대신 봉투 자재사용량을 청구수량으로 완전 대체(`_자재별_처리()` 재사용, 봉입자재비(E)와 동일 자재별 데이터 공유). 자재이력 없으면 기존처럼 봉입건수로 폴백
  - **출력비(P)**: 원본 "청구페이지" 컬럼이 한 행이라도 있는 작업(사람이 직접 확인/입력한 신뢰값, 전체의 31.4%)은 확정청구페이지 총량을 그대로 유지한 채 자재 사용 비중으로만 나누는 신규 `_자재비례배분_처리()` 적용. 청구페이지가 전혀 없는 작업(68.6%, 아무도 검증 안 한 원본값)은 **용지 자재사용량 × 인쇄면 배율**(신규 단가마스터 필드, 단면=1배/양면=2배, 기본값 양면)로 대체 — 자재별 등록 단가에도 동일 배율 적용
  - DB: `단가마스터.인쇄면 ENUM('단면','양면') DEFAULT '양면'` 신규 컬럼(`init_db_mariadb.py` 마이그레이션, SQLite 미반영)
  - API: `단가마스터_자재단가_신규.코드` Literal을 3종→5종 확장("출력비"·"봉입비" 추가), `단가마스터_신규/수정`에 인쇄면 필드 추가, `POST /거래명세서미리보기` 응답에 `수량불일치`(신규 `billing.자재수량_불일치_목록()`) 필드 추가 — `build_품목행()` 호출부 4곳의 SELECT에 "청구페이지" 원본 컬럼 누락돼 있던 것도 함께 추가(안 하면 KeyError)
  - 프론트: `PricingFormDialog.tsx`에 "인쇄면" 드롭다운 신규(다이얼로그 폭 `max-w-lg`→`max-w-xl`로 확대, 사용자 승인), `PricingMasterTable.tsx`·`Dashboard.tsx`·`serverMappers.ts`에 필드 반영, `PricingMaterialSection.tsx` 코드옵션에 "출력비"·"봉입비" 추가(용지·봉투 자재종류 매핑 공유), `InvoicePreviewDialog.tsx`에 수량불일치 경고 배너(호박색) 신규
  - **구현 중 발견·수정한 버그**: `_자재비례배분_처리()`가 자재단가 미등록 상태에서도 자재명을 라벨로 노출해, 자재단가를 하나도 등록 안 한 흔한 경우에도 출력비가 자재 종류 수만큼 원본 표 행이 쪼개지던 문제(BC카드 표본 검증 중 발견) — `_자재별_처리()`와 동일하게 미매칭 자재는 라벨 없는 기본단가 한 줄로 합치도록 즉시 수정
  - 검증: `python -m py_compile`, `npx tsc --noEmit`·`npx eslint .`·`npm run build` 전부 통과. 로컬 MariaDB 실데이터 138개 (거래처,업무명,작업명) 조합 전수 회귀 — 변경 없음 111개(정상 케이스, 자재사용량이 원본과 일치), 의도된 변경 27개(휴먼 에러로 확인된 불일치 반영, 강남대성수능연구소·KB국민카드·삼성화재해상보험 등), 오류 0개. 현대백화점·라온기획·한국예탁결제원·KB증권 콕 집어 재확인
  - **사무실 PC 배포 필요**(`scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py`(인쇄면 컬럼 마이그레이션) + 프론트엔드 5개 파일) — 아직 미착수, 사용자 확인 후 진행 예정
- **실사용 검수 중 발견한 버그 2건(같은 날, 후속 세션)** — D-202608-00079(현대해상)를 사용자가 검수하다 제보
  - **① 미분류(어느 조건식에도 안 걸린) 원본 항목이 있어도 확정이 막히지 않던 문제**: 출력비 자재별 분할로 원본이 자재 그룹(예: "XP"·"FXP")별로 갈라졌는데, 조건식이 그중 한 그룹("FXP")만 매칭하도록 등록돼 있어 나머지("XP", 96,960원)가 미분류로 남았음에도 확정 버튼이 그대로 눌려 발행요청목록으로 넘어감 — 왼쪽 원본 표의 체크(✓) 표시·"미선택 N건" 요약(2026-07-24·2026-08-09)은 있었지만, **"미선택이 있으면 확정을 막는" 기능 자체가 처음부터 구현된 적이 없었던 것으로 확인**(`docs/CHANGELOG.md`·`InvoicePreviewDialog.tsx` 전수 확인). 지금까지는 출력비·봉입비가 항상 한 줄이라 미분류가 실질적으로 잘 안 생겨서 드러나지 않았던 gap — 오늘 자재별 분할 기능으로 처음 실제 금액 누락(96,960원+세액)으로 이어짐. **수정은 보류**(사용자가 검수 중이라 요청, 이번엔 원인 진단만 완료)
  - **② 조건식을 처음 만들면서 동시에 확정하면 수정이력에 스푸리어스한 품목추가/삭제가 대량 기록되는 문제 — 수정 완료** ✅: 감사이력 기준선은 "조건식 적용 후 청구명세서"(2026-08-13/14 확정)인데, 거래처에 조건식(개별·통합)이 하나도 저장돼 있지 않다가 이번 확정과 동시에 처음 만든 경우엔 기준선 조회 시점에 "기존 규칙"이 없어 원본목록 그대로로 폴백(코드에 명시된 기존 동작) — 그 결과 새로 만든 조건식으로 통합된 항목이 전부 "품목삭제(원본이름)+품목추가(규칙이름)" 쌍으로 수정이력에 남음(D-202608-00079: 15개 기록 중 실제 문제는 1개뿐). `scripts/api.py`의 `거래명세서요청()` 1줄 수정 — 기존에 저장된 규칙(`규칙목록`)이 비어있으면(=원본목록 폴백 상황) 품목 변경 이력 계산 자체를 생략. 공급가액·세액·합계 조정 로그와 "편집됨" 배지/부분취소 게이트는 원본목록 기준 그대로 유지(실제 총액이 달라진 경우는 계속 포착됨). 검증: `python -m py_compile`, 조건 분기 로직 4가지 케이스 단위 검증 통과. **사무실 PC 배포 필요**(`scripts/api.py`만) — 아직 미착수
  - **③ "편집됨" 팝업에서 수정이력이 많으면 아래 원본/최종 비교 표가 화면 밖으로 밀려나는 문제 — 수정 완료** ✅ (D-202608-00079처럼 수정이력이 많은 건을 검수하다 발견 — 수정이력 표에 높이 제한이 없어 다이얼로그 고정 높이(`h-[85vh]`) 안에서 아래 `flex-1` 영역(원본/최종 비교)을 밀어냄): `InvoiceHistoryDialog.tsx` 1개 파일 수정 — 수정이력을 기본 접힘(`▶ 수정이력 (N건)`)으로 바꾸고 클릭해야 펼쳐지게 변경, 펼쳤을 때도 `max-h-56 overflow-y-auto`로 자체 스크롤 적용해 재발 방지. 검증: `npx tsc --noEmit`·`npx eslint .` 통과. 프론트엔드 전용, 사무실 PC 배포 대상 아님
  - **④ 편집 없는(원본 그대로 발행된) 거래명세서도 "원본" 배지로 미리보기 가능하게 — 구현 완료** ✅ (사용자 요청 — 지금까지는 "편집됨" 배지가 `수정이력있음`(감사이력 1건 이상)인 건에만 보여서, 편집이 전혀 없는 건은 내용을 확인할 방법이 없었음): 조사 결과 `POST /거래명세서요청`이 `거래명세서_품목`에 원본·최종 스냅샷을 저장하는 조건은 편집여부와 무관(`요청.품목_최종 is not None`뿐)이라, Next.js로 확정된 거래명세서는 편집 여부와 상관없이 이미 스냅샷이 저장돼 있음을 실제 DB(편집여부=0 건)로 확인 — **백엔드 변경 없이 프론트엔드 표시 조건만 수정**. `InvoiceIssuedLevel1Table.tsx`(발행요청목록·발행완료 공유 표) — 배지를 항상 렌더링하도록 변경, 라벨을 `수정이력있음` 여부로 "편집됨"(기존 색상 그대로)/"원본"(중립 회색) 분기. 검증: `npx tsc --noEmit`·`npx eslint .` 통과. 프론트엔드 전용, 사무실 PC 배포 대상 아님
- **① 미분류 항목 확정 경고창 신규 구현 — 구현·브라우저 실사용 확인 완료(같은 날, 후속 세션)** ✅ (위 "실사용 검수 중 발견한 버그 2건"의 ①번, 그동안 "수정 보류" 상태였던 건 — 사용자가 해결 방향으로 "확정 시 경고 팝업"을 선택해 착수): `InvoicePreviewDialog.tsx` — 미분류(어느 조건식에도 안 걸린) 원본 항목이 남은 채로 "확정" 버튼을 클릭하면, 기존 "겹치는 항목 안내"(2026-08-08)와 동일한 공용 `ConfirmDialog` 컴포넌트를 재사용해 미분류 건수·금액·품목별(자재명 포함) 내역을 보여주고 "그래도 확정"/"미리보기로 돌아가기" 중 선택하게 함 — 취소하면 아무 것도 제출되지 않고 편집 화면으로 복귀. 기존 `handleConfirmClick()`은 미분류 존재 여부만 검사하는 게이트로 남고, 실제 제출 로직은 `applyConfirm()`으로 분리. 프론트엔드 전용(사무실 PC 배포 대상 아님). 검증: `npx tsc --noEmit`·`npx eslint .` 통과, 사용자가 브라우저에서 3단계 시나리오(미분류 있음→경고·미분류 0건→바로 제출·"미리보기로 돌아가기") 직접 확인 완료. 상세: `.claude/plans/bug_미분류_확정시_차단없음.md`(해결 완료로 갱신)
- **2026-08-17 작업분 전체 — 사무실 PC 배포 완료(2026-08-17)** ✅: 봉입자재비 자재단가 이관+각대대봉투단가 폐지, 출력비·봉입비 자재사용량 기준 전환(인쇄면 컬럼 포함), 조건식 신규생성+즉시확정 스푸리어스 이력 버그 수정 — `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py`(인쇄면 컬럼 마이그레이션 + 한국예탁결제원·KB증권 자재단가 이관 SQL 재실행 포함) 반영 및 FastAPI 재시작까지 사용자가 직접 완료(`.claude/plans/plan_2026-08-17_사무실PC배포체크리스트.md` 기준). 위 미분류 확정 경고창은 프론트엔드 전용이라 이 배포 범위와 무관. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음.

### 2026-08-18
- **"원본 vs 최종 비교" 팝업 왼쪽 "원본"을 조건식 적용 후 기준으로 표시 — 구현·로컬 종단 테스트·사무실 PC 배포 및 실사용 확인까지 완료** ✅ (사용자 스크린샷 제보, D-202608-00081: 발행요청목록·발행완료의 "원본 vs 최종 비교" 팝업이 왼쪽 "원본" 칸을 만들 때 서로 다른 두 기준을 섞어 쓰고 있었음 — 품목 목록은 조건식 적용 전 원자재 단위 그대로(`거래명세서_품목` 구분='원본')인데, 하단 공급가액·부가세·합계는 조건식 적용 후·사람이 손대기 전 기준(2026-08-14에 감사이력 기준선으로 이미 도입된 `기준목록`)을 가져다 써서, 수동 수정이 없었던 건은 품명은 안 바뀌었는데 합계만 최종과 우연히 같아 보이는 혼란스러운 화면이 나오고 있었음)
  - 확정 시점(`거래명세서요청()`)에 이미 계산해두던 `기준목록`(조건식 적용 후·사람이 손대기 전 스냅샷, 지금까지는 감사이력 "이전값" 계산에만 쓰이고 저장은 안 됐음)을 `거래명세서_품목`에 신규 구분값 `'기준'`으로 별도 저장. `거래명세서_품목.구분` ENUM을 `('원본','최종')`→`('원본','최종','기준')`으로 확장(`scripts/init_db_mariadb.py`, 신규 설치용 CREATE TABLE·기존 DB용 마이그레이션 둘 다 반영)
  - `거래명세서품목이력()`(팝업 데이터 조회) — `구분='기준'` 스냅샷이 있으면(이번 변경 이후 확정된 건) 그걸 "원본" 칸에 표시, 없으면(이 변경 이전 발행된 과거 건) 기존처럼 가공 전 원본으로 폴백(과거 데이터 백필 없이 하위호환). `구분='원본'`(가공 전 원자재 단위) 저장 자체는 그대로 유지 — 부가세구분 판정(작업명 조회) 등 다른 로직이 계속 이 raw 데이터를 씀
  - `InvoiceHistoryDialog.tsx`는 응답의 `원본`/`원본공급가액`/`원본세액` 키를 그대로 쓰므로 로직 변경 없이 안내 문구·헤더만 수정("자동계산" → "조건식 적용")
  - 검증: `python -m py_compile`·`npx tsc --noEmit`·`npx eslint .` 통과, 마이그레이션 재실행 멱등성 확인. **로컬 MariaDB 실데이터 종단 테스트**(삼성화재해상보험 모니터링안내장 실사례, 임시 확정 2건 후 정리) — 수동 수정 없이 확정 시 원본(조건식 적용, 4건→3건 그룹화)과 최종이 정확히 일치, 확정 후 수동 수정한 케이스는 원본(손대기 전)과 최종(수동 수정 반영)이 의도대로 다르게 표시되고 차액도 정확히 일치함을 확인
  - **사무실 PC 배포(`scripts/api.py`·`scripts/init_db_mariadb.py` 마이그레이션 + FastAPI 재시작) 및 실사용 확인까지 사용자가 직접 완료**. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음.
- **Streamlit 유지보수 공식 종료 결정 + 밀린 플랜 파일 정리(같은 날, 후속)** ✅: 실사용자가 없고(개발 중) Next.js가 탭1~4(작업현황요약·거래처별현황·담당자별현황·거래명세서관리) 전부 완료된 상태임을 사용자가 확인 — Streamlit(`app.py`)을 지금까지의 "당분간 병행"에서 **유지보수 종료**로 공식 전환(`CLAUDE.md` 기술스택 표에서 SQLite/sqlite3 행 제거, `docs/작업현황-대시보드-생성-KNOWLEDGE.md` "최종 배포 목표"·아키텍처 섹션도 갱신 — 코드 변경 없음, 문서만). 이어서 오래 밀려있던 플랜 파일 상태를 사용자 확인 받아 정리: ①`plan_단가마스터_자재명정규화.md` "보류 항목①"(출력비 자재별 단가, 2026-08-16부터 미착수로 남아있었음)이 실제로는 2026-08-17 "출력비·봉입비 자재사용량 전환" 작업으로 이미 해결돼 있었음을 확인해 갱신 ②`plan_봉투크기단가확장.md` Phase 2(봉투 크기별 단가 확장)는 자재단가 등록 기능(`단가마스터_자재단가`)이 그 역할을 대체해 더 이상 진행 안 함으로 종료 ③`plan_2026-08-17_사무실PC배포체크리스트.md`의 "남은 미해결 항목" 목록 최신화(미분류 확정 차단은 이미 해결 반영, 압착(씰링) 공정 확장·장수 기준 출력비 청구 이슈는 아래 신규 플랜 파일 2개로 각각 이전 — 체크리스트 파일 자체는 이미 "완료" 상태라 그 안에 한 줄로만 남아있으면 다음 세션이 놓치기 쉬워, 둘 다 전용 파일로 승격)
- **신규 이슈 기록 2건 (다음 세션에서 착수 예정, 이번엔 코드 조사·구현 전혀 안 함)**:
  1. **공정 추가(씰링·에폭시·코팅·제본·중철) 시 "건수=봉입건수" 전제가 깨지는 문제** — `.claude/plans/plan_신규공정확장_씰링에폭시코팅제본중철.md` 신규 생성. 이 공정들은 봉투 자재를 쓰지 않는 작업이라 "건수"가 봉입건수를 뜻하지 않게 됨 — 로컬 MariaDB 확인 결과 씰링(97건)·에폭시(4건)·제본(2,287건, 대부분 강남대성수능연구소 작업명 내부 표기)·중철(18건)은 **이미 데이터가 들어와 있음**(2026-08-17 메모의 "데이터 없어 보류"는 갱신 필요했음 — 이번에 반영), 압착·코팅은 아직 0건. 씰링 샘플 다수에서 `건수`==`장수`가 정확히 일치하는 정황 발견(봉투 없이 종이 자체를 봉함하는 방식 추정)
  2. **"장수 기준으로 출력비를 청구하는 거래처" 조사** — `.claude/plans/plan_장수기준_출력비청구_조사.md` 신규 생성(내용은 기존 2026-08-17 메모를 파일로 옮긴 것뿐, 새로 조사한 내용 없음)

### 2026-08-20
- **미발행목록 "예상공급가액" ≠ 발행요청목록 "청구공급가액" 불일치 버그 수정 — 코드 수정·검증·사무실 PC 배포까지 전부 완료** ✅ (사용자가 KB증권 의뢰서 93362 스크린샷으로 208,290원 차이 제보): 원인은 2026-08-17 "출력비·봉입비 자재사용량 기준 전환"이 `build_품목행()`(미리보기·확정·저장)에만 반영되고 `calc_공급가맵()`(미발행목록·발행목록·예상공급가액·부분취소)에는 반영 안 된 것 — KB증권 "거래원장 각대봉투"에 개별 등록된 단가(130원)가 `calc_공급가맵()`에서 무시되고 기본단가(24원)로만 계산돼 과소 청구되고 있었음. **부분취소 API도 이 옛 함수로 재계산한 값을 그대로 거래명세서 금액에 덮어쓰고 있어, 화면 표시 문제를 넘어 부분취소 시 정확했던 금액이 부정확해지는 실제 청구 버그였음**(같이 발견·수정)
  - `scripts/billing.py`: `build_품목행()`의 품목별 계산 로직을 `_작업별_품목누적()` 공유 함수로 추출(로직 변경 없음, 위치만 이동), `calc_공급가맵()`이 이 함수를 의뢰서 1건씩 호출하도록 재작성(미발행목록이 요구하는 "의뢰서별 개별 표시" 구조는 유지) — 두 계산 함수가 다시는 따로 갱신되다 어긋나지 않도록 재발 방지
  - `scripts/api.py`: SELECT에 "청구페이지" 컬럼 추가 4곳(예상공급가액·미발행목록·발행목록·부분취소), 부분취소 API의 재계산 함수를 `calc_공급가맵()`→`build_품목행()`으로 교체(실제 확정 시점과 완전히 같은 함수 사용)
  - 검증: `python -m py_compile` 통과. KB증권 93362 재현 — 수정 전 10,995,284원 vs 11,203,574원(차액 208,290원) → 수정 후 정확히 일치. 로컬 MariaDB 전수/샘플 회귀(자재단가 등록 거래처 전수 1,187건+랜덤 500건, 총 1,687건) — 1,467건 변경없음(회귀 없음), 220건 의도된 변경(자재단가·인쇄면 배율 정상 반영), 신버전 `calc_공급가맵()`과 `build_품목행()` 불일치 0건. 로컬 API 서버(포트 8000) 재시작 완료
  - **사무실 PC 배포 완료(2026-08-20)** — `scripts/billing.py`·`scripts/api.py` 반영(DB 스키마 변경 없음, 마이그레이션 불필요) 및 FastAPI 재시작까지 사용자가 직접 완료. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음. 상세: `.claude/plans/bug_공급가액계산_불일치.md`

### 2026-08-21~22
- **신규 공정별 개별 청구 기능 구현 완료 — 로컬 PC 코드·검증까지 완료, 사무실 PC 배포는 대기 중** ✅ (`.claude/plans/plan_공정별단가청구.md`): 당사 생산공정관리시스템이 5월분 `운영통계자료`부터 공정 세분화 컬럼 10개(압착·주소출력·봉입·수작업·중철·제본·무광코팅·유광코팅·에폭시·날개접지)를 추가로 내려주기 시작 — `data/7월운영통계자료.xlsx`(5월분 8,438행) 실데이터 분석으로 이 필드들이 "합산이 아니라 그 공정을 거친 물량이 건수와 동일한 값으로 반복 표시"되는 방식임을 확인(실사례: 96373 제본+수작업, 96299 중철+수작업 — 합산 아님). 사용자 확정 요구사항대로 압착비·봉입(기계)비·수작업비·중철비·제본비 등 9개 품목을 각각 별도 청구 항목으로 신설.
  - DB: `운영통계자료` 신규 컬럼 10개, 신규 테이블 `단가마스터_공정단가`(공정코드 8종 enum, 매칭 자식 테이블 불필요 — 자재단가와 달리 고정 enum이라 자재명 검색 구조 불필요)
  - 데이터 파이프라인: `scripts/data_transform.py`(`MARIADB_컬럼`·`운영통계_행_변환()`)·`scripts/api.py`(`운영통계행` Pydantic 모델, 하위호환 기본값 0) 반영. `preprocess.py`는 컬럼명 기준 읽기라 코드 수정 불필요(실제 재적재 테스트로 확인)
  - 계산: `scripts/billing.py`의 `_작업별_품목누적()`에 신규 파라미터 10개 + 품목 9개 추가. **설계 단계 승인 계획에서 놓쳤던 이중 청구 위험을 구현 중 발견해 수정** — 봉입비를 "봉투 자재사용량"으로 계산하면서 "수작업비"를 별도로 또 청구하면 같은 물량이 두 번 잡히므로, 신규 데이터가 있으면 "봉입"(기계) 컬럼만 정확히 청구수량으로 사용하도록 폴백 로직 재설계(배송계열합>0 신호로 신·구 데이터 분기)
  - 기존 필드 재사용: `단가마스터.봉입단가`("봉입")·`각대대봉투봉입단가`(UI 라벨 "수작업 단가", "수작업")를 그대로 활용 — `.claude/plans/bug_각대대봉투_봉입비_계산.md`(2026-07-22)가 "개별 건 단위 봉투형태 데이터가 들어오면 분리하라"고 예견해뒀던 시점이 이번에 도래
  - 청구서 인쇄 코드: 템플릿(`data/거래명세서_템플릿_base.xlsx`)에 이미 `P/M/E/F/H/BB/AB/D` 범례가 있고 H·BB·AB가 미사용 상태로 예약돼 있었음을 확인 — 수작업비→H, 중철비·제본비→BB(범례에 이미 "중철,제본" 명시), 압착비·주소출력비·무광/유광코팅비·에폭시가공비·날개접지비(6개)→AB(부가사업) 통합 인쇄로 배정(사용자 확정, 템플릿 수정 불필요)
  - API: `billing.build_단가맵()`이 계산에 쓰이는 **8곳**(미발행목록·발행목록·미리보기·거래명세서엑셀·부분취소 등, SKILL-27 대상)에 `공정단가df` 신규 전달 + `운영통계자료`를 읽는 하드코딩 SELECT **9곳**에 신규 10개 컬럼 추가(빠지면 그 화면만 조용히 0으로 계산되는 함정 — 전부 발견·수정), `단가마스터_공정단가` CRUD 엔드포인트 신설
  - 화면: `frontend/components/PricingProcessSection.tsx` 신규(자재단가 UI보다 단순 — 매칭 자재 검색 없이 고정 8종 select). `InvoicePreviewDialog.tsx`는 품목을 서버 응답 그대로 순회 렌더링하는 구조라 **코드 수정 없이 신규 품목 자동 표시** 확인
  - 화면 실사용 검수 중 발견한 부수 버그 2건도 같이 수정: ①자재단가·공정단가 편집창을 열 때 스크롤이 다이얼로그 하단(취소·저장 버튼)까지 안 내려가던 문제(두 컴포넌트 다 "매칭 자재 추가" 시 쓰던 정확한 스크롤 로직을 "편집창 열기" 시점에도 적용하도록 통일) ②신규 단가 최초 추가 시 자재/공정단가 섹션이 안 보이는 건 FK(단가마스터_id)가 아직 없어서인 의도된 동작임을 확인(저장 한 번 누르면 바로 이어서 같은 창에서 추가 가능 — 코드 변경 없음)
  - 검증: 1차(DB 미사용, 실사례 96373·96299 정확히 일치 + 구버전 폴백은 `bug_각대대봉투_봉입비_계산.md`의 실제 과거값(6,090×14=85,260원)과 정확히 일치) + 2차(로컬 MariaDB 마이그레이션 실행 + `7월운영통계자료.xlsx` 재적재[26,774행] 후 실제 DB 데이터로 `build_품목행()`·`calc_공급가맵()` 종단 테스트, 두 함수 결과 일치) + `tsc`·`eslint`·`build` 전부 통과. **사무실 PC 배포는 이번 계획에 포함 안 함 — 별도 승인 후 진행 예정**
- **추가봉입비가 봉투 삽입 없는 작업(반제품·제본단독)에도 장수 전체로 과다청구되던 버그 수정 — 코드 수정·로컬 검증 완료** ✅ (`.claude/plans/bug_추가봉입비_반제품제본단독오차.md`, 위 공정별 단가 청구 기능 화면 검증 중 발견, 그 기능과는 무관 — billing.py 2026-07-19 최초 작성 시점부터 있던 결함): "추가봉입비"(봉투 1개당 기본 1장 넘는 초과 페이지 비용)가 `장수 - 봉입건수`로 계산되는데, 봉투 삽입 이벤트 자체가 없는 작업(반제품 — `apply_반제품_logic()`이 건수를 0으로 만든 행, 또는 제본만 하고 봉투 없이 발송·납품하는 "제본 단독" 작업)에서는 봉입건수가 0이 되면서 장수 전체가 그대로 초과분으로 잡히던 문제(실사례: 96315 "계약보전_맞춤형약관" 1,573,600원 과다청구). 옛 Streamlit 화면의 **표시용** 통계에는 이미 있던 "기준수량 0이면 결과도 0" 가드가 실제 청구 계산(billing.py)에는 처음부터 없었음 — `_작업별_품목누적()`에 "삽입기준수량(신규 데이터는 봉입+수작업, 구버전은 봉입건수)이 0이면 추가용지도 0"으로 가드 추가. 로컬 DB 전체(3개월치) 회귀 스캔 결과 영향 그룹 2,406개(1,268개 의뢰서) 확인 — 개발 단계 DB라 소급 정산 이슈 없음(사용자 확인, 운영 이관 시 거래처마스터 등 데이터 전부 클리어 예정)
- **신규 이슈 기록 1건 (다음 세션, 위 신규공정확장 이슈 완료 후 착수 예정)**: 인쇄면(양면/단면)을 거래처+업무명 단위가 아니라 용지 자재 단위로 관리해야 하는 구조적 문제 — `.claude/plans/bug_인쇄면_자재별관리.md` 신규 생성(코드 변경 없음, 기록만). 실사용 검수 중 발견한 96315 표지출력비 오차(504 vs 503 기대)는 조사 결과 당사 공정관리시스템 원시데이터 입력 오류로 확인돼 코드 조치 불필요 — 다만 "내지는 양면, 표지는 단면"처럼 같은 작업 안에서도 자재별로 인쇄면이 다를 수 있는데 지금 구조는 이를 표현 못 한다는 구조적 개선점만 별도 기록
- **"신규 공정 확장(건수 의미 재정의)" 이슈 — 사용자 판단으로 추가 조사 없이 종료** ✅ (`.claude/plans/plan_신규공정확장_씰링에폭시코팅제본중철.md`, 같은 날 세션에서 문서 불일치 발견 후 처리): `plan_공정별단가청구.md`(2026-08-21)가 이 이슈를 "해소함"으로 표시했다고 기록했으나 실제로는 대상 플랜 파일 상태가 갱신 안 돼 있던 것을 발견 — 사용자에게 확인 요청한 결과, 봉입비 계산(핵심 문제)은 공정별단가청구로 이미 해결됐다고 보고 **추가 조사 없이 종료** 결정. `apply_반제품_logic()`(반제품여부 판정)·청구페이지 계산 3가지 경우의 "건수" 기반 분기는 **코드 조사를 하지 않은 채로 남음** — 신규 공정 거래처에서 이상 징후가 보이면 해당 플랜 파일을 다시 열어 조사 재개할 것.
- **"인쇄면 자재별 관리 + 장수기준/페이지기준 청구단위" 통합 개선 — 구현·검증 완료(로컬 PC만, 사무실 PC 배포는 대기 중)** ✅ (`.claude/plans/plan_출력비_장수페이지기준_인쇄면자재별.md`, 같은 날 후속 세션): 위에서 기록만 해뒀던 `bug_인쇄면_자재별관리.md`와 `plan_장수기준_출력비청구_조사.md` 두 이슈를, 사용자가 "결국 같은 문제"라고 짚어줘 하나의 계획으로 병합 — 용지 자재는 인쇄면(단면/양면)에 따라 "장 수"와 "페이지 수"가 다를 수 있는데(단면=항상 같음, 양면=페이지 수가 장 수의 2배), 거래처마다 어느 쪽을 청구 기준으로 삼는지가 다르다는 점을 "인쇄면"(자재의 물리적 속성)과 "청구단위"(거래처와의 계약 조건, 신규)라는 서로 다른 두 축으로 분리하면 두 문제가 동시에 풀린다는 설계
  - DB: `단가마스터.청구단위 ENUM('페이지기준','장수기준') DEFAULT '페이지기준'`(계약 조건 — 기존 유일한 동작과 동일한 기본값이라 회귀 없음) + `단가마스터_자재단가.인쇄면 ENUM('단면','양면') NULL`(자재 단위, NULL이면 상위 거래처+업무명 값으로 폴백) 신규 컬럼 2개
  - 계산: `scripts/billing.py`의 `_작업별_품목누적()` 출력비 `elif 용지 > 0:` 분기 — 거래처 레벨 배율 하나를 전체 용지 자재에 곱하던 방식에서, 자재마다 (자재별 등록 인쇄면 우선 → 없으면 거래처 레벨 인쇄면) × 청구단위로 개별 배율을 계산하는 방식으로 재작성
  - API: `scripts/api.py` — 단가마스터 신규/수정 Pydantic 모델에 `청구단위` 추가, 자재단가 신규/수정 모델에 `인쇄면`(Optional) 추가. **SKILL-27 대상이었으나 자재단가df를 만드는 공용 헬퍼(`_자재단가df_조회()`) 한 곳만 있어 예상보다 단순**하게 반영됨 — `GET /단가마스터` 응답 조립부(자재단가맵)는 이 헬퍼와 별개의 자체 SELECT라 놓치기 쉬웠던 지점, 발견해서 함께 반영
  - 프론트: `PricingFormDialog.tsx`(청구단위 드롭다운 신규)·`PricingMaterialSection.tsx`(코드="출력비" 자재에만 인쇄면 선택 노출)·`PricingMasterTable.tsx`·`Dashboard.tsx`·`serverMappers.ts`·`PricingMaster.tsx` 반영
  - 검증: `py_compile`·`tsc`·`eslint`·`build` 전부 통과. `_작업별_품목누적()` 단위 검증 4케이스(회귀 없음·장수기준 배율1 고정·자재별 인쇄면 혼합·인쇄면 키조차 없는 구버전 rates 하위호환) 전부 기대값 일치. **96315(삼성화재해상보험) 실제 DB로 종단 검증** — 변경 전(자재별 인쇄면 미설정) 값이 기존 알려진 값(표지 504장·내지 125,384장, `bug_인쇄면_자재별관리.md` 기록과 동일)과 완전히 일치해 회귀 없음 확인 → 실제 "표지출력비" 자재단가 행에 인쇄면='단면'을 임시로 설정해 재계산하니 수량이 504→252로 정확히 절반이 되고 내지(양면)는 그대로 유지, 총합계 차액도 252×150원과 정확히 일치하는 것까지 확인 → **검증 직후 즉시 원상복구(인쇄면=NULL)**, DB에 테스트 흔적 없음
  - **사무실 PC 배포는 다음 세션에서 사용자 확인 후 진행 예정** — 실제 "장수기준" 거래처·자재별 "인쇄면" 값은 코드가 판단할 수 없는 실제 계약/물리 조건이라, 배포 후 사용자가 단가마스터 화면에서 해당 거래처를 직접 설정해야 함
- **단가 미등록으로 품목이 조용히 누락되는 문제 — 감지·확정 차단 기능 구현 완료(같은 날, 후속 요청, 로컬 PC만·사무실 PC 배포 대기 중)** ✅ (`.claude/plans/plan_단가미등록_품목누락_감지.md`): 사용자가 미리보기 화면(왼쪽 "시스템 자동계산 원본" 표)이 **단가가 등록된 품목만 보여준다**는 걸 지적 — 실제 생산 실적(장수·자재사용량·공정 수량)이 있어도 대응 단가가 하나도 없으면 그 줄 자체가 원본 표에 생기지 않아, 2026-08-17에 고친 "미분류"(원본엔 있지만 오른쪽으로 안 옮겨진 경우, 96,960원 누락 사례)와 성격은 같지만 원인이 다른 새로운 누락 경로였음. 조사 결과 `scripts/billing.py`의 모든 계산 함수(`_작업별_품목누적()`의 `항목계산` 루프, `_자재별_처리()`, `_자재비례배분_처리()`)가 "단가>0이고 수량>0"일 때만 줄을 추가하는 구조라, 이 게이트를 통과 못하면 그 수량이 어디에도 안 남고 조용히 사라지는 게 확인됨
  - `_작업별_품목누적()`·`_자재별_처리()`·`_자재비례배분_처리()`·`build_품목행()`에 선택 인자 `미등록수집=None`(리스트) 추가 — 안 넘기면(기존 모든 호출부) 100% 동일하게 동작(회귀 없음), 넘기면 위 게이트를 통과 못한 경우마다 `{"품목","작업","수량"}`을 수집. 별도의 병렬 집계 함수를 새로 만들지 않고 `build_품목행()`이 이미 하던 동일한 작업별 순회에 얹는 방식으로 단순화(SKILL-42류 함정 회피)
  - `scripts/api.py`의 `/거래명세서미리보기`가 이 리스트를 응답에 `단가미등록` 필드로 추가(기존 `수량불일치` 옆)
  - `InvoicePreviewDialog.tsx` — "미분류"와 동일한 확정 차단 패턴(`handleConfirmClick()`이 단가미등록→미분류 순서로 체크, `ConfirmDialog`로 "그래도 확정"/"미리보기로 돌아가기") 추가 + 미리보기 단계에서도 바로 보이도록 빨간색 안내 배너도 함께 추가(미분류는 원본 표 체크 표시로 보이지만 이번 문제는 원본 표에 줄 자체가 없어 배너가 유일한 사전 인지 경로)
  - 검증: 단위 검증 6케이스(압착비·봉입비·출력비 폴백분기·출력자재비 각각 미등록 감지 + 정상 케이스 회귀 없음) + 기존 출력비 회귀 4케이스 재검증 전부 통과. **96315 실제 DB 종단 검증** — 정상 상태 단가미등록 0건 확인 후, "표지출력비" 자재단가를 0원으로 임시 변경해 해당 줄(504장·75,600원)이 원본에서 사라지고 `단가미등록`에 정확히 잡히는 것을 확인, 검증 직후 즉시 원상복구(별도 연결로 재확인). 검증 중 "내지제작비" 자재에 인쇄면='양면'이 실제로 설정돼 있는 것을 발견 — 사용자가 앞선 화면 테스트 체크리스트를 따라 실제로 저장해둔 값임을 확인하고 그대로 둠(제 임시 테스트 잔여물 아님)
  - 사무실 PC 배포는 위 인쇄면/청구단위 기능과 함께 다음 승인 시 진행 예정
- **조건식 편집창(ConditionRuleModal) 값 입력칸 — 값이 있는 상태에서 클릭해도 전체 후보가 뜨도록 수정(같은 날, 후속 제보, 프론트엔드 전용)** ✅: 조건식을 "복사"해 기존 값이 채워진 채로 다른 값을 고르려 하면, 최종청구품명·작업구분(조)·조건 값(자재명·작업명 등) 입력칸이 `<input list>`+`<datalist>`(브라우저 기본 자동완성) 방식이라 이미 있는 값과 매칭되는 후보만 필터링돼 전체 목록이 안 뜨는 문제(값을 지워야만 전체가 보임) — 2026-07-19 다른 화면(PricingFormDialog.tsx 등)에서 똑같은 이유로 이미 만들어둔 `EditableCombo`(클릭/포커스 시 입력값과 무관하게 항상 전체 후보부터 표시)로 3곳 전부 교체. 단가 입력칸만 숫자 키패드·스핀 버튼이 더 중요해 기존 `<input type="number">` 그대로 유지(제보도 자재명·작업명 중심이었음). `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과. 프론트엔드 전용이라 사무실 PC 배포 대상 아님.
- **출력자재비(F)에도 자재별 인쇄면 확장(같은 날, 후속 요청)** ✅ (`.claude/plans/plan_출력비_장수페이지기준_인쇄면자재별.md` "Phase 6"): 사용자가 실제 발행된 삼성화재해상보험 청구서를 캡처해 제보 — "내지제작비"(F, 5원)가 규격 "page"로 인쇄돼 있어 페이지 단가로 등록된 건데, 로컬 96315로 대조해보니 F는 항상 raw 장 수(62,692장)로만 계산되고 있어 **절반만 청구되는 구조적 결함**을 발견. `_작업별_품목누적()`에 `_출력자재비_배율()` 신규 추가 — 단, 출력비(P)와 달리 **거래처 레벨 인쇄면으로 자동 폴백하지 않고, 그 자재에 인쇄면을 명시적으로 등록한 경우에만** 적용(미등록이면 절대다수 거래처와 동일하게 항상 장 수 그대로 — 이미 정상인 다른 거래처의 F 자재가 거래처 레벨 인쇄면 때문에 조용히 2배 청구되는 회귀를 원천 차단). `PricingMaterialSection.tsx`의 인쇄면 드롭다운을 "출력비" 외 "출력자재비"에도 노출(안내 문구는 폴백 여부 차이를 반영해 분리). 검증: 단위 검증 4케이스 통과 + 96315 실제 DB로 내지제작비(id=37)에 인쇄면='양면' 임시 설정 → 수량 62,692→125,384(정확히 2배), 금액 +313,460원(125,384×5원과 정확히 일치) 확인 후 즉시 원상복구, 다른 자재(표지제작비, 인쇄면 미설정)는 불변 확인. 사무실 PC 배포는 위 항목과 함께 대기 중.
- **우편요금 관리 — 미발행 목록 입력 + 거래명세서 미리보기 자동 반영 구현 완료(같은 날, 후속 요청, 로컬 PC만, 사무실 PC 배포 대기 중)** ✅ (`.claude/plans/plan_우편요금관리.md`): 마케팅 담당자가 우체국 발송 시 발생하는 우편요금을 지금까지 시스템 밖에서 수기 관리하고 있었는데, 월 1회 업무는 기존 "새 행 추가"(2026-08-12)로 해결되지만 매일 발송하는 업무는 영업일 수만큼 매번 수기 입력이 필요해 감당이 안 됨 — 미발행 목록 화면에서 의뢰서 단위로 우편요금을 입력해두면 거래명세서 요청 시 미리보기 원본에 자동으로 반영되도록 구현
  - DB: 신규 테이블 `업무의뢰서_우편요금`(의뢰서번호 PK, 금액) — 실시간 수신+주기적 TRUNCATE 재적재 대상인 `운영통계자료`와 분리해 재적재해도 입력값이 안전하도록 설계, 의뢰서가 취소돼 미발행 목록으로 돌아와도 값 보존(거래명세서품명이력과 동일한 관례)
  - 계산: `scripts/billing.py`의 `build_품목행()`·`calc_공급가맵()`에 선택 인자 `우편요금맵` 추가(기존 호출부는 인자 안 넘겨 100% 회귀 없음). 우편요금은 특정 작업명에 속한 비용이 아니라 의뢰서 단위 정액 비용이라 작업명별 루프 밖에서 별도 처리 — **거래처+업무명 단위로 합산**해 업무명 개수만큼 줄이 생기도록 구현(사용자 확정). 원본품목 타입의 `단가`가 nullable이 아니라 프론트 타입을 안 건드리기 위해 수량=1·단가=합계금액으로 표현(요청하신 "수량·단가 불필요, 금액만"은 화면에서 수량 1·단가=금액이 보이는 형태로 절충). Excel 구분(코드) 열은 **"PO"**로 표시(처음엔 "코드 없이 품목명만"으로 검토했으나 사용자가 "PO"로 확정) — 템플릿 범례에 없는 새 코드지만 자유 텍스트 셀이라 템플릿 수정 불필요
  - API: 신규 `PUT /업무의뢰서/{request_no}/우편요금`(upsert), `GET /미발행목록` 응답에 `우편요금` 필드 추가. 회귀 위험 재확인 중 발견한 유사 호출부(`GET /예상공급가액`·`GET /발행목록`·부분취소 재계산)까지 전부 일관되게 반영해 "예상금액"과 실제 확정 금액이 어긋나지 않도록 함(2026-08-20 `bug_공급가액계산_불일치.md`의 교훈 재적용)
  - 프론트: `InvoiceSelectionTable.tsx`(수천 행 가상 스크롤 화면)에 편집 가능한 우편요금 입력칸 신규 — blur/Enter 시에만 저장해 타이핑마다 네트워크 요청이 안 나가게 함. 구현 중 커스텀 입력 컴포넌트 이름이 한글이라 `react-hooks` 린트가 컴포넌트로 인식 못 해 `useState` 사용이 막히는 문제 발견 → 영문 컴포넌트명(`PostageInput`)으로 해결. 외부 값 동기화는 `useEffect`+`setState` 대신 `key` 재마운트 패턴 사용(SKILL-24, 이 프로젝트 린트가 전자를 막음)
  - 검증: `py_compile`·`tsc`·`eslint`·`build` 전부 통과. 로컬 MariaDB 마이그레이션 실행 완료. **실제 DB 종단 검증**(삼성화재해상보험 96319·96320·96337) — 같은 업무명 의뢰서 2건의 우편요금이 정확히 합산되고 다른 업무명은 별도 줄로 분리, 예상공급가액에도 정확히 반영됨을 확인 후 테스트 데이터 즉시 삭제
  - 사무실 PC 배포는 다음 세션에서 위 항목들과 함께 진행 예정

### 2026-08-23
- **우편요금 입력칸 천단위 콤마 표시 추가(프론트엔드 전용)** ✅: `InvoiceSelectionTable.tsx`의 우편요금 입력칸에 `InvoicePreviewDialog.tsx`의 기존 수량·금액 입력칸과 동일한 콤마표시()/콤마숫자파싱() 왕복 변환 패턴(2026-08-09 SKILL)을 적용 — 타이핑할 때마다 천단위 콤마가 실시간으로 붙어 보이고, 값이 0이면 빈칸(placeholder "0")으로 표시. 서버 저장 시점(blur/Enter)은 그대로 유지, 저장 로직 변경 없음. `npx tsc --noEmit`·`npx eslint .`·`npm run build` 통과. 프론트엔드 전용이라 사무실 PC 배포 대상 아님.
- **2026-08-21~22 작업분 중 4건 사무실 PC 배포 완료** ✅ (`.claude/plans/plan_2026-08-22_사무실PC배포체크리스트.md`): 사용자가 `scripts/billing.py`·`api.py`·`init_db_mariadb.py`·`data_transform.py` 반영 + DB 마이그레이션(운영통계자료 신규 컬럼 10개·단가마스터_공정단가 테이블·단가마스터.청구단위·단가마스터_자재단가.인쇄면) + FastAPI 재시작까지 직접 완료(순조롭게 끝남, 특이사항 없음). 배포된 4건: ①신규 공정별 개별 청구(압착비·중철비·제본비 등 9개 품목) ②추가봉입비 반제품·제본단독 과다청구 버그 수정 ③출력비 장수기준/페이지기준+자재별 인쇄면(출력자재비(F) 확장 포함) ④단가 미등록 품목 누락 감지·확정 차단.
- **우편요금 관리(5번째, 같은 날 후속) 사무실 PC 배포 완료** ✅: `scripts/billing.py`·`api.py`·`init_db_mariadb.py` 반영 + DB 마이그레이션(신규 테이블 `업무의뢰서_우편요금` 생성) + FastAPI 재시작까지 사용자가 직접 완료. **2026-08-21~22 작업분 5건 전체 사무실 PC 배포 종료** — 로컬·사무실 PC 코드·DB 완전 동기화 상태, 남은 미배포 항목 없음.
- **봉입비(M)가 수작업 물량까지 중복 청구하던 버그 수정 — 코드 수정·검증 완료(로컬 PC만, 사무실 PC 배포 대기 중)** ✅ (`.claude/plans/bug_봉입비_수작업중복청구.md`, 사용자가 의뢰서 97509(현대해상 자동차증권) 미리보기 화면에서 "중봉투봉입 수량에 수작업 수량이 중복된 것 같다"고 제보): 2026-08-21 "봉입비는 기계 물량만 청구"(`봉입_기준수량 = 봉입`, `배송계열합>0`일 때)라고 설계는 해뒀지만, 그 봉투 자재에 개별 자재단가(`단가마스터_자재단가`)가 등록돼 있으면 `_자재별_처리()`가 이 상한을 무시하고 자재의 **실사용량 원본(기계+수작업 합계)**을 그대로 청구줄에 써버려 설계 의도가 무력화돼 있었음 — 수작업 물량이 봉입비(M)와 수작업비(H) 양쪽에서 중복 청구됨. `_작업별_품목누적()`의 봉입비 호출을 `_자재별_처리()`에서 이미 있던 `_자재비례배분_처리()`(2026-08-17 출력비에서 검증된 "총량 유지+비율배분" 헬퍼)로 교체(`scripts/billing.py` 348행, 함수명 1줄 교체) — 총량과 자재dict 합계가 같은 기존 경로는 수학적으로 결과가 동일해 회귀 없음. **영향 범위는 봉입비에 자재단가가 등록된 3개 거래처+업무명 조합뿐**(현대해상 자동차증권·현대해상 만기안내/만기예고·삼성화재해상보험 계약보전(PLK)웰컴팩) — 로컬 MariaDB 969건 회귀 스캔(구버전 vs 신버전 `build_품목행()` 총합계 비교, `git show HEAD` 방식) 결과 정확히 이 3개 조합 66건만 금액이 줄어들고(중복분 제거) 나머지 903건은 완전히 동일함을 확인. 66건 전부 아직 "거래명세서 요청"조차 들어간 적 없는 순수 미발행 상태임을 확인해(`거래명세서_의뢰서` 조회 0건) **재정산 필요 없음**. `python -m py_compile` 통과. **후속 수정(같은 날)**: 사용자가 미리보기 화면에서 봉입비 수량이 "2,016.808건"처럼 소수점으로 나온다고 재차 제보 — 비율배분(`_자재비례배분_처리()`) 특성상 자재가 여러 종류면 소수점이 남는데, "건수"는 정수여야 자연스러움. `_자재비례배분_처리()`에 선택 인자 `정수반올림`(2026-08-23, 기본값 False — 기존 출력비 호출은 영향 없음)을 추가해 최대잔여법(largest remainder)으로 총량은 정확히 보존한 채 각 줄을 정수로 반올림하도록 하고, 봉입비 호출에만 `정수반올림=True` 적용. 97509 재검증(2,016.808+2,111.192 → **2,017+2,111=4,128 정수**), 969건 재회귀 스캔(동일하게 66건만 영향, 나머지 903건 불변) 확인 완료. 사용자가 "총합계 수량을 고려한 반올림·절삭 체크도 하나 추가하라"고 요청 — `_자재비례배분_처리()` 최대잔여법 단위 테스트 7종(홀수 총량·자재 1종·기본단가 혼합·총량 0·자재수>총량·잔여 동점 등) 전부 "합계=반올림된 총량 정확 일치+전부 정수" 통과 + 영향받는 3개 거래처+업무명 조합 **전수(샘플 아닌 146개 의뢰서·318개 의뢰서×작업 조합 전체)** 검증에서 봉입비 수량 합계가 원본 데이터 기대값과 오차 0으로 전부 일치함을 확인. **후속 질문(같은 날)**: "소수 첫째자리가 둘 다 0.5면 어떻게 되나" — 재현해보니 총량은 항상 정확했지만 동점일 때 어느 자재가 반올림되는지가 단가와 무관하게 딕셔너리 순서(업무적 의미 없음)로 결정되고 있었음. 사용자 확정에 따라 **동점 시 단가가 낮은 자재에 우선 반올림**하도록 정렬 키에 단가를 2차 기준으로 추가(`_자재비례배분_처리()`). 동점 재현 테스트(순서 바꿔도 항상 저단가 승리 확인)·기존 단위 테스트 7종·3개 조합 전수(318건)·969건 회귀 스캔 전부 재실행해 회귀 없음 재확인. 사무실 PC 배포는 다음 승인 시 진행 예정.
- **미발행목록·발행요청목록 "예상공급가액"이 "부가세 포함" 거래처에서 부가세만큼 부풀려 표시되던 버그 수정 — 코드 수정·검증 완료(같은 날, 로컬 PC만)** ✅ (`.claude/plans/bug_예상공급가액_부가세미반영.md`, 위 봉입비 버그 검증 중 사용자가 D-202608-00093(현대해상, 19개 의뢰서)에서 발견 — 조건식 수정 없이 그대로 확정했는데 예상공급가액 18,537,330원 vs 청구공급가액 16,852,782원, 1,684,548원 차이 제보): 원인은 `calc_공급가맵()`의 "합계"가 부가세 미반영 원시값인데, `단가마스터.부가세구분='포함'`(단가에 부가세 내장) 거래처에서는 실제 확정 저장 로직(`build_품목행()`→`billing.부가세_표시분리()`, 2026-08-12 도입)이 이 원시값을 역산해 공급가액·부가세로 분리하는 반면 `/미발행목록`·`/발행목록`은 이 역산 과정 없이 원시값을 그대로 "예상공급가액"으로 써서 부가세만큼(총액의 약 9~10%) 부풀려 보였음(부가세구분='별도' 거래처는 원시값=공급가액이라 영향 없었음 — 그래서 지금까지 발견 안 됨). `scripts/api.py`에 헬퍼 `_예상공급가액_표시()` 신규 추가(`billing.부가세_표시분리()`로 확정 로직과 동일하게 계산) — `/미발행목록`·`/발행목록` 2곳 교체. **영향 범위가 예상보다 훨씬 컸음**: 처음엔 오늘 고친 봉입비 자재단가 등록 3곳만의 문제로 보였으나, 실제로는 "부가세구분=포함"으로 등록된 모든 거래처(BC카드·교보생명보험·삼성화재해상보험·현대해상 등 다수 업무명)에서 2026-08-12 부가세 표시분리 도입 이후 계속 있던 버그였음(청구공급가액=확정 저장값은 처음부터 정확했어서 실제 청구 금액 자체엔 영향 없음, "예상" 표시 화면에만 국한). D-202608-00093 재계산 결과 차이가 1,684,548원→665원으로 축소(99.96% 해소) — 남은 665원은 오늘 앞서 고친 봉입비 정수반올림이 개별의뢰서 19건 계산과 결합계산 사이에 만드는 반올림 잔여차(자연스러운 예상치 오차, 실제 청구액엔 무관)임을 봉입비 항목만 따로 대조해 확인. 1,500건 샘플 회귀 스캔(786개 의뢰서) — 부가세구분='포함' 422건만 총액÷1.1 비율로 정확히 변경, '별도' 364건은 완전히 동일(회귀 없음). **후속 개선(같은 날)**: 남은 665원 잔여차를 사용자에게 설명하자 "미리보기에서 저장된 조건식 계산값을 그대로 쓰면 안 되냐"는 제안을 받음 — 확인해보니 `거래명세서_품목`에 2026-08-18부터 이미 저장 중이던 구분='기준'(조건식 적용 후·손대기 전) 스냅샷 합계에 부가세 분리만 적용하면 확정 저장값과 **완전히 정확히 일치**(665원→0원)함을 확인. 처음엔 "조건식을 라이브로 반영"하는 대안도 검토했으나 미발행 3,301건 규모에서 20초가 걸려(실측) 성능상 불가능했는데, 이미 저장된 값을 읽기만 하면 되는 이 방법은 그 문제 자체가 없음. `scripts/api.py`의 `GET /발행목록`을 기준 스냅샷 기반으로 교체(기준 없는 구버전 거래명세서는 기존 라이브 계산 폴백), `/미발행목록`은 조건식 결과 자체가 없어(요청 전) 그대로 유지. 화면 표시도 함께 정리 — 미발행목록 "예상공급가액"에 "실제 청구 금액과 다를 수 있음" 안내 툴팁 추가, 발행요청목록·발행완료 헤더를 "수정전공급가액"·"확정공급가액"으로 개명(`InvoiceSelectionTable.tsx`·`InvoiceIssuedLevel1Table.tsx`). D-202608-00093·00088 둘 다 재검증해 차이 0원 확인, `tsc`·`eslint` 통과. 사무실 PC 배포는 다음 승인 시 진행 예정.
- **발행 취소 시 "거래명세서 전체 총액"이 개별 의뢰서 예상공급가액으로 잘못 복사되던 회귀 버그 수정(같은 날, 로컬 PC만)** ✅ (`.claude/plans/bug_취소시_예상공급가액_전체값복사.md`, 위 "수정전공급가액" 변경의 직접적인 회귀 — 사용자가 미발행목록에서 현대해상 자동차증권 19건 전부 예상공급가액이 16,852,782원으로 동일하게 나온다고 제보): `GET /발행목록`을 직접 호출해 백엔드는 정상(42건 전부 다른 값)임을 확인, 원인은 `frontend/components/Tab4IssuedList.tsx`의 발행 취소 처리가 `발행행.예상공급가액`(오늘 "거래명세서 전체 총액"으로 의미가 바뀐 필드)을 그대로 개별 `미발행행.예상공급가액`으로 복사하고 있었던 것 — 취소돼 미발행으로 돌아간 19건 전부에 "19건 전체 몫"이 잘못 붙음. 게다가 `Tab4.tsx`가 "거래명세서 관리" 최상위 탭 재진입 시에만 미발행목록을 재조회해(2026-08-09 설계) 발행요청목록→미발행목록처럼 서브탭만 전환하면 잘못된 값이 화면에 남아있었음. `Tab4IssuedList.tsx`는 취소 시 예상공급가액을 `null`로(신뢰 불가), `Tab4.tsx`는 재조회 로직을 `refreshInvoice` 콜백으로 추출해 취소 직후에도 호출하도록 수정. 실제로 만들어졌던 거래명세서(D-202608-00094)는 `build_품목행()`으로 독립 재계산해보니 저장값과 정확히 일치(우연이 아니라 취소 전 D-202608-00093과 실제로 같은 19개 의뢰서를 재요청한 것)해 재정산 불필요함도 확인. `tsc`·`eslint` 통과. **후속 발견(같은 날)**: 재요청(D-202608-00095) 후 사용자가 "수정전공급가액이 여전히 미발행목록의 원본으로 보인다"고 재차 제보 — 실제 DB·API 직접 호출로 백엔드는 완전히 정상(`거래명세서_품목` 구분='기준' 합계=18,538,060, `/발행목록`도 정확한 16,852,782 반환)임을 재확인, 원인은 **또 다른** 낙관적 업데이트 버그: `Tab4Invoice.tsx`의 "확정" 처리가 미발행행(개별·조건식 미적용 값)을 스프레드해 새 발행행을 만들면서 `예상공급가액` 필드를 안 덮어써, 레벨1 그룹 테이블(`issuedGrouping.ts`)이 19개의 서로 다른 개별값을 합산한 16,852,117원을 잘못 표시(취소 버그와 같은 클래스). `Tab4Invoice.tsx`는 확정 시에도 `예상공급가액: null`로, 신규 `frontend/app/api/invoice-issued-list/route.ts`(`/발행목록` 프록시)와 `Tab4.tsx`의 `refreshIssued()` 콜백을 추가해 확정 직후·탭 재진입 시 서버 값으로 즉시 덮어쓰도록 수정 — `serverMappers.ts`에 `mapIssuedRow()`도 신규 추출(기존 `app/page.tsx` 수동 매핑에서 누락돼 있던 우편요금 필드도 함께 바로잡음). `tsc`·`eslint`·`npm run build` 전부 통과, 실행 중인 로컬 API 서버에 진단용 토큰으로 직접 호출해 백엔드 응답이 처음부터 정확했음을 재확인. **진짜 근본 원인(같은 날, 후속)**: 위 낙관적 업데이트 수정을 전부 반영한 뒤에도 시크릿 창(캐시·세션 완전 배제)에서 다시 확정해도 320,202,858원(19×16,852,782)이 재현된다는 제보로 재조사 — `frontend/lib/issuedGrouping.ts`의 `build레벨1그룹()`이 "예상공급가액"을 **합산(sum)** 하고 있었음을 발견. 바로 위 "확정공급가액"은 이미 `lines[0].확정공급가액`(거래명세서번호 단위 값이라 그룹 내 모든 라인이 동일 — 대표값 하나만 사용, 합산 안 함)으로 올바르게 처리돼 있었는데, "예상공급가액"만 이 패턴을 안 따르고 있었던 것 — `bug_예상공급가액_부가세미반영.md` 이전(예상공급가액이 "의뢰서 개별 몫"이던 시절)엔 합산이 맞았지만, 그 수정으로 API가 "거래명세서 전체 총액을 모든 의뢰서 라인에 동일하게 반복"하는 방식으로 바뀐 뒤로는 의뢰서 건수만큼 부풀려지는 버그가 됐음. 지금까지 고친 낙관적 업데이트 수정들은 전부 정당한 개선이었지만, 서버가 정확한 값을 줘도 이 그룹화 단계에서 다시 부풀려졌기 때문에 효과가 없었던 것. `예상공급가액: sum(...)` → `lines[0].예상공급가액`(확정공급가액과 동일한 "대표값" 패턴)으로 수정, `tsc`·`eslint`·`npm run build` 통과. **사무실 PC 배포 완료(2026-08-23, 같은 날)**: `scripts/billing.py`(DB 스키마 변경 없음) 반영 + FastAPI 재시작까지 사용자가 직접 완료. 이번 세션 3건(봉입비 중복청구·예상공급가액 부가세미반영·발행목록 낙관적 업데이트+그룹집계) 전부 GitHub(`e87494d`)에 커밋·푸시 완료 — 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음.

### 2026-08-24
- **자재사용현황 5~7월 자료 갱신 — 로컬·사무실 PC 반영 완료** ✅: `data/자재사용현황.xlsx`가 5~7월 신규 자료로 교체됨(불필요 컬럼 3개 삭제 확인 — `업무의뢰서자재`·`미존재`·`미사용`, `작업내역서자재`→`자재코드`는 이름만 변경돼 기존 코드가 그대로 인식). 분석 중 신버전에 새 자재종류 **"반제품"**(약관홀더 등 61건)이 처음 등장한 것을 발견 — `billing.build_자재map()`이 이미 `봉투·용지·삽지`만 청구 계산에 반영하는 구조라 **코드 수정 없이 자동 제외**됨을 확인(사용자 확정: 청구 대상 아님, 그대로 둠). 자재종류·자재코드·자재명이 전부 빈 행 7건(고객사="보이드(Void)")은 DB `자재사용현황.자재종류` `NOT NULL` 제약에 걸려 그대로 반영 불가 — `scripts/preprocess.py`의 `load_자재_raw()`에 결측 제외 필터 1줄 추가(제외 건수 콘솔 출력). `python scripts/preprocess.py`로 로컬 MariaDB 반영·검증 완료(운영통계자료 26,774건, 자재사용현황 12,717건, 날짜범위 2026-05-04~07-30, 두 테이블 정확히 일치) → 같은 파일·코드로 **사무실 PC도 반영 완료**, 배포 직후 두 PC 행수가 다르게 보인다는 제보(로컬 12,571 vs 사무실 12,818)가 있었으나 실제로는 HeidiSQL 테이블 목록 패널의 "행 수"가 InnoDB 근사치라 발생한 표시상 차이였고, 양쪽 다 `SELECT COUNT(*)`로 직접 조회하면 정확히 일치함을 확인(SKILL-45, 코드 결함 아님).
- **거래처명 자동완성 검색창(EditableCombo) 필터·위치 개선 — 검증 완료(프론트엔드 전용)** ✅: 담당자관리 화면에서 "현대해상"까지 입력한 뒤 다시 클릭(스크롤 등)하면 필터가 통째로 풀려 무관한 거래처 전체 목록이 다시 뜨는 문제 제보 — `EditableCombo.tsx`가 클릭/포커스 시 입력값과 무관하게 항상 "전체 목록"을 보여주도록 돼 있던 것을(2026-07-19에 다른 문제를 고치려고 넣었던 설계) "현재 입력값 기준 필터"로 변경(값이 비어있으면 filter()가 자연히 전체를 보여줘 2026-07-19 문제도 그대로 해결됨, 클릭 시 `el.select()`로 기존 텍스트가 전체 선택되니 바로 새로 타이핑해도 문제 없음). 이어서 "이름을 모르고 훑어봐야 할 때 목록이 너무 짧고 화면 아래쪽에 있으면 잘린다"는 피드백으로, `PricingMaterialSection.tsx`가 2026-08-16에 이미 검증해둔 위/아래 자동반전+가변 높이 드롭다운 로직(`계산_드롭다운위치()`)을 공용 함수(`frontend/lib/dropdownPosition.ts`, SKILL-46)로 추출해 `EditableCombo`에도 적용(최대 높이 192px→280px). `tsc`·`eslint`·`build` 통과, 프론트엔드 전용이라 사무실 PC 배포 대상 아님.
- **역발행 거래처 표시 기능 구현 — 로컬·사무실 PC 반영 완료** ✅ (`.claude/plans/plan_역발행거래처표시.md`): 고객사가 거래명세서를 우리 쪽으로 역으로 발행하는("역발행") 거래처를 거래처마스터에서 체크할 수 있게 신규(`역발행` 컬럼, 기본값 비워둠) — 체크된 거래처 건은 발행요청목록·발행완료·거래처마스터 목록에 배지로 표시. 사용자 요청으로 추가된 요구사항: 역발행 거래처의 거래명세서가 생성되면(`POST /거래명세서요청`) 담당자 확인 없이 바로 발행되지 않도록, 2026-08-12에 "거래처 승인 대기"용으로 만들어둔 게이트(`거래명세서.발행가능`)를 생성 시점부터 자동으로 0으로 시작시킴 — 화면의 "승인대기" 표시·발행 차단(409)·체크박스 켬/끔은 기존 코드를 그대로 재사용(신규 로직은 "역발행이면 초기값 0" 한 곳뿐). `scripts/api.py`·`scripts/init_db_mariadb.py`(역발행 컬럼 마이그레이션)·프론트엔드(`ClientFormDialog.tsx` 체크박스, `ClientMasterTable.tsx`·`InvoiceIssuedLevel1Table.tsx`·`InvoiceIssuedLevel2Table.tsx` 배지, `issuedGrouping.ts`) 구현, `tsc`·`eslint`·`build` 통과 + 로컬 MariaDB 임시 테스트 거래처로 종단 검증(역발행=1→발행가능_초기값 0, 역발행=0→1, 발행목록 조회 매핑 정상 확인 후 즉시 삭제) 완료. **사무실 PC 배포 완료(2026-08-24)** — `scripts/api.py`·`init_db_mariadb.py` 반영 + 마이그레이션 + FastAPI 재시작까지 사용자가 직접 완료(체크리스트: `.claude/plans/plan_2026-08-24_사무실PC배포체크리스트.md`). 로컬·사무실 PC 코드·데이터 동기화 상태, 남은 미배포 항목 없음.

### 2026-08-25
- **버그 3건 수정 + 공정단가 "비고" 미리보기 표시 — 사무실 PC 배포 완료** ✅ (`.claude/plans/plan_공정단가_비고_미리보기표시.md`): ①공정별 단가(`PricingProcessSection.tsx`) 수정 화면에서 select 옵션에 현재 값이 중복으로 들어가 React "duplicate key" 오류가 나던 버그 수정(프론트엔드 전용) ②`billing.자재수량_불일치_목록()`(2026-08-17 작성, 신규공정 도입 이전 로직)이 봉투를 아예 안 쓰는 신규공정 작업(제본 등 책 제작류)에도 "봉투 자재이력 없음" 오탐 경고를 띄우던 문제 — `_작업별_품목누적()`이 이미 쓰는 "배송계열합" 판정 기준을 동일 적용해 수정, 로컬 MariaDB 1,500건 회귀 비교(구버전 대비 602건 오탐 제거, 신규 오탐 0건) 검증 완료 ③공정단가 등록 시 입력한 "비고"가 거래명세서 미리보기 원본 표에 자재명과 같은 자리·스타일로 표시되도록 신규 구현(`_공정비고` 곁가지 인덱스, "미등록수집"과 동일한 회귀 없는 선택 인자 패턴). `py_compile`·`tsc`·`eslint` 통과, 실제 DB(강남대성수능연구소 공정단가 비고)로 종단 검증. **사무실 PC 배포 완료** — `scripts/api.py`·`scripts/billing.py` 반영(DB 마이그레이션 불필요, 기존 컬럼만 사용) + FastAPI 재시작, 사용자가 브라우저에서 ① duplicate key 오류 해소 직접 확인.
- **로컬 PC 거래처마스터·단가마스터(전체)·조건식·담당자마스터 → 사무실 PC 이관 완료** ✅: 사무실 PC가 아직 거래처·단가·조건식 데이터를 자체적으로 등록해가며 테스트 중이던 단계라, 로컬 PC에서 그동안 쌓인 실사용 데이터(거래처마스터 12건, 단가마스터 31건+자재단가 32건/매칭 45건+공정단가 5건, 청구품목규칙 35건, 청구품목통합규칙 61건, 통합시트설정 3건, 담당자 5건+담당거래처 6건)를 사무실 PC로 그대로 이관(로컬 데이터로 덮어쓰기, 사용자 확정). 이관 SQL(`output/거래처단가조건식_로컬데이터_이관_2026-08-25.sql`)은 단가마스터→자재단가→매칭, 단가마스터→공정단가, 담당자→담당거래처 같은 FK 계층을 `LAST_INSERT_ID()` 세션 변수로 체이닝해 사무실 PC의 auto_increment 값과 무관하게 안전하게 연결되도록 생성. 검증 중 통합시트설정에서 실제 거래처가 아닌 override 기능 테스트 잔여 데이터("테스트오버라이드QA") 1건을 발견해 제외. 사무실 PC 실행 중 `거래처마스터` PRIMARY KEY 충돌(BC카드 등 사무실 PC에 이미 등록된 거래처 존재) 발생 — 사용자가 "로컬 데이터로 덮어쓰기"를 확정해, 대상 6개 테이블(거래처마스터·단가마스터·담당자·청구품목규칙·청구품목통합규칙·통합시트설정)을 먼저 비운 뒤(다른 테이블과의 FK 없음을 스키마 전수 확인해 거래명세서 등 실 발행 이력에 영향 없음 확인) 로컬 데이터로 다시 채우는 방식으로 스크립트 수정. 임시 테스트 DB에 사전 충돌 데이터(BC카드 등)를 재현해 실제로 정상 교체되는지까지 실행 검증(2건) 후 사용자가 사무실 PC에서 최종 실행해 완료 확인.

### 2026-08-29
- **재료비(용지·봉투·삽지) "무상(고객사 제공)" 체크박스 — 사무실 PC 배포 완료** ✅ (`.claude/plans/plan_재료비_무상표시.md`): 2026-08-22 "단가미등록 감지" 기능이, 고객사가 자재를 직접 제공해 원래부터 무상인 항목(의뢰서 97531, 삼성화재해상보험(주)·모니터링안내장 봉입자재비)을 "단가 등록을 깜빡한 실수"로 오판해 경고를 띄우던 문제 — DB에 "아직 안 정한 0"과 "의도적으로 무상인 0"을 구분할 방법이 없던 게 근본 원인이었음. 단가마스터에 `용지제작무상`·`봉투제작무상`·`삽지제작무상`(TINYINT(1), 기본 미체크 — 대부분 유상이므로) 신규 컬럼 + 단가마스터 등록/수정 화면에 체크박스 추가, 체크된 항목만 `_자재별_처리()`의 단가미등록 수집에서 제외("미등록수집" 선택 인자 패턴 재사용, 회귀 없음). 사용자 추가 요구사항 — 기본이 무상인데 특정 자재만 유상 등록되는 모순을 양방향으로 차단: 무상 체크 시 `PricingMaterialSection`(자재별 단가)의 대응 코드가 신규 등록 대상에서 빠지고(정방향), 그 코드로 이미 등록된 자재별 단가가 있으면 무상 체크박스 자체가 비활성화(역방향). `py_compile`·`tsc`·`eslint`·`build` 통과, 로컬 MariaDB로 의뢰서 97531 재현 검증(무상=0→미등록 경고 뜸, 무상=1→사라짐, 원상복구 후 재현 확인). **사무실 PC 배포 완료** — `scripts/api.py`·`scripts/billing.py`·`scripts/init_db_mariadb.py`(신규 컬럼 3개 마이그레이션) 반영 + FastAPI 재시작, 사용자가 브라우저에서 확인 완료.
- **거래명세서품명이력 "확정 시마다 덮어쓰기"로 변경 — 사무실 PC 배포 완료** ✅: "새 행 추가"(수동입력)로 잘못된 품명을 넣고 확정한 뒤, 다음 확정에서 그 행을 전부 지우고(수동입력 0건) 확정해도 예전 잘못된 품명이 미리보기에 계속 자동 반영되던 문제(사용자 제보 — 삼성화재해상보험(주) "내용증명" 오타 품명이 2026-08-13 이후 계속 재등장). 원인: `POST /거래명세서요청`이 수동입력 행이 있을 때만 `거래명세서품명이력`에 INSERT/UPDATE하고, 0건이면 이 블록 자체를 건너뛰어(`if 품명별조:`) 기존 이력이 전혀 갱신되지 않던 구조 — "추가·갱신만 가능, 삭제 불가능"이었음. 확정할 때마다 그 거래처의 이력을 먼저 전부 `DELETE`한 뒤 이번 확정 내용으로만 다시 채우는 방식으로 변경(사용자 확정 — "확정 시마다 항상 이번 확정분으로 덮어쓰기"), 수동입력 0건이면 이력도 0건이 되어 자동 반영이 깨끗하게 사라짐. 로컬 MariaDB로 0건/1건 확정 시나리오 직접 시뮬레이션 검증(0건 확정 후 이력 완전히 비워짐, 1건 확정 후 그 1건만 남음 확인). **사무실 PC 배포 완료** — `scripts/api.py` 반영(DB 마이그레이션 불필요) + FastAPI 재시작.
- **"요청내용"(거래명세서 엑셀 첫 번째 표지 시트) 실데이터 채우기 — 로컬 PC 구현·검증 완료, 사무실 PC 배포는 대기 중** (`.claude/plans/plan_요청내용표지채우기.md`): 사용자가 "요청내역 시트도 생성된 거래명세서에 맞게 수정돼야 한다"고 요청 — 조사 결과 실제 이름은 "요청내용"으로, 이 프로젝트가 재활용한 신용카드 이용대금명세서 템플릿의 잔재 표지 페이지였고, 다른 시트의 빈 셀(Q23~Q31)을 참조하는 죽은 수식이라 항상 빈칸이었음. `billing.py`에 신규 `_요청내용_채우기()`(사업자등록증·법인명·발행일자·품목·공급가액·세액·합계·담당자이메일·비고·거래명세서번호를 리터럴 값으로 채움, 죽은 mailto 하이퍼링크도 제거) 추가, `_esc`/`_set_num`/`_set_str`을 모듈 레벨로 승격해 재사용. **사용자 확정 — 여러 "조"가 하나의 파일로 합쳐지는 다중시트 케이스에도 표지를 항상 포함**시키기로 해, 지금까지 "표지는 내부용이라 통합본에서 무조건 제외"하던 `combine_거래명세서_시트들()`의 기존 설계(2026-07-29)를 뒤집음 — 표지가 워크북 맨 앞(index 0)을 차지하면서 데이터 시트들의 인쇄영역(`_xlnm.Print_Area`) `localSheetId`가 전부 +1 밀리는 문제를 오프셋 보정으로 해결(`표지정보=None`이면 기존과 100% 동일하게 동작 — 레거시 `묶음번호` 경로는 이번 범위에서 제외해 회귀 없음). "비고"에는 사용자 확정대로 `"시트참조(시트명)"`(여러 조면 통합시트명) 형식으로 표기 — 원본 템플릿 C10의 기존 캐시값이 이미 이 표기를 쓰고 있었음을 확인. `scripts/api.py`(`_거래명세서_엑셀_시트목록()`에 거래처마스터.사업자등록번호 조회 1줄 추가 + 표지정보 조립, `GET /거래명세서엑셀/{no}` 호출부 갱신)까지 반영. 검증: `py_compile` 통과, 로컬 MariaDB 실데이터(D-202608-00088, 단일 시트)·합성 다중시트+통합시트 데이터 양쪽 종단 테스트(openpyxl로 표지 셀 값 확인) + 인쇄영역 `localSheetId` 교차검증(모든 definedName이 올바른 탭을 정확히 가리킴) + 레거시(`표지정보=None`) 단일/다중 경로 회귀 없음 확인 + 전체 zip 파트 well-formed XML 검증까지 통과. DB 스키마 변경 없음, 프론트엔드 변경 없음. **사용자가 원본 템플릿(`data/거래명세서_템플릿_base.xlsx`) 자체의 죽은 수식(요청내용 C2~C10)·담당자 연락처 기본값("노재민팀장", 거래명세서 시트 B31)을 직접 정리하기로 함**("data 폴더 원본 수정 금지" 원칙에 따라 Claude는 손대지 않음 — 코드는 이 정리 여부와 무관하게 정상 동작). 사무실 PC 배포는 다음 세션에서 사용자 확인 후 진행 예정.
- **거래명세서 미리보기 경고 배너 많을 때 확정·취소 버튼 접근 불가 버그 수정**(위 표지 기능 화면 테스트 중 사용자 실사용 제보): `InvoicePreviewDialog.tsx`의 바깥 모달 틀이 고정 높이(`h-[98vh]`)+`overflow-hidden`(스크롤 불가)인데, "장수·봉입건수 불일치"·"단가미등록" 경고 목록은 길이 제한이 없어 항목이 많으면(실사례 — BC카드 여러 업무명 동시 선택 시 경고 20건 이상) 그 아래 취소·확정 버튼이 화면 밖으로 밀려나고 스크롤도 안 돼 아예 접근 불가능해지던 문제. 두 경고 목록(`<ul>`)에만 `max-h-32 overflow-y-auto`(높이 제한 + 자체 스크롤)를 적용해 목록이 아무리 길어도 배너 전체 높이가 일정 이상 커지지 않도록 수정 — 다른 레이아웃·기존 배너 문구는 변경 없음. `tsc --noEmit`·`eslint` 통과, 프론트엔드 전용(사무실 PC 배포 대상 아님, 로컬 PC Next.js 재시작만으로 반영).
- **"요청내용" 표지 "담당자이메일" 필드 수정**(화면 테스트 중 사용자 발견 — 삼성화재해상보험(주) 실사례 스크린샷): 처음 구현 시 `_담당자_조회()`가 반환하는 **내부 계정 담당자**(거래명세서 본문 B31에 쓰이는 것과 동일한, 예: "서윤" 등 우리 회사 직원) 이메일을 잘못 넣었음 — 사용자가 "거래처 마스터"의 **수신이메일**(그 거래처 자체의 수신 이메일, 예: 삼성화재해상보험(주) → jooyeon1178.kang@samsung.com)이어야 한다고 확인해줌. `scripts/api.py`의 거래처마스터 조회 쿼리에 `수신이메일` 컬럼을 추가하고 `표지정보`의 키를 `담당자`→`수신이메일`로 교체, `scripts/billing.py`의 `_요청내용_채우기()`도 그 값을 그대로 쓰도록 수정(거래명세서 본문 B31 담당자 연락처는 기존 그대로 내부 담당자 유지, 영향 없음). 로컬 MariaDB(KB증권 — 수신이메일 미등록 케이스)로 빈 값 처리까지 재검증 완료, `py_compile` 통과.

### 2026-08-30
- **"요청내용" 표지 채우기 + 담당자이메일 수정 — 사무실 PC 배포 완료** ✅ (`.claude/plans/plan_요청내용표지채우기.md`, 체크리스트: `.claude/plans/plan_2026-08-29_사무실PC배포체크리스트_요청내용표지.md`): 전날(2026-08-29) 로컬 PC에서 구현·검증한 "요청내용" 표지 채우기 기능 전체(다중시트 표지 포함·담당자이메일→거래처 수신이메일 수정 포함)를 사무실 PC에 배포 — `scripts/api.py`·`scripts/billing.py` 반영(DB 마이그레이션 불필요) + FastAPI 재시작 + 사용자가 정리해둔 `data/거래명세서_템플릿_base.xlsx`(요청내용 C2~C10·거래명세서 시트 B31 빈 칸 정리본)도 같은 경로로 반영, 사용자가 사무실 PC 브라우저에서 실제 확인 완료. 로컬·사무실 PC 코드·템플릿 동기화 상태, 남은 미배포 항목 없음.
- **로그인 계정(`seonghk`) 비밀번호 초기화**: 로그인 실패 제보로 bcrypt 해시 재생성 후 DB 반영(`auth.비밀번호_확인()`으로 재검증 완료) — 사용자가 새 비밀번호로 로그인 확인.
- **거래처별 "조별표지" 분리(통합 명세서 대신 조별 표지 블록) — 사무실 PC 배포 완료** ✅ (`.claude/plans/plan_조별표지분리.md`): 삼성화재해상보험(주) 등 특정 거래처는 통합 명세서를 쓰지 않고 "요청내용" 표지에 조별 부분합을 2개씩 가로로(3개 이상이면 아래 행으로 이어서) 나눠 보여줘야 한다는 마케팅팀 요청(실제 발행 샘플로 레이아웃 확인) — 거래처마스터에 `조별표지`(TINYINT, 역발행과 동일 패턴) 신규 컬럼, 체크되면 저장 시점에 통합시트명을 시스템이 자동 무효화(`POST /거래명세서요청`)하고 다운로드 시 `billing._요청내용_다중블록_채우기()`(신규)가 원본 블록(A~D열)을 그대로 복제해 오른쪽(E~H열)·아래(row-block)로 확장하는 방식(원본이 이미 검증된 스타일이라 SKILL-11 "정상 원본 검증" 문제 없이 그대로 복제 재사용). 구현 중 좌/우 블록을 같은 행에 합치지 않고 덮어써 왼쪽 블록이 사라지던 버그 1건 발견·수정. 합성 데이터(조 2~6개)로 전체 zip well-formed XML + openpyxl 로드 검증, 기존 단일 블록 경로 회귀 없음 확인. `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` + 프론트엔드(`ClientFormDialog.tsx`·`ClientMasterTable.tsx`·`serverMappers.ts`·`InvoicePreviewDialog.tsx`) 반영, `tsc`·`eslint`·`build` 통과. **사무실 PC 배포 및 실사용 확인 완료**.
- 후속 버그 수정 2건(같은 날, 실사용 검증 중 발견): ① `PUT`/`POST /거래처마스터`의 Pydantic 모델·실제 INSERT/UPDATE SQL에 `조별표지` 컬럼 자체가 빠져 있어, 체크박스를 저장해도 프론트 낙관적 업데이트만 반영되고 DB엔 실제로 안 남던 문제(다른 탭 이동 후 해제된 것처럼 보임) — 모델·SQL 양쪽에 컬럼 추가로 수정. ② 표지 "품목" 필드가 라인아이템(표시품명) 조합이 아니라 그 조 시트의 B12(업무명)를 그대로 읽어와야 한다는 지적 — `billing.읽기_시트2_셀()` 신규(이미 생성된 시트에서 셀 값을 직접 읽어와, 업무명 계산 로직이 바뀌어도 표지가 항상 실제 시트와 일치) + "거래명세서번호" 행을 빈칸이 아니라 인보이스 공통값으로 채우도록 수정. "비고" 형식도 기존 단일 표지와 동일하게 `시트참조(시트명)`으로 통일.
- **거래명세서 개별 조 시트 상단 업무명(B12) 사용자 직접 입력 "조상단업무명" — 사무실 PC 배포 완료** ✅ (`.claude/plans/plan_조상단업무명.md`): 조별표지 검증 중, 개별 조 시트 B12에 자동으로 들어가던 업무명이 사실은 인보이스 전체 첫 값 하나를 모든 조 시트에 똑같이 쓰고 있어(`SELECT DISTINCT ... [0]`) 조마다 다른 업무명이 필요한 실사례(제휴증권 자동갱신증권 vs 맞춤형약관)와 안 맞는다는 게 확인됨 — 처음엔 "구분표시/규격/비고"(조건식 규칙 단위 입력)와 같은 방식을 검토했으나, 사용자가 "그러면 한 조에 규칙이 10개면 10번 입력해야 하냐"고 정확히 지적해, 이미 있는 "통합상단업무명"(조 2개 이상일 때 통합 시트 하나에 대해 미리보기 화면에서 한 번 입력받는 기존 기능)과 동일한 패턴을 조 단위로 한 단계 더 세분화해 재사용하는 것으로 설계 변경. 신규 테이블 `조상단업무명설정`(거래처+업무명조합+조 키, "마지막 입력값 재사용" 캐시)·`거래명세서_품목.조상단업무명` 컬럼, 신규 `GET /조별상단업무명기본값`, 미리보기 화면의 각 "작업구분" 그룹 헤더 줄(통합상단업무명 입력창과는 별개 위치 — 조별표지 체크 여부와 무관하게 항상 보임)에 입력칸 추가. `write_거래명세서_excel()` 시그니처는 안 바꾸고 호출부 한 곳(`_거래명세서_엑셀_시트목록()`)에서만 효과_업무명을 계산해 넘기는 방식이라 단일시트·통합시트 경로에 영향 없음 — 조별표지 표지의 "품목"도 이미 B12를 재읽기하는 구조라 코드 변경 없이 자동으로 새 값을 반영. `scripts/init_db_mariadb.py`·`scripts/api.py`·`InvoicePreviewDialog.tsx`·`Tab4Invoice.tsx` + 신규 Next.js 프록시 라우트(`frontend/app/api/group-title-defaults/route.ts`) 반영, `py_compile`·`tsc`·`eslint`·`build` 통과. **사무실 PC 배포 및 실사용 확인 완료**.
- 통합 명세서 케이스도 표지 "품목"이 B12 값을 따르도록 통일(같은 날 후속, BC카드 실사례 제보): 조별표지가 아닌 "통합 명세서" 케이스는 여전히 확정 시점에 저장된 구식 업무명 조합(`거래명세서.품목`)을 표지에 그대로 쓰고 있어 통합 시트 B12(통합상단업무명)와 어긋나던 문제 — `_거래명세서_엑셀_시트목록()`에서 통합바이트 생성 직후 `billing.읽기_시트2_셀(통합바이트, "B12")`로 `표지정보["품목"]`을 덮어쓰도록 수정(조별표지 때와 동일 원리). `scripts/api.py` 1줄 수정, **사무실 PC 배포 완료**. 이번 세션(조별표지분리·조상단업무명·후속 버그 3건) 전체 로컬·사무실 PC 코드·DB 동기화 상태, 남은 미배포 항목 없음.

### 2026-08-31
- **`docs/API규격서_생산공정관리시스템전달용.md`(생산공정관리시스템 개발 담당자 전달용 연동 문서) 갱신 완료** ✅ (`.claude/plans/plan_API규격서_생산공정시스템전달.md`): 문서 최종 갱신일(2026-07-20) 이후 실제 코드에는 반영됐지만 문서엔 빠져 있던 필드 2종을 발견해 반영 — ①`운영통계` 행에 2026-08-21 추가된 공정 세분화 필드 10개(압착·주소출력·봉입·수작업·중철·제본·무광코팅·유광코팅·에폭시·날개접지) 및 그중 5개는 "합산이 아니라 건수와 동일값을 반복 표시하는 방식(OR-flag)"이라는 실사례 기반 안내문 추가 ②`자재사용현황` 행에 2026-08-15 추가된 `자재코드` 필드 반영 — 배치 파일(`data/자재사용현황.xlsx`, 원본 컬럼명 `작업내역서자재`)로는 이미 들어오고 있는 값이나 실시간 API로는 아직 안 보내주고 있다는 점을 명시하고, `작업명`과 함께 실시간 연동 시 포함해달라는 요청 문구 추가. 서버 요청 크기·행 개수 제한 여부도 코드(uvicorn 기본 설정, pandas 벡터화 처리, DB 스키마)로 확인해 "행 개수 제한 없음" 안내 추가.
- **`docs/작업현황-대시보드-생성-KNOWLEDGE.md` 스테일 참조 2건 정정** ✅: "한눈에 보기" 이력에 이미 삭제된 플랜 파일(`plan_신규공정확장_씰링에폭시코팅제본중철.md`, `plan_장수기준_출력비청구_조사.md`)이 "미착수"로 남아있어 혼란을 유발하던 부분을, 실제로는 2026-08-22에 각각 다른 플랜으로 흡수·완료된 뒤 2026-08-30 삭제됐다는 내용으로 정정.
- **위 API 규격서를 가독성 있는 웹페이지(Claude Artifact)로 발행** ✅: 좌측 목차+스크롤 연동, JSON 하이라이트·복사 버튼, 필드 표 필수/선택 배지, 라이트·다크 테마를 갖춘 페이지로 재구성(원본 `.md` 내용 변경 없이 표현만 재구성) — https://claude.ai/code/artifact/185304f6-b07b-4905-8707-17929dcd70ac
- **남은 미확인 항목 2건 완료(같은 날 후속)** ✅ (`.claude/plans/plan_API규격서_생산공정시스템전달.md` 전체 완료로 종료): ①웹페이지에 `GET /health` 테스트 방법(브라우저 접속·curl 예시·오류 시 대응 표) 섹션 추가, 같은 링크로 재발행 완료 ②내부 원본 문서 `docs/API규격서.md`에도 전달용 문서와 동일한 내용(신규 공정 필드 10개·자재코드·행개수제한 안내)을 동기화, 최종 갱신일 2026-08-31로 갱신 — 전달용 문서·내부 원본 문서·웹페이지 3곳이 전부 일치하는 상태로 종료.
- **조건식(청구품목규칙)을 처음 만드는 동시에 확정하면 수정이력에서 품목 수량 변경이 통째로 누락되던 버그 수정 — 사무실 PC 배포까지 전부 완료** ✅ (`.claude/plans/bug_수정이력_신규규칙동시확정_품목변경누락.md`, 사용자가 D-202608-00133(KB증권, 골드&와이즈 책자 발송)에서 "출력비"를 "라벨지출력"으로 조건식 새로 만들면서 수량도 130→906으로 직접 고쳐 확정했는데, 수정이력에 공급가액·부가세·합계 변경만 남고 수량 변경 자체가 안 보인다고 제보): 원인은 `POST /거래명세서요청`의 감사이력 기준선(`기준목록`) 계산이 "이번 확정에 쓰인 규칙"이 아니라 "이번 확정 이전에 DB에 저장돼 있던 규칙"만 보고 있던 것 — 그 거래처+업무명에 조건식이 하나도 없던 최초 상태라 기준선이 원본(가공 전) 이름으로 폴백되고, 최종 품명(방금 만든 규칙 결과)과 이름이 안 맞아 2026-08-17에 넣어둔 "스푸리어스 품목삭제+품목추가 방지" guard가 이 케이스를 통째로 걸러버리면서 그 안에 섞인 진짜 수량 변경까지 함께 사라졌음. `scripts/api.py`에 `효과_규칙목록`(요청.규칙 우선, 없으면 기존 DB 조회값 폴백) 신규 도입 — 기준목록 계산과 guard 양쪽 모두 이 값을 쓰도록 교체(프론트가 매 확정마다 오른쪽 표의 조건식 있는 행 전체를 보내고, 저장 로직이 항상 "전체 교체" 방식이라 요청.규칙이 이번 확정 후 DB에 실제로 남을 규칙과 동일함을 활용). 부수 효과로 2026-08-18에 도입된 "원본 vs 최종 비교" 팝업 왼쪽 "원본" 칸(같은 `기준목록`을 `거래명세서_품목` 구분='기준'으로 저장)도 함께 정상화됨(예전엔 새 규칙이 적용 안 된 옛 품명으로 잘못 표시). `python -m py_compile` 통과, **실제 DB 데이터(D-202608-00133) 기반 재현 검증** — 저장된 원본 품목과 실제 저장된 규칙으로 수정된 로직을 그대로 재현해 스푸리어스 항목 없이 `수량: 130→906`·`금액` 두 건만 정확히 남는 것을 확인. DB 전체 스캔으로 영향 범위도 확인 — "편집됨인데 품목 계열 수정이력이 아예 없는" 건이 전체 통틀어 2건(D-202608-00088·D-202608-00133)뿐이며 둘 다 "조건식 최초 생성+동시 확정" 패턴과 정확히 일치함을 확인, 과거 2건은 소급 정정하지 않기로 확정(사용자 확정 — 수량 변경 자체는 공급가액·부가세·합계 변동으로 이미 간접 기록됨). **브라우저 실사용 재현 확인** — 테스트를 위해 KB증권+골드&와이즈의 조건식 2건을 삭제해 "최초 상태"를 재현한 뒤, 조건식 신규 생성+수량 동시 수정+확정 시나리오를 사용자가 직접 재현해 수정이력에 정상 기록됨을 확인. **사무실 PC 배포 완료(같은 날 후속)** — `scripts/api.py` 1개 파일만(DB 마이그레이션 불필요) 반영 + API 서버 재시작, 사용자가 사무실 PC 브라우저에서도 확인 완료. 로컬·사무실 PC 코드 동기화 상태, 남은 미배포 항목 없음. 체크리스트: `.claude/plans/plan_2026-08-31_사무실PC배포체크리스트_수정이력누락.md`.
- **자재별 단가 등록 화면 — "무상(고객사 제공)" 체크된 항목이 새 행 추가 시 여전히 선택지에 남아있던 버그 수정(같은 날, 프론트엔드 전용)** ✅: 사용자가 용지제작단가를 "무상"으로 체크한 거래처에서 "자재별 단가" → "+ 자재단가 추가"를 열었더니 차단 대상인 "출력자재비(용지)"가 여전히 항목 드롭다운에 보인다고 제보 — 원인은 `PricingMaterialSection.tsx`의 `openNew()`가 새 행의 항목(코드) 기본값을 항상 `"출력자재비"`로 하드코딩하고 있었는데, 무상 차단 필터(2026-08-25)가 "지금 선택된 값은 예외로 계속 보여준다"는 규칙을 갖고 있어(수정 중인 기존 행의 값을 안 지우기 위한 설계) 하필 그 하드코딩된 기본값이 차단 대상 코드와 겹쳐 자기 자신을 예외 처리해버렸음(봉투·삽지 무상은 기본값과 겹치지 않아 문제없이 정상 동작하고 있었음). `openNew()`가 차단 안 된 첫 항목(대부분 "출력비" — 자재비가 아니라 서비스 비용이라 애초에 차단 대상이 아님)을 기본값으로 고르도록 수정. `tsc`·`eslint` 통과, 프론트엔드 전용이라 사무실 PC 배포 대상 아님(로컬 PC Next.js 재시작만으로 반영).

### 2026-09-01
- **거래명세서 원본 표 합산 로직에 "업무명" 구분 키 추가 — 사무실 PC 배포 완료** ✅ (`.claude/plans/bb-ab-vast-piglet.md`): 사용자가 "코드 BB·AB(공정단가) 품목의 비고를 조건식에서 자재명처럼 선택 가능하게 해달라"고 요청 — 실제 화면(강남대성수능연구소(주), A교재·S교재가 둘 다 작업명 "컬러 표지제작"을 씀)을 캡처해 검증하는 과정에서 더 근본적인 문제로 확인됨: 무광코팅비 수량이 A타입(520)+S타입(1,792)의 합인 2,312로 표시돼, 서로 다른 업무명의 수량·금액이 원본 표 집계 단계에서 이미 한 줄로 합쳐지고 있었음(P/F 자재단가 품목은 자재명이 합산 키에 있어 문제없이 분리되지만, BB/AB처럼 자재명이 없는 품목은 업무명이 달라도 작업명+단가가 같으면 그대로 합쳐짐). `scripts/billing.py`의 공유 집계 함수(`_작업별_품목누적()`·`_자재별_처리()`·`_자재비례배분_처리()`·`build_품목행()` 우편요금 처리) 전부의 합산 키를 `(품목,작업,단가,자재명)` → `(품목,작업,단가,업무,자재명)`으로 변경해 근본 수정 — 모든 품목 코드가 공유하는 로직이라 한 번에 전체 해결됨. `정렬행_원본목록()`에 "업무명" 필드 신규 추가해 조건식에도 "업무명" 필드로 노출(`ConditionRuleModal.tsx`·`InvoicePreviewDialog.tsx`) — 애초 요청이었던 "비고 조건 필드"는 이걸로 대체돼 진행하지 않기로 사용자 확정("코드==BB AND 업무명==A교재" 식으로 직접 구분 가능해짐). 왼쪽 원본 표도 업무명이 2개 이상 섞이면 업무명별 그룹 헤더(오른쪽 표의 "작업구분" 그룹 헤더와 동일 패턴, 소계 없음)로 묶어 보여주도록 개선. 검증: 로컬 MariaDB 실제 데이터(A교재+S교재 컬러 표지제작)로 A/S 단독 실행 합계와 합쳐서 실행한 합계가 정확히 일치함을 확인(204,787,714원, 회귀 없음 — 줄만 분리되고 총액은 불변). `python -m py_compile`·`npx tsc --noEmit`·`npx eslint .`·`npm run build` 전부 통과. **사무실 PC 배포 완료** — `scripts/billing.py`·`scripts/api.py` 반영(DB 마이그레이션 불필요) + FastAPI 재시작, 사용자가 실사용 확인 완료.
- **"편집됨" 오탐 버그 수정(같은 날, 후속) — 사무실 PC 배포 완료** ✅: 위 검증 중 실제로 아무 것도 안 고친 확정 건에 "편집됨" 배지와 "공급가액"·"합계" 수정이력이 잘못 남는 것을 발견 — 원인은 "내지 출력(칼라 HD)" 245,484장×16.8원=4,124,131.2원처럼 소수 단가 품목이 섞이면 합계에 0.2원 같은 잔여가 남는데, `POST /거래명세서요청`의 감사이력 비교 기준값(`기준_공급가액`/`기준_세액`)은 이 잔여를 그대로 두는 반면 프론트(`Tab4Invoice.tsx`)는 제출 직전 `Math.round()`로 항상 원 단위 정수로 반올림해서 보내 생기던 반올림 단위 불일치였음(업무명 작업과 무관한 기존 버그, 소수 단가 품목이 섞인 모든 "별도" 거래처에서 재현 가능). `scripts/api.py`에 `기준_공급가액, 기준_세액 = round(기준_공급가액), round(기준_세액)` 한 줄 추가 — 비교 기준값도 프론트와 같은 원 단위로 통일(공용 함수 `billing.부가세_표시분리()` 자체는 다른 5개 호출부에 영향 없도록 건드리지 않음). 실제 문제 값(기준_품목합=8,690,025.20)으로 재계산해 반올림 후 저장된 값(8,690,025/869,003)과 정확히 일치함을 확인. **사무실 PC 배포 완료** — `scripts/api.py` 반영 + API 서버 재시작.
- **"장수/자재사용량 불일치"·"단가미등록" 오탐 버그 수정(같은 날, 후속) — 사무실 PC 배포 완료** ✅: 업무명 분리가 정상 동작하는지 화면에서 확인하다, 실제로는 아무 문제 없는 항목인데도 "장수(520)와 용지 자재사용량(2,312)이 다름"(두 줄 다 자재사용량이 A+S 합친 값으로 동일) 경고와 "출력비·출력자재비 단가 미등록" 오탐이 뜨는 것을 발견 — 원인은 `build_품목행()`의 자재별 세부내역 누적 루프와 `자재수량_불일치_목록()`의 동일 루프가 `for 번호 in 의뢰서번호셋:`으로 "이 거래명세서에 묶인 전체 의뢰서"(여러 업무명이 섞일 수 있음)를 그대로 훑으면서 `자재map.get((번호,작업))`을 업무명 구분 없이 조회하고 있었던 것 — A업무 그룹을 처리할 때도 S업무의 같은 작업명 자재 사용량까지 섞여 들어가 ①불일치 경고는 양쪽 다 합친 값으로 잘못 보이고 ②S업무 자재("S타입" 라벨)가 A업무 전용 자재단가와 매칭이 안 돼 미등록으로 잘못 잡혔음(업무명이 다르면 실제 금액이 틀어질 수도 있었던 잠재 결함, 이번 사례는 두 업무 단가가 우연히 같아 금액 자체는 무사했음). `scripts/billing.py`의 두 함수 모두에 `(작업명,거래처명,업무명) → 그 그룹 소속 의뢰서번호 집합` 매핑(`_그룹의뢰서맵`)을 신규 도입해 순회 범위를 그 그룹 자신의 의뢰서로 한정. 실제 DB로 재현 검증 — 수정 전 "컬러 표지제작"에 대해 미등록수집 4건·불일치 경고 2건이 잡히던 것이 수정 후 둘 다 0건으로 정상화됨을 확인, 총합계 불변 회귀도 재검증(204,787,714원 그대로 일치). **사무실 PC 배포 완료** — `scripts/billing.py` 반영 + API 서버 재시작.
- **거래명세서 Excel "구분자 문구" 행 삽입 기능 신규 — 사무실 PC 배포 완료** ✅ (`.claude/plans/bb-ab-vast-piglet.md`): 사용자가 참고 예시 엑셀에서 품목 행 사이에 품명~금액 열이 하나로 병합된 구분선 문구("S교재"/"A교재")가 들어간 걸 보고 요청 — 자료(업무명 등) 기반 자동 그룹핑이 아니라, "새 행 추가"처럼 사용자가 원하는 위치에 직접 문구만 입력하는 순수 수동 기능으로 확정(배경색 없음). `scripts/billing.py`의 `write_거래명세서_excel()`에 신규 헬퍼 `_구분자_밴드_적용()` — 그 행의 품명(D:G)·금액(K:M) 병합 두 개를 지우고 D:M 하나로 합쳐 캡션 텍스트만 기입(`<mergeCells count>` 갱신 포함, 기존 `_insert_extra_item_rows()`가 쓰는 것과 동일한 병합 개수 갱신 패턴 재사용). `거래명세서_품목.구분자` 컬럼 신규(DB 마이그레이션, `scripts/init_db_mariadb.py`) — `scripts/api.py`의 Pydantic 모델·저장(원본/기준 행은 항상 false, 최종 행만 실제값)·`GET /거래명세서엑셀/{no}` 조회 경로 전부 반영. 프론트엔드(`InvoicePreviewDialog.tsx`)에 "+ 구분자 추가" 버튼(맨 아래 추가) 신규 — `renderRightRow()`가 구분자 행이면 품명·작업구분·수량·단가·금액 5칸을 하나의 캡션 입력창으로 렌더링. 검증: `billing.write_거래명세서_excel()`을 직접 호출해 만든 실제 xlsx를 `openpyxl`로 열어 병합 범위(`D17:M17`)·캡션 텍스트·앞뒤 일반 행 무변화·`mergeCells count` 정합성(69→68) 전부 확인. `python -m py_compile`·로컬 마이그레이션 실행·`npx tsc --noEmit`·`npx eslint .`·`npm run build` 전부 통과. **사무실 PC 배포 완료** — `scripts/billing.py`·`scripts/api.py`·`scripts/init_db_mariadb.py` 반영 + 마이그레이션 실행 + API 서버 재시작.
- **구분자 위치 지정 삽입 버튼 + 품명이력 자동 재사용 제외(같은 날, 후속) — 사무실 PC 배포 완료** ✅: 실사용 중 "+ 구분자 추가"가 항상 맨 아래에 행을 만들어, 원하는 위치(예: 맨 위)로 옮기려면 ▲ 버튼을 여러 번 눌러야 하는 번거로움과, 확정한 구분자가 다음 미리보기 오픈 시 기존 "새 행 추가" 이력 재사용 기능에 같이 휩쓸려 원래 자리와 무관하게 하단에 자동으로 다시 나타나는 문제를 연달아 제보받아 수정 — ① `InvoicePreviewDialog.tsx`에 각 행마다 "구분자삽입" 버튼(▲▼복사삭제 옆) 신규, `insertSeparatorAbove(idx)`로 원하는 행 바로 위에 즉시 삽입(이동 불필요) ② `scripts/api.py`의 품명이력 저장 조건에 `and not r.get("구분자")` 추가 — 구분자는 애초에 "품명이력"에 저장되지 않아 다음번엔 자동 반영되지 않음(필요하면 그때그때 다시 추가). 배포 직후 로컬 DB에 이전(수정 전) 세션에서 저장된 구분자 이력 3건("A 교재"·"S 교재"·"이 하 여 백")이 아직 남아있어 그대로 재등장하는 것을 발견 — 코드 배포와 무관한 잔존 데이터였음을 확인하고 직접 삭제해 정리, 사용자가 재확인해 정상 동작 확인. **사무실 PC 배포 완료** — `scripts/api.py` 반영 + API 서버 재시작, 사용자 실사용 확인까지 완료. 오늘(업무명 합산 키+오탐 2건+구분자 기능+후속 2건) 작업 전체 로컬·사무실 PC 코드·DB 동기화 상태, 남은 미배포 항목 없음. 체크리스트: `.claude/plans/plan_2026-09-01_사무실PC배포체크리스트.md`.
