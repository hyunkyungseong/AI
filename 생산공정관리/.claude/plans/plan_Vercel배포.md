# 플랜: Vercel 배포 (Next.js 프론트엔드 외부 공개)

> 상태: ⏳ 미착수 | 분리 이력: 2026-07-31, `plan_1단계_MariaDB전환.md`의 [5단계](외부 접속 통로 준비)를 이 파일로 이동
> 최종 목표: Next.js(`frontend/`)를 Vercel에 배포해, 사무실 PC FastAPI(`192.168.30.201:8001`)를 인터넷 경유로 안전하게 호출
> 상세 배경: `docs/작업현황-대시보드-생성-KNOWLEDGE.md` "최종 배포 목표" 섹션

---

## 왜 지금 시작 안 하는가 (2026-07-19 결정, 계속 유효)

로컬(`localhost:3000`)에서 화면을 더 다듬은 뒤 배포하기로 함 — Next.js 5단계(탭1~4·거래처 마스터)는 2026-07-19 전체 완료됐고, 이후로도 거래명세서 규칙 엔진·조별 분할발급 등 화면이 계속 다듬어지는 중(`docs/CHANGELOG.md` 참고). **실제 착수 시점은 사용자가 "이제 배포하자"고 판단할 때** — 그전엔 아래 체크리스트를 미리 준비만 해두는 용도.

## 체크리스트

- [ ] 1. Cloudflare Tunnel 설치·구성 (무료, 공인 IP·포트포워딩 노출 없이 HTTPS 주소 확보로 Vercel → 사무실 PC FastAPI 연결) — **사용자 동의 필요**
- [ ] 2. Vercel 계정·프로젝트 생성, `frontend/` 폴더를 GitHub 연동 자동배포로 연결
- [ ] 3. Vercel 환경변수에 `FASTAPI_URL`을 Cloudflare Tunnel 주소로 설정 (`.env.local`의 로컬용 값과 별개)
- [ ] 4. 배포 후 실제 팀원 브라우저로 접속 확인 (로그인 → 탭 5개 전체 동작)

## 참고 — 이미 끝난 것 (착수 시 다시 안 해도 됨)

- Next.js 프론트엔드 자체 구현: `plan_5단계_Next.js프론트엔드.md` 전체 완료(2026-07-19)
- FastAPI 로그인 인증(JWT), 사내 전용 보안 구조: `plan_1단계_MariaDB전환.md` [3단계] 완료
- 사무실 PC에 FastAPI·MariaDB 이미 상시 구동 중(포트 8001): `plan_1단계_MariaDB전환.md` [6단계] 완료

---

*생성일: 2026-07-31 (plan_1단계_MariaDB전환.md에서 분리)*
