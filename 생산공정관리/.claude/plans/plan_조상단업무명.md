# 거래명세서 개별 조 시트 상단 업무명(B12) 사용자 직접 입력 — "조상단업무명"

> 승인된 상세 설계: `C:\Users\hyunkyung\.claude\plans\d202608-00121-2-vast-boole.md` (plan mode 승인, 2026-08-30)

## Context
개별 조 시트마다 B12에 자동으로 들어가는 "업무명"이 지금은 인보이스 전체 첫 값 하나를 모든 조
시트에 똑같이 쓰고 있음(조별표지 검증 중 발견). 조마다 다른 업무명을 사용자가 직접 입력할 수
있어야 함. "구분표시/규격/비고"(규칙 단위) 대신 "통합상단업무명"(조 단위, 미리보기 그룹 헤더에서
한 번만 입력)과 같은 패턴으로 설계 확정.

## 체크리스트

- [x] 1단계 — DB: `조상단업무명설정` 신규 테이블 + `거래명세서_품목.조상단업무명` 컬럼(로컬 마이그레이션 실행·확인 완료)
- [x] 2단계 — 백엔드: `GET /조별상단업무명기본값` 신규 엔드포인트
- [x] 3단계 — 백엔드: 확정 요청 Pydantic 모델(`조상단업무명맵`) + `POST /거래명세서요청` 저장 로직
      (INSERT에 컬럼 추가 + `조상단업무명설정` upsert)
- [x] 4단계 — 백엔드: `_거래명세서_엑셀_시트목록()`에서 `효과_업무명` 계산해 개별 조 시트에 적용
      (조별표지 표지 "품목"도 자동으로 함께 반영됨 — 기존에 B12를 재읽기하는 방식으로 짜둔 덕분)
- [x] 5단계 — 프론트엔드: `InvoicePreviewDialog.tsx` state·effect·그룹헤더 입력칸(작업구분 줄)·
      확정 payload·타입, `Tab4Invoice.tsx`의 `edited` 타입·payload도 함께 갱신
- [x] 6단계 — 프론트엔드: 신규 GET 프록시 라우트 `frontend/app/api/group-title-defaults/route.ts`
- [x] 7단계(자동 검증) — py_compile 통과, 로컬 마이그레이션 실행+테이블/컬럼 확인,
      tsc/eslint/build 전부 통과(신규 라우트 `/api/group-title-defaults` 빌드 산출물 확인)
- [x] 7단계(사용자 검증) — 사용자가 실제 브라우저에서 확인 완료
- [x] 8단계 — 통합 명세서(조별표지 아닌 경우) 케이스도 표지 "품목"이 B12 값을 따르도록 통일
      (BC카드 실사례 제보, `_거래명세서_엑셀_시트목록()`에서 통합바이트 생성 직후
      `billing.읽기_시트2_셀(통합바이트, "B12")`로 `표지정보["품목"]` 덮어씀 — `scripts/api.py` 1줄)
- [x] 9단계 — `docs/CHANGELOG.md` 기록 완료 + 사무실 PC 배포 완료(체크리스트:
      `.claude/plans/plan_2026-08-30_사무실PC배포체크리스트.md`) — 상태: ✅ 전체 완료

## 화면 테스트 체크리스트 (사용자 진행)

1. `API서버_재시작.bat` + `웹화면_재시작.bat` 실행
2. 조가 2개 이상인 거래처로 [거래명세서 관리] → 의뢰서 선택 → "거래명세서 요청" 클릭
   → 예상 결과: 오른쪽 표의 각 "작업구분: OOO (N건)" 줄 우측에 작은 입력칸("시트 상단 업무명 —
     비워두면 자동")이 보여야 함(작업구분 복사 버튼 왼쪽)
3. 두 작업구분에 서로 다른 텍스트 입력 후 확정
4. 발행완료 목록에서 다운로드 → 각 작업구분 시트의 상단(B12 자리)이 입력한 텍스트와 일치하는지 확인
   (조별표지 거래처면 "요청내용" 표지의 "품목"도 같은 텍스트로 바뀌어야 함)
5. 같은 거래처+같은 업무명 조합으로 다시 "거래명세서 요청"을 열어봤을 때, 입력했던 값이 그대로
   미리 채워져 있는지 확인
6. 입력 안 한(빈칸) 작업구분은 기존처럼 자동 계산된 업무명이 그대로 나오는지 확인(회귀 없음)

❌ 오류 시: 빨간색 오류 메시지를 캡처하거나 내용을 그대로 복사해서 알려주세요.
