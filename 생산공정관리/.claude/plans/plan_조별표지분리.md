# 거래처별 "조별 표지 분리" — 통합명세서 대신 요청내용에 조별 블록 표시

> 승인된 상세 설계: `C:\Users\hyunkyung\.claude\plans\d202608-00121-2-vast-boole.md` (plan mode에서 승인됨, 2026-08-30)

## Context
거래명세서번호 D202608-00121(삼성화재해상보험, 조 2개)이 기본 원칙대로 통합명세서를 생성했으나,
이 거래처는 통합명세서 없이 "요청내용" 표지에 조별 부분합을 2개씩 가로로, 조가 2개를 넘으면
세로로 이어붙여 보여줘야 함(마케팅팀 실제 발행 샘플 확인). 거래처마스터 boolean 플래그(역발행과
동일 패턴)로 관리, 조 개수 제한 없이 지원, 플래그 켜진 거래처는 통합시트명을 시스템이 자동 차단.

## 체크리스트

- [x] 0단계 — `data/거래명세서_템플릿_base.xlsx` sheet1.xml 재덤프해 row-block 구조 확정(row2~11,
      B/C열만 존재, 라벨은 sharedStrings 참조, 거래명세서번호 행은 블록마다 빈칸)
- [x] 1단계 — DB: `거래처마스터.조별표지` 컬럼 추가 (init_db_mariadb.py CREATE + 마이그레이션,
      로컬 MariaDB 마이그레이션 실행 및 컬럼 존재 확인 완료)
- [x] 2단계 — 백엔드: `POST /거래명세서요청`에서 조별표지 거래처 통합시트명 자동 차단(저장 시점)
- [x] 3단계 — 백엔드: `_거래명세서_엑셀_시트목록()`에서 조별 표지 데이터(조별목록) 조립 +
      다운로드 경로 이중 방어(`not 조별표지`)
- [x] 4단계 — billing.py: 2블록(A~H) 뼈대 + row-block 복제 헬퍼 + `_요청내용_다중블록_채우기()`
      (셀 병합 버그 1건 발견·수정 — 좌/우 블록을 같은 행에 합치치 않고 덮어쓰던 문제)
- [x] 5단계 — billing.py: `combine_거래명세서_시트들()` 분기 연결(len<=1/len>1 두 지점 모두)
- [x] 6단계 — 프론트엔드: ClientFormDialog 체크박스, ClientMasterTable 뱃지(헤더 "설정"으로 통합),
      Dashboard.tsx 타입, serverMappers 변환, ClientMaster.tsx Pick 타입
- [x] 7단계 — 프론트엔드: InvoicePreviewDialog 통합시트명 UI 숨김(안내문 대체) + 요청 body 조건부
      처리 + 조정불가 안내문 분기. 미리보기 응답에 `조별표지` 필드 추가(api.py)
- [x] 8단계(자동 검증) — py_compile 통과, 로컬 마이그레이션 실행+컬럼 확인, 합성데이터(조 2/3/4/5/6개)
      well-formed XML(minidom, 전체 zip 파트) + openpyxl 로드 검증, 기존 단일블록 경로 회귀 없음 확인,
      tsc/eslint/build 전부 통과
- [x] 실사용 검증 중 발견된 버그 2건 수정(2026-08-30, 사용자 실제 화면 확인 중 발견):
      ① `PUT`/`POST /거래처마스터`의 Pydantic 모델·실제 SQL에 `조별표지` 컬럼 자체가 빠져있어
      체크박스 저장이 DB에 반영 안 되던 문제(화면엔 낙관적 업데이트로 저장된 것처럼 보였음) —
      두 모델·INSERT·UPDATE문 전부 수정
      ② 표지 "품목" 필드를 라인아이템(표시품명) 조합 대신 그 조 시트의 B12(업무명, 실제
      write_거래명세서_excel()이 써넣은 값)를 그대로 읽어오도록 변경(`billing.읽기_시트2_셀()`
      신규) + "거래명세서번호" 행을 빈칸이 아니라 인보이스 공통값으로 채우도록 수정
- [x] 8단계(사용자 검증) — 사용자가 실제 브라우저에서 확인 완료(비고 형식을 기존 단일 표지와
      동일하게 `시트참조(시트명)`으로 통일해달라는 요청 반영)
- [x] 9단계 — `docs/CHANGELOG.md` 기록 완료 + 사무실 PC 배포 완료(체크리스트:
      `.claude/plans/plan_2026-08-30_사무실PC배포체크리스트.md`) — 상태: ✅ 전체 완료

## 화면 테스트 체크리스트 (사용자 진행)

1. API서버_재시작.bat 실행(코드 반영) → [거래처 마스터] 탭 → 임의 거래처 하나 수정 화면 열기
   → 예상 결과: "조별표지" 체크박스가 "역발행" 밑에 보여야 함
2. 그 거래처에 체크박스 체크 후 저장 → 목록으로 돌아와 확인
   → 예상 결과: 그 행의 "설정" 칸에 "조별표지" 배지가 떠야 함
3. 조가 2개 이상인 그 거래처로 [거래명세서 관리] → 의뢰서 선택 → "거래명세서 요청" 클릭
   → 예상 결과: 통합시트명 입력칸 대신 "이 거래처는 조별표지 대상이라 통합 명세서를 만들지
     않습니다" 안내문이 보여야 함
4. 확정 후 발행완료 목록에서 그 거래명세서를 다운로드
   → 예상 결과: 통합 명세서 시트 없이 작업구분별 시트만 있어야 하고, "요청내용" 표지에 각
     작업구분이 옆으로 2개씩(3개 이상이면 아래로 이어서) 나뉘어 표시돼야 함(번호·품목·공급가액·
     세액·합계·비고 각각 정확히)

❌ 오류 시: 빨간색 오류 메시지를 캡처하거나 내용을 그대로 복사해서 알려주세요.
